import Xr, { useRef as kt, useCallback as Se, useSyncExternalStore as rn, useEffect as ke, useMemo as sn, useState as ei, createElement as fs } from "react";
function X(e) {
  if (typeof e == "number")
    return (Math.abs(e * 2654435761) >>> 0).toString(16);
  if (typeof e == "boolean")
    return e ? "1" : "0";
  if (e === null)
    return "null";
  if (e === void 0)
    return "undefined";
  if (typeof e == "string") {
    let t = 2166136261;
    for (let n = 0; n < e.length; n++)
      t ^= e.charCodeAt(n), t += (t << 1) + (t << 4) + (t << 7) + (t << 8) + (t << 24), t = t >>> 0;
    return t.toString(16);
  }
  if (Array.isArray(e)) {
    let t = 2166136261;
    for (let n = 0; n < e.length; n++) {
      t ^= (n + 1) * 2654435761;
      const r = X(e[n]);
      for (let i = 0; i < r.length; i++)
        t ^= r.charCodeAt(i), t *= 16777619, t = t >>> 0;
    }
    return t.toString(16);
  }
  if (typeof e == "object") {
    let t = 2166136261;
    const n = Object.keys(e).sort();
    for (let r = 0; r < n.length; r++) {
      const i = n[r], s = X(i);
      t ^= parseInt(s, 16), t *= 16777619, t = t >>> 0;
      const o = X(e[i]);
      t ^= parseInt(o, 16), t *= 16777619, t = t >>> 0;
    }
    return t.toString(16);
  }
  return X(String(e));
}
const D = {
  Remove: "remove",
  Replace: "replace",
  Add: "add"
}, ti = Symbol.for("__MUTATIVE_PROXY_DRAFT__"), ls = Symbol("__MUTATIVE_RAW_RETURN_SYMBOL__"), Ot = Symbol.iterator, oe = {
  mutable: "mutable",
  immutable: "immutable"
}, xn = {};
function nt(e, t) {
  return e instanceof Map ? e.has(t) : Object.prototype.hasOwnProperty.call(e, t);
}
function hr(e, t) {
  if (t in e) {
    let n = Reflect.getPrototypeOf(e);
    for (; n; ) {
      const r = Reflect.getOwnPropertyDescriptor(n, t);
      if (r)
        return r;
      n = Reflect.getPrototypeOf(n);
    }
  }
}
function Dn(e) {
  return Object.getPrototypeOf(e) === Set.prototype;
}
function Un(e) {
  return Object.getPrototypeOf(e) === Map.prototype;
}
function se(e) {
  var t;
  return (t = e.copy) !== null && t !== void 0 ? t : e.original;
}
function Pe(e) {
  return !!C(e);
}
function C(e) {
  return typeof e != "object" ? null : e == null ? void 0 : e[ti];
}
function Ln(e) {
  var t;
  const n = C(e);
  return n ? (t = n.copy) !== null && t !== void 0 ? t : n.original : e;
}
function he(e, t) {
  if (!e || typeof e != "object")
    return !1;
  let n;
  return Object.getPrototypeOf(e) === Object.prototype || Array.isArray(e) || e instanceof Map || e instanceof Set || !!(t != null && t.mark) && ((n = t.mark(e, oe)) === oe.immutable || typeof n == "function");
}
function ni(e, t = []) {
  if (Object.hasOwnProperty.call(e, "key")) {
    const n = e.parent.copy, r = C(me(n, e.key));
    if (r !== null && (r == null ? void 0 : r.original) !== e.original)
      return null;
    const i = e.parent.type === 3, s = i ? Array.from(e.parent.setMap.keys()).indexOf(e.key) : e.key;
    if (!(i && n.size > s || nt(n, s)))
      return null;
    t.push(s);
  }
  if (e.parent)
    return ni(e.parent, t);
  t.reverse();
  try {
    hs(e.copy, t);
  } catch {
    return null;
  }
  return t;
}
function Ie(e) {
  return Array.isArray(e) ? 1 : e instanceof Map ? 2 : e instanceof Set ? 3 : 0;
}
function me(e, t) {
  return Ie(e) === 2 ? e.get(t) : e[t];
}
function ut(e, t, n) {
  Ie(e) === 2 ? e.set(t, n) : e[t] = n;
}
function Ht(e, t) {
  const n = C(e);
  return (n ? se(n) : e)[t];
}
function we(e, t) {
  return e === t ? e !== 0 || 1 / e === 1 / t : e !== e && t !== t;
}
function on(e) {
  if (e)
    for (; e.finalities.revoke.length > 0; )
      e.finalities.revoke.pop()();
}
function Ce(e, t) {
  return t ? e : [""].concat(e).map((n) => {
    const r = `${n}`;
    return r.indexOf("/") === -1 && r.indexOf("~") === -1 ? r : r.replace(/~/g, "~0").replace(/\//g, "~1");
  }).join("/");
}
function hs(e, t) {
  for (let n = 0; n < t.length - 1; n += 1) {
    const r = t[n];
    if (e = me(Ie(e) === 3 ? Array.from(e) : e, r), typeof e != "object")
      throw new Error(`Cannot resolve patch at '${t.join("/")}'.`);
  }
  return e;
}
function ps(e) {
  const t = Object.create(Object.getPrototypeOf(e));
  return Reflect.ownKeys(e).forEach((n) => {
    let r = Reflect.getOwnPropertyDescriptor(e, n);
    if (r.enumerable && r.configurable && r.writable) {
      t[n] = e[n];
      return;
    }
    r.writable || (r.writable = !0, r.configurable = !0), (r.get || r.set) && (r = {
      configurable: !0,
      writable: !0,
      enumerable: r.enumerable,
      value: e[n]
    }), Reflect.defineProperty(t, n, r);
  }), t;
}
const ys = Object.prototype.propertyIsEnumerable;
function ri(e, t) {
  let n;
  if (Array.isArray(e))
    return Array.prototype.concat.call(e);
  if (e instanceof Set) {
    if (!Dn(e)) {
      const r = Object.getPrototypeOf(e).constructor;
      return new r(e.values());
    }
    return Set.prototype.difference ? Set.prototype.difference.call(e, /* @__PURE__ */ new Set()) : new Set(e.values());
  } else if (e instanceof Map) {
    if (!Un(e)) {
      const r = Object.getPrototypeOf(e).constructor;
      return new r(e);
    }
    return new Map(e);
  } else if (t != null && t.mark && (n = t.mark(e, oe), n !== void 0) && n !== oe.mutable) {
    if (n === oe.immutable)
      return ps(e);
    if (typeof n == "function") {
      if (t.enablePatches || t.enableAutoFreeze)
        throw new Error("You can't use mark and patches or auto freeze together.");
      return n();
    }
    throw new Error(`Unsupported mark result: ${n}`);
  } else if (typeof e == "object" && Object.getPrototypeOf(e) === Object.prototype) {
    const r = {};
    return Object.keys(e).forEach((i) => {
      r[i] = e[i];
    }), Object.getOwnPropertySymbols(e).forEach((i) => {
      ys.call(e, i) && (r[i] = e[i]);
    }), r;
  } else
    throw new Error("Please check mark() to ensure that it is a stable marker draftable function.");
}
function Q(e) {
  e.copy || (e.copy = ri(e.original, e.options));
}
function Ze(e) {
  if (!he(e))
    return Ln(e);
  if (Array.isArray(e))
    return e.map(Ze);
  if (e instanceof Map) {
    const n = Array.from(e.entries()).map(([r, i]) => [
      r,
      Ze(i)
    ]);
    if (!Un(e)) {
      const r = Object.getPrototypeOf(e).constructor;
      return new r(n);
    }
    return new Map(n);
  }
  if (e instanceof Set) {
    const n = Array.from(e).map(Ze);
    if (!Dn(e)) {
      const r = Object.getPrototypeOf(e).constructor;
      return new r(n);
    }
    return new Set(n);
  }
  const t = Object.create(Object.getPrototypeOf(e));
  for (const n in e)
    t[n] = Ze(e[n]);
  return t;
}
function Et(e) {
  return Pe(e) ? Ze(e) : e;
}
function be(e) {
  var t;
  e.assignedMap = (t = e.assignedMap) !== null && t !== void 0 ? t : /* @__PURE__ */ new Map(), e.operated || (e.operated = !0, e.parent && be(e.parent));
}
function pr() {
  throw new Error("Cannot modify frozen object");
}
function $e(e, t, n, r, i) {
  {
    n = n ?? /* @__PURE__ */ new WeakMap(), r = r ?? [], i = i ?? [];
    const o = n.has(e) ? n.get(e) : e;
    if (r.length > 0) {
      const u = r.indexOf(o);
      if (o && typeof o == "object" && u !== -1)
        throw r[0] === o ? new Error("Forbids circular reference") : new Error(`Forbids circular reference: ~/${i.slice(0, u).map((a, d) => {
          if (typeof a == "symbol")
            return `[${a.toString()}]`;
          const c = r[d];
          return typeof a == "object" && (c instanceof Map || c instanceof Set) ? Array.from(c.keys()).indexOf(a) : a;
        }).join("/")}`);
      r.push(o), i.push(t);
    } else
      r.push(o);
  }
  if (Object.isFrozen(e) || Pe(e)) {
    r.pop(), i.pop();
    return;
  }
  switch (Ie(e)) {
    case 2:
      for (const [u, a] of e)
        $e(u, u, n, r, i), $e(a, u, n, r, i);
      e.set = e.clear = e.delete = pr;
      break;
    case 3:
      for (const u of e)
        $e(u, u, n, r, i);
      e.add = e.clear = e.delete = pr;
      break;
    case 1:
      Object.freeze(e);
      let o = 0;
      for (const u of e)
        $e(u, o, n, r, i), o += 1;
      break;
    default:
      Object.freeze(e), Object.keys(e).forEach((u) => {
        const a = e[u];
        $e(a, u, n, r, i);
      });
  }
  r.pop(), i.pop();
}
function Nn(e, t) {
  const n = Ie(e);
  if (n === 0)
    Reflect.ownKeys(e).forEach((r) => {
      t(r, e[r], e);
    });
  else if (n === 1) {
    let r = 0;
    for (const i of e)
      t(r, i, e), r += 1;
  } else
    e.forEach((r, i) => t(i, r, e));
}
function ii(e, t, n) {
  if (Pe(e) || !he(e, n) || t.has(e) || Object.isFrozen(e))
    return;
  const r = e instanceof Set, i = r ? /* @__PURE__ */ new Map() : void 0;
  if (t.add(e), Nn(e, (s, o) => {
    var u;
    if (Pe(o)) {
      const a = C(o);
      Q(a);
      const d = !((u = a.assignedMap) === null || u === void 0) && u.size || a.operated ? a.copy : a.original;
      ut(r ? i : e, s, d);
    } else
      ii(o, t, n);
  }), i) {
    const s = e, o = Array.from(s);
    s.clear(), o.forEach((u) => {
      s.add(i.has(u) ? i.get(u) : u);
    });
  }
}
function bs(e, t) {
  const n = e.type === 3 ? e.setMap : e.copy;
  e.finalities.revoke.length > 1 && e.assignedMap.get(t) && n && ii(me(n, t), e.finalities.handledSet, e.options);
}
function un(e) {
  e.type === 3 && e.copy && (e.copy.clear(), e.setMap.forEach((t) => {
    e.copy.add(Ln(t));
  }));
}
function an(e, t, n, r) {
  if (e.operated && e.assignedMap && e.assignedMap.size > 0 && !e.finalized) {
    if (n && r) {
      const s = ni(e);
      s && t(e, s, n, r);
    }
    e.finalized = !0;
  }
}
function Fn(e, t, n, r) {
  const i = C(n);
  i && (i.callbacks || (i.callbacks = []), i.callbacks.push((s, o) => {
    var u;
    const a = e.type === 3 ? e.setMap : e.copy;
    if (we(me(a, t), n)) {
      let d = i.original;
      i.copy && (d = i.copy), un(e), an(e, r, s, o), e.options.enableAutoFreeze && (e.options.updatedValues = (u = e.options.updatedValues) !== null && u !== void 0 ? u : /* @__PURE__ */ new WeakMap(), e.options.updatedValues.set(d, i.original)), ut(a, t, d);
    }
  }), e.options.enableAutoFreeze && i.finalities !== e.finalities && (e.options.enableAutoFreeze = !1)), he(n, e.options) && e.finalities.draft.push(() => {
    const s = e.type === 3 ? e.setMap : e.copy;
    we(me(s, t), n) && bs(e, t);
  });
}
function _s(e, t, n, r, i) {
  let { original: s, assignedMap: o, options: u } = e, a = e.copy;
  a.length < s.length && ([s, a] = [a, s], [n, r] = [r, n]);
  for (let d = 0; d < s.length; d += 1)
    if (o.get(d.toString()) && a[d] !== s[d]) {
      const c = t.concat([d]), f = Ce(c, i);
      n.push({
        op: D.Replace,
        path: f,
        // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
        value: Et(a[d])
      }), r.push({
        op: D.Replace,
        path: f,
        // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
        value: Et(s[d])
      });
    }
  for (let d = s.length; d < a.length; d += 1) {
    const c = t.concat([d]), f = Ce(c, i);
    n.push({
      op: D.Add,
      path: f,
      // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
      value: Et(a[d])
    });
  }
  if (s.length < a.length) {
    const { arrayLengthAssignment: d = !0 } = u.enablePatches;
    if (d) {
      const c = t.concat(["length"]), f = Ce(c, i);
      r.push({
        op: D.Replace,
        path: f,
        value: s.length
      });
    } else
      for (let c = a.length; s.length < c; c -= 1) {
        const f = t.concat([c - 1]), h = Ce(f, i);
        r.push({
          op: D.Remove,
          path: h
        });
      }
  }
}
function gs({ original: e, copy: t, assignedMap: n }, r, i, s, o) {
  n.forEach((u, a) => {
    const d = me(e, a), c = Et(me(t, a)), f = u ? nt(e, a) ? D.Replace : D.Add : D.Remove;
    if (we(d, c) && f === D.Replace)
      return;
    const h = r.concat(a), p = Ce(h, o);
    i.push(f === D.Remove ? { op: f, path: p } : { op: f, path: p, value: c }), s.push(f === D.Add ? { op: D.Remove, path: p } : f === D.Remove ? { op: D.Add, path: p, value: d } : { op: D.Replace, path: p, value: d });
  });
}
function Ts({ original: e, copy: t }, n, r, i, s) {
  let o = 0;
  e.forEach((u) => {
    if (!t.has(u)) {
      const a = n.concat([o]), d = Ce(a, s);
      r.push({
        op: D.Remove,
        path: d,
        value: u
      }), i.unshift({
        op: D.Add,
        path: d,
        value: u
      });
    }
    o += 1;
  }), o = 0, t.forEach((u) => {
    if (!e.has(u)) {
      const a = n.concat([o]), d = Ce(a, s);
      r.push({
        op: D.Add,
        path: d,
        value: u
      }), i.unshift({
        op: D.Remove,
        path: d,
        value: u
      });
    }
    o += 1;
  });
}
function ot(e, t, n, r) {
  const { pathAsArray: i = !0 } = e.options.enablePatches;
  switch (e.type) {
    case 0:
    case 2:
      return gs(e, t, n, r, i);
    case 1:
      return _s(e, t, n, r, i);
    case 3:
      return Ts(e, t, n, r, i);
  }
}
const Pt = (e, t, n = !1) => {
  if (typeof e == "object" && e !== null && (!he(e, t) || n))
    throw new Error("Strict mode: Mutable data cannot be accessed directly, please use 'unsafe(callback)' wrap.");
}, cn = {
  get size() {
    return se(C(this)).size;
  },
  has(e) {
    return se(C(this)).has(e);
  },
  set(e, t) {
    const n = C(this), r = se(n);
    return (!r.has(e) || !we(r.get(e), t)) && (Q(n), be(n), n.assignedMap.set(e, !0), n.copy.set(e, t), Fn(n, e, t, ot)), this;
  },
  delete(e) {
    if (!this.has(e))
      return !1;
    const t = C(this);
    return Q(t), be(t), t.original.has(e) ? t.assignedMap.set(e, !1) : t.assignedMap.delete(e), t.copy.delete(e), !0;
  },
  clear() {
    const e = C(this);
    if (this.size) {
      Q(e), be(e), e.assignedMap = /* @__PURE__ */ new Map();
      for (const [t] of e.original)
        e.assignedMap.set(t, !1);
      e.copy.clear();
    }
  },
  forEach(e, t) {
    const n = C(this);
    se(n).forEach((r, i) => {
      e.call(t, this.get(i), i, this);
    });
  },
  get(e) {
    var t, n;
    const r = C(this), i = se(r).get(e), s = ((n = (t = r.options).mark) === null || n === void 0 ? void 0 : n.call(t, i, oe)) === oe.mutable;
    if (r.options.strict && Pt(i, r.options, s), s || r.finalized || !he(i, r.options) || i !== r.original.get(e))
      return i;
    const o = xn.createDraft({
      original: i,
      parentDraft: r,
      key: e,
      finalities: r.finalities,
      options: r.options
    });
    return Q(r), r.copy.set(e, o), o;
  },
  keys() {
    return se(C(this)).keys();
  },
  values() {
    const e = this.keys();
    return {
      [Ot]: () => this.values(),
      next: () => {
        const t = e.next();
        return t.done ? t : {
          done: !1,
          value: this.get(t.value)
        };
      }
    };
  },
  entries() {
    const e = this.keys();
    return {
      [Ot]: () => this.entries(),
      next: () => {
        const t = e.next();
        if (t.done)
          return t;
        const n = this.get(t.value);
        return {
          done: !1,
          value: [t.value, n]
        };
      }
    };
  },
  [Ot]() {
    return this.entries();
  }
}, ws = Reflect.ownKeys(cn), yr = (e, t, { isValuesIterator: n }) => () => {
  var r, i;
  const s = t.next();
  if (s.done)
    return s;
  const o = s.value;
  let u = e.setMap.get(o);
  const a = C(u), d = ((i = (r = e.options).mark) === null || i === void 0 ? void 0 : i.call(r, u, oe)) === oe.mutable;
  if (e.options.strict && Pt(o, e.options, d), !d && !a && he(o, e.options) && !e.finalized && e.original.has(o)) {
    const c = xn.createDraft({
      original: o,
      parentDraft: e,
      key: o,
      finalities: e.finalities,
      options: e.options
    });
    e.setMap.set(o, c), u = c;
  } else a && (u = a.proxy);
  return {
    done: !1,
    value: n ? u : [u, u]
  };
}, It = {
  get size() {
    return C(this).setMap.size;
  },
  has(e) {
    const t = C(this);
    if (t.setMap.has(e))
      return !0;
    Q(t);
    const n = C(e);
    return !!(n && t.setMap.has(n.original));
  },
  add(e) {
    const t = C(this);
    return this.has(e) || (Q(t), be(t), t.assignedMap.set(e, !0), t.setMap.set(e, e), Fn(t, e, e, ot)), this;
  },
  delete(e) {
    if (!this.has(e))
      return !1;
    const t = C(this);
    Q(t), be(t);
    const n = C(e);
    return n && t.setMap.has(n.original) ? (t.assignedMap.set(n.original, !1), t.setMap.delete(n.original)) : (!n && t.setMap.has(e) ? t.assignedMap.set(e, !1) : t.assignedMap.delete(e), t.setMap.delete(e));
  },
  clear() {
    if (!this.size)
      return;
    const e = C(this);
    Q(e), be(e);
    for (const t of e.original)
      e.assignedMap.set(t, !1);
    e.setMap.clear();
  },
  values() {
    const e = C(this);
    Q(e);
    const t = e.setMap.keys();
    return {
      [Symbol.iterator]: () => this.values(),
      next: yr(e, t, { isValuesIterator: !0 })
    };
  },
  entries() {
    const e = C(this);
    Q(e);
    const t = e.setMap.keys();
    return {
      [Symbol.iterator]: () => this.entries(),
      next: yr(e, t, {
        isValuesIterator: !1
      })
    };
  },
  keys() {
    return this.values();
  },
  [Ot]() {
    return this.values();
  },
  forEach(e, t) {
    const n = this.values();
    let r = n.next();
    for (; !r.done; )
      e.call(t, r.value, r.value, this), r = n.next();
  }
};
Set.prototype.difference && Object.assign(It, {
  intersection(e) {
    return Set.prototype.intersection.call(new Set(this.values()), e);
  },
  union(e) {
    return Set.prototype.union.call(new Set(this.values()), e);
  },
  difference(e) {
    return Set.prototype.difference.call(new Set(this.values()), e);
  },
  symmetricDifference(e) {
    return Set.prototype.symmetricDifference.call(new Set(this.values()), e);
  },
  isSubsetOf(e) {
    return Set.prototype.isSubsetOf.call(new Set(this.values()), e);
  },
  isSupersetOf(e) {
    return Set.prototype.isSupersetOf.call(new Set(this.values()), e);
  },
  isDisjointFrom(e) {
    return Set.prototype.isDisjointFrom.call(new Set(this.values()), e);
  }
});
const ms = Reflect.ownKeys(It), si = /* @__PURE__ */ new WeakSet(), oi = {
  get(e, t, n) {
    var r, i;
    const s = (r = e.copy) === null || r === void 0 ? void 0 : r[t];
    if (s && si.has(s))
      return s;
    if (t === ti)
      return e;
    let o;
    if (e.options.mark) {
      const d = t === "size" && (e.original instanceof Map || e.original instanceof Set) ? Reflect.get(e.original, t) : Reflect.get(e.original, t, n);
      if (o = e.options.mark(d, oe), o === oe.mutable)
        return e.options.strict && Pt(d, e.options, !0), d;
    }
    const u = se(e);
    if (u instanceof Map && ws.includes(t)) {
      if (t === "size")
        return Object.getOwnPropertyDescriptor(cn, "size").get.call(e.proxy);
      const d = cn[t];
      if (d)
        return d.bind(e.proxy);
    }
    if (u instanceof Set && ms.includes(t)) {
      if (t === "size")
        return Object.getOwnPropertyDescriptor(It, "size").get.call(e.proxy);
      const d = It[t];
      if (d)
        return d.bind(e.proxy);
    }
    if (!nt(u, t)) {
      const d = hr(u, t);
      return d ? "value" in d ? d.value : (
        // !case: support for getter
        (i = d.get) === null || i === void 0 ? void 0 : i.call(e.proxy)
      ) : void 0;
    }
    const a = u[t];
    if (e.options.strict && Pt(a, e.options), e.finalized || !he(a, e.options))
      return a;
    if (a === Ht(e.original, t)) {
      if (Q(e), e.copy[t] = Wn({
        original: e.original[t],
        parentDraft: e,
        key: e.type === 1 ? Number(t) : t,
        finalities: e.finalities,
        options: e.options
      }), typeof o == "function") {
        const d = C(e.copy[t]);
        return Q(d), be(d), d.copy;
      }
      return e.copy[t];
    }
    return a;
  },
  set(e, t, n) {
    var r;
    if (e.type === 3 || e.type === 2)
      throw new Error("Map/Set draft does not support any property assignment.");
    let i;
    if (e.type === 1 && t !== "length" && !(Number.isInteger(i = Number(t)) && i >= 0 && (t === 0 || i === 0 || String(i) === String(t))))
      throw new Error("Only supports setting array indices and the 'length' property.");
    const s = hr(se(e), t);
    if (s != null && s.set)
      return s.set.call(e.proxy, n), !0;
    const o = Ht(se(e), t), u = C(o);
    return u && we(u.original, n) ? (e.copy[t] = n, e.assignedMap = (r = e.assignedMap) !== null && r !== void 0 ? r : /* @__PURE__ */ new Map(), e.assignedMap.set(t, !1), !0) : (we(n, o) && (n !== void 0 || nt(e.original, t)) || (Q(e), be(e), nt(e.original, t) && we(n, e.original[t]) ? e.assignedMap.delete(t) : e.assignedMap.set(t, !0), e.copy[t] = n, Fn(e, t, n, ot)), !0);
  },
  has(e, t) {
    return t in se(e);
  },
  ownKeys(e) {
    return Reflect.ownKeys(se(e));
  },
  getOwnPropertyDescriptor(e, t) {
    const n = se(e), r = Reflect.getOwnPropertyDescriptor(n, t);
    return r && {
      writable: !0,
      configurable: e.type !== 1 || t !== "length",
      enumerable: r.enumerable,
      value: n[t]
    };
  },
  getPrototypeOf(e) {
    return Reflect.getPrototypeOf(e.original);
  },
  setPrototypeOf() {
    throw new Error("Cannot call 'setPrototypeOf()' on drafts");
  },
  defineProperty() {
    throw new Error("Cannot call 'defineProperty()' on drafts");
  },
  deleteProperty(e, t) {
    var n;
    return e.type === 1 ? oi.set.call(this, e, t, void 0, e.proxy) : (Ht(e.original, t) !== void 0 || t in e.original ? (Q(e), be(e), e.assignedMap.set(t, !1)) : (e.assignedMap = (n = e.assignedMap) !== null && n !== void 0 ? n : /* @__PURE__ */ new Map(), e.assignedMap.delete(t)), e.copy && delete e.copy[t], !0);
  }
};
function Wn(e) {
  const { original: t, parentDraft: n, key: r, finalities: i, options: s } = e, o = Ie(t), u = {
    type: o,
    finalized: !1,
    parent: n,
    original: t,
    copy: null,
    proxy: null,
    finalities: i,
    options: s,
    // Mapping of draft Set items to their corresponding draft values.
    setMap: o === 3 ? new Map(t.entries()) : void 0
  };
  (r || "key" in e) && (u.key = r);
  const { proxy: a, revoke: d } = Proxy.revocable(o === 1 ? Object.assign([], u) : u, oi);
  if (i.revoke.push(d), si.add(a), u.proxy = a, n) {
    const c = n;
    c.finalities.draft.push((f, h) => {
      var p, _;
      const b = C(a);
      let g = c.type === 3 ? c.setMap : c.copy;
      const T = me(g, r), w = C(T);
      if (w) {
        let v = w.original;
        w.operated && (v = Ln(T)), un(w), an(w, ot, f, h), c.options.enableAutoFreeze && (c.options.updatedValues = (p = c.options.updatedValues) !== null && p !== void 0 ? p : /* @__PURE__ */ new WeakMap(), c.options.updatedValues.set(v, w.original)), ut(g, r, v);
      }
      (_ = b.callbacks) === null || _ === void 0 || _.forEach((v) => {
        v(f, h);
      });
    });
  } else {
    const c = C(a);
    c.finalities.draft.push((f, h) => {
      un(c), an(c, ot, f, h);
    });
  }
  return a;
}
xn.createDraft = Wn;
function vs(e, t, n, r, i) {
  var s;
  const o = C(e), u = (s = o == null ? void 0 : o.original) !== null && s !== void 0 ? s : e, a = !!t.length;
  if (o != null && o.operated)
    for (; o.finalities.draft.length > 0; )
      o.finalities.draft.pop()(n, r);
  const d = a ? t[0] : o ? o.operated ? o.copy : o.original : e;
  return o && on(o), i && $e(d, d, o == null ? void 0 : o.options.updatedValues), [
    d,
    n && a ? [{ op: D.Replace, path: [], value: t[0] }] : n,
    r && a ? [{ op: D.Replace, path: [], value: u }] : r
  ];
}
function Ss(e, t) {
  var n;
  const r = {
    draft: [],
    revoke: [],
    handledSet: /* @__PURE__ */ new WeakSet()
  };
  let i, s;
  t.enablePatches && (i = [], s = []);
  const u = ((n = t.mark) === null || n === void 0 ? void 0 : n.call(t, e, oe)) === oe.mutable || !he(e, t) ? e : Wn({
    original: e,
    parentDraft: null,
    finalities: r,
    options: t
  });
  return [
    u,
    (a = []) => {
      const [d, c, f] = vs(u, a, i, s, t.enableAutoFreeze);
      return t.enablePatches ? [d, c, f] : d;
    }
  ];
}
function dn(e) {
  const { rootDraft: t, value: n, useRawReturn: r = !1, isRoot: i = !0 } = e;
  Nn(n, (s, o, u) => {
    const a = C(o);
    if (a && t && a.finalities === t.finalities) {
      e.isContainDraft = !0;
      const d = a.original;
      if (u instanceof Set) {
        const c = Array.from(u);
        u.clear(), c.forEach((f) => u.add(s === f ? d : f));
      } else
        ut(u, s, d);
    } else typeof o == "object" && o !== null && (e.value = o, e.isRoot = !1, dn(e));
  }), i && (e.isContainDraft || console.warn("The return value does not contain any draft, please use 'rawReturn()' to wrap the return value to improve performance."), r && console.warn("The return value contains drafts, please don't use 'rawReturn()' to wrap the return value."));
}
function ui(e) {
  var t;
  const n = C(e);
  if (!he(e, n == null ? void 0 : n.options))
    return e;
  const r = Ie(e);
  if (n && !n.operated)
    return n.original;
  let i;
  function s() {
    i = r === 2 ? Un(e) ? new Map(e) : new (Object.getPrototypeOf(e)).constructor(e) : r === 3 ? Array.from(n.setMap.values()) : ri(e, n == null ? void 0 : n.options);
  }
  if (n) {
    n.finalized = !0;
    try {
      s();
    } finally {
      n.finalized = !1;
    }
  } else
    i = e;
  if (Nn(i, (o, u) => {
    if (n && we(me(n.original, o), u))
      return;
    const a = ui(u);
    a !== u && (i === e && s(), ut(i, o, a));
  }), r === 3) {
    const o = (t = n == null ? void 0 : n.original) !== null && t !== void 0 ? t : i;
    return Dn(o) ? new Set(i) : new (Object.getPrototypeOf(o)).constructor(i);
  }
  return i;
}
function br(e) {
  if (!Pe(e))
    throw new Error(`current() is only used for Draft, parameter: ${e}`);
  return ui(e);
}
const Os = (e) => function t(n, r, i) {
  var s, o, u;
  if (typeof n == "function" && typeof r != "function")
    return function(O, ...U) {
      return t(O, (W) => n.call(this, W, ...U), r);
    };
  const a = n, d = r;
  let c = i;
  if (typeof r != "function" && (c = r), c !== void 0 && Object.prototype.toString.call(c) !== "[object Object]")
    throw new Error(`Invalid options: ${c}, 'options' should be an object.`);
  c = Object.assign(Object.assign({}, e), c);
  const f = Pe(a) ? br(a) : a, h = Array.isArray(c.mark) ? (O, U) => {
    for (const W of c.mark) {
      if (typeof W != "function")
        throw new Error(`Invalid mark: ${W}, 'mark' should be a function.`);
      const _e = W(O, U);
      if (_e)
        return _e;
    }
  } : c.mark, p = (s = c.enablePatches) !== null && s !== void 0 ? s : !1, _ = (o = c.strict) !== null && o !== void 0 ? o : !1, g = {
    enableAutoFreeze: (u = c.enableAutoFreeze) !== null && u !== void 0 ? u : !1,
    mark: h,
    strict: _,
    enablePatches: p
  };
  if (!he(f, g) && typeof f == "object" && f !== null)
    throw new Error("Invalid base state: create() only supports plain objects, arrays, Set, Map or using mark() to mark the state as immutable.");
  const [T, w] = Ss(f, g);
  if (typeof r != "function") {
    if (!he(f, g))
      throw new Error("Invalid base state: create() only supports plain objects, arrays, Set, Map or using mark() to mark the state as immutable.");
    return [T, w];
  }
  let v;
  try {
    v = d(T);
  } catch (O) {
    throw on(C(T)), O;
  }
  const I = (O) => {
    const U = C(T);
    if (!Pe(O)) {
      if (O !== void 0 && !we(O, T) && (U != null && U.operated))
        throw new Error("Either the value is returned as a new non-draft value, or only the draft is modified without returning any value.");
      const _e = O == null ? void 0 : O[ls];
      if (_e) {
        const ye = _e[0];
        return g.strict && typeof O == "object" && O !== null && dn({
          rootDraft: U,
          value: O,
          useRawReturn: !0
        }), w([ye]);
      }
      if (O !== void 0)
        return typeof O == "object" && O !== null && dn({ rootDraft: U, value: O }), w([O]);
    }
    if (O === T || O === void 0)
      return w([]);
    const W = C(O);
    if (g === W.options) {
      if (W.operated)
        throw new Error("Cannot return a modified child draft.");
      return w([br(O)]);
    }
    return w([O]);
  };
  return v instanceof Promise ? v.then(I, (O) => {
    throw on(C(T)), O;
  }) : I(v);
}, fn = Os();
Object.prototype.constructor.toString();
function ai(e, t) {
  const n = Object.keys(e), r = Object.keys(t);
  return n.length === r.length && Object.keys(e).every((i) => t.hasOwnProperty(i));
}
function _r(e, t) {
  return Object.keys(e).length === Object.keys(t).length && Object.keys(e).every((n) => t.hasOwnProperty(n) && e[n] === t[n]);
}
function At(e, t) {
  return typeof e != "object" || typeof t != "object" || e === null || t === null ? e === t : ai(e, t) ? Object.keys(e).every((n) => At(e[n], t[n])) : !1;
}
function Vn(e) {
  if (!Xe(e))
    return e;
  const t = {};
  for (const [n, r] of Object.entries(e))
    r !== void 0 && (t[n] = r);
  return t;
}
function ci(e, t) {
  if (!Xe(e) || !Xe(t))
    return t;
  const n = Object.assign({}, e);
  for (const r of Object.keys(t)) {
    if (t[r] === void 0)
      continue;
    if (t[r] === null) {
      delete n[r];
      continue;
    }
    const i = Xe(e[r]) && Xe(t[r]);
    n[r] = i ? ci(e[r], t[r]) : t[r];
  }
  return n;
}
function Xe(e) {
  return typeof e == "object" && e !== null && !Array.isArray(e);
}
function Es(e, t, n) {
  if (!e || t.length === 0)
    return;
  let r = e || {};
  for (let s = 0; s < t.length - 1; s++) {
    const o = t[s];
    (!(o in r) || typeof r[o] != "object") && (r[o] = typeof t[s + 1] == "number" ? [] : {}), r = r[o];
  }
  const i = t[t.length - 1];
  Array.isArray(r) && typeof i == "number" ? r.splice(i, 0, n) : r[i] = n;
}
function gr(e, t, n) {
  if (!e || t.length === 0)
    return;
  let r = e || {};
  for (let i = 0; i < t.length - 1; i++) {
    const s = t[i];
    (!(s in r) || typeof r[s] != "object") && (r[s] = typeof t[i + 1] == "number" ? [] : {}), r = r[s];
  }
  r[t[t.length - 1]] = n;
}
function di(e, t) {
  if (!e || t.length === 0)
    return;
  const [n, ...r] = t;
  if (n in e) {
    if (r.length === 0) {
      Array.isArray(e) ? e.splice(n, 1) : delete e[n];
      return;
    }
    di(e[n], r), As(e[n]) && delete e[n];
  }
}
function As(e) {
  return e && Object.keys(e).length === 0;
}
const Tr = /ZULU|YEKT|YEKST|YAPT|YAKT|YAKST|XJT|WGT|WGST|WFT|WETDST|WET|WDT|WAT|WAST|WAKT|WADT|VUT|VOLT|VLAT|VLAST|VET|UZT|UZST|UYT|UYST|UTC|UT|ULAT|ULAST|UCT|TVT|TRUT|TOT|TMT|TKT|TJT|TFT|TAHT|SGT|SCT|SAST|SADT|RET|PYT|PYST|PWT|PST|PONT|PMST|PMDT|PKT|PKST|PHT|PGT|PETT|PETST|PET|PDT|OMST|OMSST|NZT|NZST|NZDT|NUT|NST|NPT|NOVT|NOVST|NFT|NDT|MYT|MVT|MUT|MUST|MST|MSK|MSD|MPT|MMT|MHT|MEZ|METDST|MET|MESZ|MEST|MDT|MAWT|MART|MAGT|MAGST|LKT|LINT|LIGT|LHST|LHDT|KST|KRAT|KRAST|KOST|KGT|KGST|KDT|JST|JAYT|IST|IRT|IRKT|IRKST|IOT|IDT|ICT|HST|HKT|GYT|GMT|GILT|GFT|GET|GEST|GAMT|GALT|FNT|FNST|FKT|FKST|FJT|FJST|FET|EST|EGT|EGST|EETDST|EET|EEST|EDT|EAT|EAST|EASST|DDUT|DAVT|CXT|CST|COT|CLT|CLST|CKT|CHUT|CHAST|CHADT|CETDST|CET|CEST|CDT|CCT|CAST|CADT|BTT|BST|BRT|BRST|BRA|BOT|BORT|BNT|BDT|BDST|AZT|AZST|AZOT|AZOST|AWST|AWSST|AST|ART|ARST|ANAT|ANAST|AMT|AMST|ALMT|ALMST|AKST|AKDT|AFT|AEST|AESST|AEDT|ADT|ACWST|ACT|ACST|ACSST|ACDT$/, Cs = {
  ZULU: 0,
  YEKT: 18e3,
  YEKST: 21600,
  YAPT: 36e3,
  YAKT: 32400,
  YAKST: 32400,
  XJT: 21600,
  WGT: -10800,
  WGST: -7200,
  WFT: 43200,
  WETDST: 3600,
  WET: 0,
  WDT: 32400,
  WAT: 3600,
  WAST: 25200,
  WAKT: 43200,
  WADT: 28800,
  VUT: 39600,
  VOLT: 10800,
  VLAT: 36e3,
  VLAST: 36e3,
  VET: -14400,
  UZT: 18e3,
  UZST: 21600,
  UYT: -10800,
  UYST: -7200,
  UTC: 0,
  UT: 0,
  ULAT: 28800,
  ULAST: 32400,
  UCT: 0,
  TVT: 43200,
  TRUT: 36e3,
  TOT: 46800,
  TMT: 18e3,
  TKT: 46800,
  TJT: 18e3,
  TFT: 18e3,
  TAHT: -36e3,
  SGT: 28800,
  SCT: 14400,
  SAST: 7200,
  SADT: 37800,
  RET: 14400,
  PYT: -14400,
  PYST: -10800,
  PWT: 32400,
  PST: -28800,
  PONT: 39600,
  PMST: -10800,
  PMDT: -7200,
  PKT: 18e3,
  PKST: 21600,
  PHT: 28800,
  PGT: 36e3,
  PETT: 43200,
  PETST: 43200,
  PET: -18e3,
  PDT: -25200,
  OMST: 21600,
  OMSST: 21600,
  NZT: 43200,
  NZST: 43200,
  NZDT: 46800,
  NUT: -39600,
  NST: -12600,
  NPT: 20700,
  NOVT: 25200,
  NOVST: 25200,
  NFT: -12600,
  NDT: -9e3,
  MYT: 28800,
  MVT: 18e3,
  MUT: 14400,
  MUST: 18e3,
  MST: -25200,
  MSK: 10800,
  MSD: 14400,
  MPT: 36e3,
  MMT: 23400,
  MHT: 43200,
  MEZ: 3600,
  METDST: 7200,
  MET: 3600,
  MESZ: 7200,
  MEST: 7200,
  MDT: -21600,
  MAWT: 18e3,
  MART: -34200,
  MAGT: 39600,
  MAGST: 39600,
  LKT: 19800,
  LINT: 50400,
  LIGT: 36e3,
  LHST: 37800,
  LHDT: 37800,
  KST: 32400,
  KRAT: 25200,
  KRAST: 25200,
  KOST: 39600,
  KGT: 21600,
  KGST: 21600,
  KDT: 36e3,
  JST: 32400,
  JAYT: 32400,
  IST: 7200,
  IRT: 12600,
  IRKT: 28800,
  IRKST: 28800,
  IOT: 21600,
  IDT: 10800,
  ICT: 25200,
  HST: -36e3,
  HKT: 28800,
  GYT: -14400,
  GMT: 0,
  GILT: 43200,
  GFT: -10800,
  GET: 14400,
  GEST: 14400,
  GAMT: -32400,
  GALT: -21600,
  FNT: -7200,
  FNST: -3600,
  FKT: -10800,
  FKST: -10800,
  FJT: 43200,
  FJST: 46800,
  FET: 10800,
  EST: -18e3,
  EGT: -3600,
  EGST: 0,
  EETDST: 10800,
  EET: 7200,
  EEST: 10800,
  EDT: -14400,
  EAT: 10800,
  EAST: -21600,
  EASST: -21600,
  DDUT: 36e3,
  DAVT: 25200,
  CXT: 25200,
  CST: -21600,
  COT: -18e3,
  CLT: -14400,
  CLST: -10800,
  CKT: -36e3,
  CHUT: 36e3,
  CHAST: 45900,
  CHADT: 49500,
  CETDST: 7200,
  CET: 3600,
  CEST: 7200,
  CDT: -18e3,
  CCT: 28800,
  CAST: 34200,
  CADT: 37800,
  BTT: 21600,
  BST: 3600,
  BRT: -10800,
  BRST: -7200,
  BRA: -10800,
  BOT: -14400,
  BORT: 28800,
  BNT: 28800,
  BDT: 21600,
  BDST: 7200,
  AZT: 14400,
  AZST: 14400,
  AZOT: -3600,
  AZOST: 0,
  AWST: 28800,
  AWSST: 32400,
  AST: -14400,
  ART: -10800,
  ARST: -10800,
  ANAT: 43200,
  ANAST: 43200,
  AMT: -14400,
  AMST: 14400,
  ALMT: 21600,
  ALMST: 25200,
  AKST: -32400,
  AKDT: -28800,
  AFT: 16200,
  AEST: 36e3,
  AESST: 39600,
  AEDT: 39600,
  ADT: -10800,
  ACWST: 31500,
  ACT: -18e3,
  ACST: 34200,
  ACSST: 37800,
  ACDT: 37800
};
function js(e) {
  return new Date(e);
}
function ks(e) {
  return /* @__PURE__ */ new Date(e + "Z");
}
const Ps = /^(\d+)[\./-](\d+)[\./-](\d+)$/;
function Is(e) {
  const t = e.match(Ps);
  if (!t)
    return null;
  const [n, r, i, s] = t;
  return r <= 0 || i <= 0 || s <= 0 ? null : r > 999 ? new Date(Date.UTC(r, i - 1, s, 0, 0, 0, 0)) : new Date(Date.UTC(s, r - 1, i, 0, 0, 0, 0));
}
function Ms(e) {
  const [t, n] = e.split(" ");
  return /* @__PURE__ */ new Date(t + "T" + n + "Z");
}
function Rs(e) {
  const [t, n] = e.split(" ");
  return /* @__PURE__ */ new Date(t + "T" + n + "Z");
}
function $s(e) {
  return new Date(e);
}
function xs(e) {
  const t = /^(\w{3}) (\w{3}) (\d{2}) (\d{4})$/;
  if (!e.match(t))
    throw new Error(`Unable to parse \`${e}\` as a date.`);
  const r = /* @__PURE__ */ new Date(e + " UTC");
  return new Date(Date.UTC(r.getUTCFullYear(), r.getUTCMonth(), r.getUTCDate(), 0, 0, 0, 0));
}
function Ds(e) {
  const t = /^(.+T.+)([+-])(\d{2})$/, n = e.match(t);
  if (n) {
    const [, r, i, s] = n, o = `${r}${i}${s}:00`;
    return new Date(o);
  }
  return null;
}
function Us(e) {
  const [t, n] = e.split(", "), [r, i, s] = t.split("/").map(Number), o = n.match(/(\d{1,2}):(\d{2}):(\d{2}) (AM|PM)/);
  if (!o)
    throw new Error(`Unable to parse time from: ${e}`);
  let [, u, a, d, c] = o;
  return u = Number(u), a = Number(a), d = Number(d), c === "PM" && u !== 12 ? u += 12 : c === "AM" && u === 12 && (u = 0), new Date(Date.UTC(s, r - 1, i, u, a, d));
}
function Ls(e) {
  switch (e) {
    case "epoch":
      return /* @__PURE__ */ new Date(0);
    case "infinity":
    case "-infinity":
    case "today":
    case "tomorrow":
    case "yesterday":
      return null;
  }
}
function Ns(e) {
  const t = e.match(Tr);
  if (!t)
    return null;
  const [n] = t, r = Cs[n], i = new Date(e.replace(Tr, "Z"));
  return new Date(i.getTime() - r * 1e3);
}
const Fs = [
  Is,
  Rs,
  xs,
  Us,
  $s,
  ks,
  Ds,
  Ms,
  js,
  Ls,
  Ns
];
function Ws(e, t) {
  try {
    const n = e(t);
    return n instanceof Date && !isNaN(n.getTime()) ? n : null;
  } catch {
    return null;
  }
}
function ln(e) {
  for (const t of Fs) {
    const n = Ws(t, e);
    if (n)
      return n;
  }
  return null;
}
function Vs(e) {
  try {
    const t = JSON.parse(e);
    return typeof t == "string" ? ln(t) : null;
  } catch {
    return null;
  }
}
function xt(e) {
  if (e !== void 0) {
    if (e === null)
      return null;
    if (e instanceof Date)
      return e;
    if (typeof e == "string") {
      const t = ln(e) || Vs(e) || ln(e.trim());
      if (!t)
        throw new Error(`Unable to parse \`${e}\` as a date.`);
      return t;
    } else if (typeof e == "number")
      return new Date(e);
    throw new Error(`Invalid date value \`${e}\`. Expected a date, number, or string, got type ${typeof e}.`);
  }
}
function qs(e) {
  return e.cardinality === "one";
}
function qn(e) {
  return e["value-type"] === "ref";
}
function zn(e) {
  return e["value-type"] === "blob";
}
function Ke(e, t) {
  return e[t];
}
function Dt(e, t) {
  return t.reduce((n, r) => n && n.get(r), e);
}
function le(e, t) {
  if (t.length === 0)
    throw new Error("path must have at least one element");
  if (t.length === 1) {
    e.delete(t[0]);
    return;
  }
  const [n, ...r] = t;
  e.has(n) && le(e.get(n), r);
}
function te(e, t, n) {
  if (t.length === 0)
    throw new Error("path must have at least one element");
  if (t.length === 1) {
    e.set(t[0], n);
    return;
  }
  const [r, ...i] = t;
  let s = e.get(r);
  s || (s = /* @__PURE__ */ new Map(), e.set(r, s)), te(s, i, n);
}
function fi(e, t, n) {
  const r = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map(), s = /* @__PURE__ */ new Map();
  for (const o of t) {
    let [u, a, d, c] = o;
    const f = Ke(e, a);
    if (!f) {
      console.warn("no such attr", u, e);
      continue;
    }
    f["checked-data-type"] === "date" && n && (d = xt(d), o[2] = d), qn(f) && te(s, [d, a, u], o), te(r, [u, a, d], o), te(i, [a, u, d], o);
  }
  return { eav: r, aev: i, vae: s };
}
function li(e) {
  const t = /* @__PURE__ */ new Map(), n = /* @__PURE__ */ new Map(), r = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
  for (const s of Object.values(e)) {
    const o = s["forward-identity"], [u, a, d] = o, c = s["reverse-identity"];
    if (te(r, [a, d], s), zn(s) && te(t, [a, d], s), s["primary?"] && te(n, [a], s), c) {
      const [f, h, p] = c;
      te(i, [h, p], s);
    }
  }
  return { blobAttrs: t, primaryKeys: n, forwardIdents: r, revIdents: i };
}
function zs(e) {
  return {
    __type: e.__type,
    attrs: e.attrs,
    triples: ne(e.eav, 3),
    cardinalityInference: e.cardinalityInference,
    linkIndex: e.linkIndex,
    useDateObjects: e.useDateObjects
  };
}
function Ks(e) {
  return hn(e.attrs, e.triples, e.cardinalityInference, e.linkIndex, e.useDateObjects);
}
function Kn(e) {
  e.attrIndexes = li(e.attrs);
}
function hn(e, t, n, r, i) {
  const s = fi(e, t, i);
  return s.useDateObjects = i, s.attrs = e, s.attrIndexes = li(e), s.cardinalityInference = n, s.linkIndex = r, s.__type = "store", s;
}
function at(e, t) {
  var n, r;
  let i;
  if (Array.isArray(t[0])) {
    const [o, u] = t[0], a = e.aev.get(o);
    if (!a)
      return null;
    i = (n = ne(a, 2).find((c) => c[2] === u)) === null || n === void 0 ? void 0 : n[0];
  } else
    i = t[0];
  if (!i)
    return null;
  const s = t[2];
  if (Array.isArray(s) && s.length === 2 && e.aev.get(s[0])) {
    const [o, u] = s, a = e.aev.get(o);
    if (!a)
      return null;
    const c = (r = ne(a, 2).find((b) => b[2] === u)) === null || r === void 0 ? void 0 : r[0];
    if (!c)
      return null;
    const [f, h, p, ..._] = t;
    return [i, h, c, ..._];
  } else {
    const [o, ...u] = t;
    return [i, ...u];
  }
}
function Bs(e, t) {
  const n = at(e, t);
  if (!n)
    return;
  const [r, i, s] = n, o = Ke(e.attrs, i);
  o && (le(e.eav, [r, i, s]), le(e.aev, [i, r, s]), qn(o) && le(e.vae, [s, i, r]));
}
let Gs = 0;
function hi(e, t, n) {
  const [r, i, s] = n;
  let o;
  const u = Dt(e.ea, [r, i, s]);
  return u && (o = u[3]), o || Date.now() * 10 + Gs++;
}
function Qs(e, t) {
  var n;
  const r = at(e, t);
  if (!r)
    return;
  let [i, s, o] = r;
  const u = Ke(e.attrs, s);
  if (!u)
    return;
  u["checked-data-type"] === "date" && e.useDateObjects && (o = xt(o));
  const a = Dt(e.eav, [i, s, o]), d = (n = a == null ? void 0 : a[3]) !== null && n !== void 0 ? n : hi(e, u, r), c = [i, s, o, d];
  qs(u) ? (te(e.eav, [i, s], /* @__PURE__ */ new Map([[o, c]])), te(e.aev, [s, i], /* @__PURE__ */ new Map([[o, c]]))) : (te(e.eav, [i, s, o], c), te(e.aev, [s, i, o], c)), qn(u) && te(e.vae, [o, s, i], c);
}
function Ys(e, t) {
  var n;
  const r = at(e, t);
  if (!r)
    return;
  const [i, s, o] = r, u = Ke(e.attrs, s);
  if (!u)
    return;
  if (!zn(u))
    throw new Error("merge operation is not supported for links");
  const a = Dt(e.eav, [i, s]);
  if (!a)
    return;
  const d = (n = a.values().next()) === null || n === void 0 ? void 0 : n.value;
  if (!d)
    return;
  const c = d[2], f = ci(c, o), h = [
    i,
    s,
    f,
    hi(e, u, d)
  ];
  te(e.eav, [i, s], /* @__PURE__ */ new Map([[f, h]]));
}
function pn(e, t) {
  var n, r;
  const [i, s] = t, o = at(e, [i]);
  if (!o)
    return;
  const [u] = o, a = e.eav.get(u);
  if (a) {
    for (const c of a.keys()) {
      const f = e.attrs[c];
      f && f["on-delete-reverse"] === "cascade" && ne(a.get(c), 1).forEach(([h, p, _]) => {
        var b;
        return pn(e, [_, (b = f["reverse-identity"]) === null || b === void 0 ? void 0 : b[1]]);
      }), // Fall back to deleting everything if we've rehydrated tx-steps from
      // the store that didn't set `etype` in deleteEntity
      (!s || // If we don't know about the attr, let's just get rid of it
      !f || // Make sure it matches the etype
      ((n = f["forward-identity"]) === null || n === void 0 ? void 0 : n[1]) === s) && (le(e.aev, [c, u]), le(e.eav, [u, c]));
    }
    a.size === 0 && le(e.eav, [u]);
  }
  const d = e.vae.get(u) && ne(e.vae.get(u), 2);
  d && d.forEach((c) => {
    var f, h, p;
    const [_, b, g] = c, T = e.attrs[b];
    (!s || !T || ((f = T["reverse-identity"]) === null || f === void 0 ? void 0 : f[1]) === s) && (le(e.eav, [_, b, g]), le(e.aev, [b, _, g]), le(e.vae, [g, b, _])), T && T["on-delete"] === "cascade" && ((h = T["reverse-identity"]) === null || h === void 0 ? void 0 : h[1]) === s && pn(e, [_, (p = T["forward-identity"]) === null || p === void 0 ? void 0 : p[1]]);
  }), ((r = e.vae.get(u)) === null || r === void 0 ? void 0 : r.size) === 0 && le(e.vae, [u]);
}
function pi(e, t) {
  const n = fi(e.attrs, t, e.useDateObjects);
  Object.keys(n).forEach((r) => {
    e[r] = n[r];
  });
}
function Hs(e, [t]) {
  e.attrs[t.id] = t, Kn(e);
}
function yi(e) {
  return ne(e.eav, 3);
}
function Js(e, [t]) {
  if (!e.attrs[t])
    return;
  const n = yi(e).filter(([r, i]) => i !== t);
  delete e.attrs[t], Kn(e), pi(e, n);
}
function Zs(e, [t]) {
  const n = e.attrs[t.id];
  n && (e.attrs[t.id] = Object.assign(Object.assign({}, n), t), Kn(e), pi(e, yi(e)));
}
function Xs(e, t) {
  const [n, ...r] = t;
  switch (n) {
    case "add-triple":
      Qs(e, r);
      break;
    case "deep-merge-triple":
      Ys(e, r);
      break;
    case "retract-triple":
      Bs(e, r);
      break;
    case "delete-entity":
      pn(e, r);
      break;
    case "add-attr":
      Hs(e, r);
      break;
    case "delete-attr":
      Js(e, r);
      break;
    case "update-attr":
      Zs(e, r);
      break;
    case "restore-attr":
      break;
    case "rule-params":
      break;
    default:
      throw new Error(`unhandled transaction action: ${n}`);
  }
}
function ne(e, t, n = []) {
  if (!e || t === 0)
    return n;
  if (t === 1) {
    for (const r of e.values())
      n.push(r);
    return n;
  }
  for (const r of e.values())
    ne(r, t - 1, n);
  return n;
}
function mt(e, t, n) {
  var r, i;
  const s = [];
  if (n != null && n.hasOwnProperty("$not")) {
    for (const u of t.keys())
      n.$not !== u && s.push(t.get(u));
    return s;
  }
  if (n != null && n.hasOwnProperty("$isNull")) {
    const { attrId: u, isNull: a, reverse: d } = n.$isNull;
    if (d)
      for (const c of t.keys()) {
        const f = e.vae.get(c), h = !f || ((r = f.get(u)) === null || r === void 0 ? void 0 : r.get(null)) || !f.get(u);
        (a ? h : !h) && s.push(t.get(c));
      }
    else {
      const c = e.aev.get(u);
      for (const f of t.keys()) {
        const h = !c || ((i = c.get(f)) === null || i === void 0 ? void 0 : i.get(null)) || !c.get(f);
        (a ? h : !h) && s.push(t.get(f));
      }
    }
    return s;
  }
  if (n != null && n.$comparator)
    return ne(t, 1).filter(n.$op);
  const o = n.in || n.$in || [n];
  for (const u of o) {
    const a = t.get(u);
    a && s.push(a);
  }
  return s;
}
function eo(e, t, n) {
  let r = "";
  return e !== void 0 && (r += "e"), t !== void 0 && (r += "a"), n !== void 0 && (r += "v"), r;
}
function to(e, [t, n, r]) {
  var i, s;
  switch (eo(t, n, r)) {
    case "e": {
      const u = e.eav.get(t);
      return ne(u, 2);
    }
    case "ea": {
      const u = (i = e.eav.get(t)) === null || i === void 0 ? void 0 : i.get(n);
      return ne(u, 1);
    }
    case "eav": {
      const u = (s = e.eav.get(t)) === null || s === void 0 ? void 0 : s.get(n);
      return u ? mt(e, u, r) : [];
    }
    case "ev": {
      const u = e.eav.get(t);
      if (!u)
        return [];
      const a = [];
      for (const d of u.values())
        a.push(...mt(e, d, r));
      return a;
    }
    case "a": {
      const u = e.aev.get(n);
      return ne(u, 2);
    }
    case "av": {
      const u = e.aev.get(n);
      if (!u)
        return [];
      const a = [];
      for (const d of u.values())
        a.push(...mt(e, d, r));
      return a;
    }
    case "v": {
      const u = [];
      for (const a of e.eav.values())
        for (const d of a.values())
          u.push(...mt(e, d, r));
      return u;
    }
    default:
      return ne(e.eav, 3);
  }
}
function no(e, t, n) {
  var r;
  const i = {};
  for (const [s, o] of t.entries()) {
    const u = (r = e.eav.get(n)) === null || r === void 0 ? void 0 : r.get(o.id), a = ne(u, 1);
    for (const d of a)
      i[s] = d[2];
  }
  return i;
}
function We(e, t, n) {
  var r;
  return (r = e.attrIndexes.forwardIdents.get(t)) === null || r === void 0 ? void 0 : r.get(n);
}
function bi(e, t, n) {
  var r;
  return (r = e.attrIndexes.revIdents.get(t)) === null || r === void 0 ? void 0 : r.get(n);
}
function ro(e, t) {
  return e.attrIndexes.blobAttrs.get(t);
}
function _i(e, t) {
  var n;
  const r = e.attrIndexes.primaryKeys.get(t);
  return r || ((n = e.attrIndexes.forwardIdents.get(t)) === null || n === void 0 ? void 0 : n.get("id"));
}
function io(e, t) {
  const n = at(e, t);
  if (!n)
    return;
  const [r, i, s] = n;
  if (Ke(e.attrs, i))
    return Dt(e.eav, [r, i]);
}
function so(e, t) {
  const n = t.filter(([r, i, s, o, u]) => {
    if (r !== "add-triple" && r !== "deep-merge-triple")
      return !0;
    const a = u == null ? void 0 : u.mode;
    if (a !== "create" && a !== "update")
      return !0;
    let d = !1;
    const c = Ke(e.attrs, s);
    if (c) {
      const f = _i(e, c["forward-identity"][1]);
      d = !!io(e, [i, f.id, i]);
    }
    return !(a === "create" && d || a === "update" && !d);
  });
  return fn(e, (r) => {
    n.forEach((i) => {
      Xs(r, i);
    });
  });
}
function oo(e) {
  return typeof e == "string" && e.startsWith("?");
}
function uo(e, t, n) {
  if (n.hasOwnProperty(e)) {
    const r = n[e];
    return gi(r, t, n);
  }
  return Object.assign(Object.assign({}, n), { [e]: t });
}
function wr(e, t, n) {
  return e === t ? n : null;
}
function ao(e) {
  switch (typeof e) {
    case "string":
      return e.startsWith("?") ? uo : wr;
    default:
      return wr;
  }
}
const co = [
  "in",
  "$in",
  "$not",
  "$isNull",
  "$comparator"
  // covers all of $gt, $lt, etc.
];
function fo(e) {
  for (const t of co)
    if (e.hasOwnProperty(t))
      return !0;
  return !1;
}
function gi(e, t, n) {
  return n ? typeof e == "object" ? fo(e) ? n : null : ao(e)(e, t, n) : null;
}
function lo(e, t, n) {
  return e.reduce((r, i, s) => {
    const o = t[s];
    return gi(i, o, r);
  }, n);
}
function ho(e, t, n) {
  return bo(e, t, n).map((r) => lo(t, r, n)).filter((r) => r);
}
function po(e, t, n) {
  return t.or ? t.or.patterns.flatMap((r) => yn(e, r, n)) : t.and ? t.and.patterns.reduce((r, i) => yn(e, i, r), n) : n.flatMap((r) => ho(e, t, r));
}
function yn(e, t, n = [{}]) {
  return t.reduce((r, i) => po(e, i, r), n);
}
function Bn(e, t) {
  return Array.isArray(t) ? t.map((n) => Bn(e, n)) : oo(t) ? e[t] : t;
}
function yo(e, { find: t, where: n }) {
  return yn(e, n).map((i) => Bn(i, t));
}
function bo(e, t, n) {
  return to(e, Bn(n, t));
}
const _o = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/i;
function Ve(e) {
  return typeof e == "string" && _o.test(e);
}
const q = [];
for (let e = 0; e < 256; ++e)
  q.push((e + 256).toString(16).slice(1));
function go(e, t = 0) {
  return (q[e[t + 0]] + q[e[t + 1]] + q[e[t + 2]] + q[e[t + 3]] + "-" + q[e[t + 4]] + q[e[t + 5]] + "-" + q[e[t + 6]] + q[e[t + 7]] + "-" + q[e[t + 8]] + q[e[t + 9]] + "-" + q[e[t + 10]] + q[e[t + 11]] + q[e[t + 12]] + q[e[t + 13]] + q[e[t + 14]] + q[e[t + 15]]).toLowerCase();
}
let Jt;
const To = new Uint8Array(16);
function wo() {
  if (!Jt) {
    if (typeof crypto > "u" || !crypto.getRandomValues)
      throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
    Jt = crypto.getRandomValues.bind(crypto);
  }
  return Jt(To);
}
const mo = typeof crypto < "u" && crypto.randomUUID && crypto.randomUUID.bind(crypto), mr = { randomUUID: mo };
function vo(e, t, n) {
  var i;
  if (mr.randomUUID && !e)
    return mr.randomUUID();
  e = e || {};
  const r = e.random ?? ((i = e.rng) == null ? void 0 : i.call(e)) ?? wo();
  if (r.length < 16)
    throw new Error("Random bytes length must be >= 16");
  return r[6] = r[6] & 15 | 64, r[8] = r[8] & 63 | 128, go(r);
}
function vr(e) {
  const t = e.replace(/-/g, ""), n = [];
  for (let r = 0; r < t.length; r += 2)
    n.push(parseInt(t.substring(r, r + 2), 16));
  return n;
}
function So(e, t) {
  for (let n = 0; n < e.length; n++) {
    if (e[n] < t[n])
      return -1;
    if (e[n] > t[n])
      return 1;
  }
  return 0;
}
function Oo(e, t) {
  return So(vr(e), vr(t));
}
function B() {
  return vo();
}
function Eo(e, t) {
  return e.localeCompare(t);
}
function Ao() {
  let e = Eo;
  if (typeof Intl == "object" && Intl.hasOwnProperty("Collator"))
    try {
      e = Intl.Collator("en-US").compare;
    } catch {
    }
  return e;
}
const Co = Ao();
let jo = 0;
function rt(e) {
  return Ut(`_${e}`, jo++);
}
function Ut(e, t) {
  return `?${e}-${t}`;
}
class qe extends Error {
  constructor(t) {
    super(t), this.name = "AttrNotFoundError";
  }
}
function ko(e, t) {
  const n = _i(e, t);
  if (!n)
    throw new qe(`Could not find id attr for ${t}`);
  return n;
}
function Sr(e, t, n, r) {
  return [Po(e, t, n, r)];
}
function Po(e, t, n, r) {
  return [
    e(n, r),
    ko(t, n).id,
    e(n, r),
    e("time", r)
  ];
}
function Io(e, t, n) {
  return e.map((r) => r === t ? n : r);
}
function Ti(e, t, n, r, i) {
  const s = We(t, n, i), o = bi(t, n, i), u = s || o;
  if (!u)
    throw new qe(`Could not find attr for ${[n, i]}`);
  if (u["value-type"] !== "ref")
    throw new Error(`Attr ${u.id} is not a ref`);
  const [a, d] = u["forward-identity"], [c, f] = u["reverse-identity"], h = r + 1, p = s ? [
    e(d, r),
    u.id,
    e(f, h),
    rt("time")
  ] : [
    e(d, h),
    u.id,
    e(f, r),
    rt("time")
  ];
  return [s ? f : d, h, p, u, !!s];
}
function Or(e, t) {
  if (typeof t != "string")
    return function(o) {
      return !1;
    };
  const r = t.replace(/[.*+?^${}()|[\]\\]/g, "\\$&").replace(/%/g, ".*").replace(/_/g, "."), i = new RegExp(`^${r}$`, e ? void 0 : "i");
  return function(o) {
    return typeof o != "string" ? !1 : i.test(o);
  };
}
function Mo(e, t) {
  if (typeof t != "object" || t.hasOwnProperty("$in") || t.hasOwnProperty("in"))
    return t;
  const n = e["checked-data-type"] === "date";
  if (t.hasOwnProperty("$gt"))
    return {
      $comparator: !0,
      $op: n ? function(i) {
        return new Date(i[2]) > new Date(t.$gt);
      } : function(i) {
        return i[2] > t.$gt;
      }
    };
  if (t.hasOwnProperty("$gte"))
    return {
      $comparator: !0,
      $op: n ? function(i) {
        return new Date(i[2]) >= new Date(t.$gte);
      } : function(i) {
        return i[2] >= t.$gte;
      }
    };
  if (t.hasOwnProperty("$lt"))
    return {
      $comparator: !0,
      $op: n ? function(i) {
        return new Date(i[2]) < new Date(t.$lt);
      } : function(i) {
        return i[2] < t.$lt;
      }
    };
  if (t.hasOwnProperty("$lte"))
    return {
      $comparator: !0,
      $op: n ? function(i) {
        return new Date(i[2]) <= new Date(t.$lte);
      } : function(i) {
        return i[2] <= t.$lte;
      }
    };
  if (t.hasOwnProperty("$like")) {
    const r = Or(!0, t.$like);
    return {
      $comparator: !0,
      $op: function(s) {
        return r(s[2]);
      }
    };
  }
  if (t.hasOwnProperty("$ilike")) {
    const r = Or(!1, t.$ilike);
    return {
      $comparator: !0,
      $op: function(s) {
        return r(s[2]);
      }
    };
  }
  return t;
}
function Ro(e, t, n, r, i, s) {
  const o = We(t, n, i), u = bi(t, n, i), a = o || u;
  if (!a)
    throw new qe(`No attr for etype = ${n} label = ${i}`);
  if (s != null && s.hasOwnProperty("$isNull")) {
    const d = We(t, n, "id");
    if (!d)
      throw new qe(`No attr for etype = ${n} label = id`);
    return [
      e(n, r),
      d.id,
      { $isNull: { attrId: a.id, isNull: s.$isNull, reverse: !o } },
      rt("time")
    ];
  }
  return o ? [
    e(n, r),
    a.id,
    Mo(a, s),
    rt("time")
  ] : [s, a.id, e(n, r), rt("time")];
}
function $o(e, t, n, r, i) {
  const [s, o, u] = i.reduce((a, d) => {
    const [c, f, h] = a, [p, _, b] = Ti(e, t, c, f, d);
    return [p, _, [...h, b]];
  }, [n, r, []]);
  return [s, o, u];
}
function bn(e, t, n, r, i, s) {
  const o = i.slice(0, i.length - 1), u = i[i.length - 1], [a, d, c] = $o(e, t, n, r, o), f = Ro(e, t, a, d, u, s);
  return c.concat([f]);
}
function xo(e, t) {
  return t ? [t].concat(e) : e;
}
function Do([e, t]) {
  return e === "or" && Array.isArray(t);
}
function Uo([e, t]) {
  return e === "and" && Array.isArray(t);
}
function Lo(e, t, n) {
  return (r, i) => {
    const s = e(r, i);
    return t == s ? s : `${s}-${n}`;
  };
}
function Er(e, t, n, r, i, s) {
  const o = e(r, i), u = s.map((a, d) => {
    const c = Lo(e, o, d);
    return wi(c, n, r, i, a);
  });
  return { [t]: { patterns: u, joinSym: o } };
}
function No(e) {
  const t = [];
  for (let n = 1; n <= e.length; n++)
    t.push(e.slice(0, n));
  return t;
}
function Ar(e, t, n, r, i) {
  return No(i).map((s) => bn(e, t, n, r, s, { $isNull: !0 }));
}
function wi(e, t, n, r, i) {
  return Object.entries(i).flatMap(([s, o]) => {
    if (Do([s, o]))
      return Er(e, "or", t, n, r, o);
    if (Uo([s, o]))
      return Er(e, "and", t, n, r, o);
    if (s === "$entityIdStartsWith")
      return [];
    const u = s.split(".");
    if (o != null && o.hasOwnProperty("$ne") && (o = Object.assign(Object.assign({}, o), { $not: o.$ne }), delete o.$ne), o != null && o.hasOwnProperty("$not")) {
      const a = bn(e, t, n, r, u, o), d = Ar(e, t, n, r, u);
      return [
        {
          or: {
            patterns: [a, ...d],
            joinSym: e(n, r)
          }
        }
      ];
    }
    return o != null && o.hasOwnProperty("$isNull") && o.$isNull === !0 && u.length > 1 ? [
      {
        or: {
          patterns: Ar(e, t, n, r, u),
          joinSym: e(n, r)
        }
      }
    ] : bn(e, t, n, r, u, o);
  });
}
function Fo(e, t, n, r) {
  const i = Ut;
  return r ? wi(i, e, t, n, r).concat(Sr(i, e, t, n)) : Sr(i, e, t, n);
}
function Wo(e, t, n) {
  return [e(t, n), e("time", n)];
}
function Vo(e, t, n, r, i, s) {
  const [o, u, a, d, c] = Ti(e, t, n, r, i), f = Io(a, e(n, r), s);
  return [o, u, f, d, c];
}
function qo(e, t, { etype: n, level: r, form: i }, s) {
  const o = Object.keys(i).filter((u) => u !== "$");
  return o.length ? Object.entries(s).map(function([a, d]) {
    return o.map(function(h) {
      var p, _, b;
      const g = !!(t.cardinalityInference && (!((b = (_ = (p = t.linkIndex) === null || p === void 0 ? void 0 : p[n]) === null || _ === void 0 ? void 0 : _[h]) === null || b === void 0) && b.isSingular));
      try {
        const [T, w, v] = Vo(e, t, n, r, h, a), I = vi(t, {
          etype: T,
          level: w,
          form: i[h],
          join: v
        }), O = g ? I[0] : I;
        return { [h]: O };
      } catch (T) {
        if (T instanceof qe)
          return { [h]: g ? void 0 : [] };
        throw T;
      }
    }).reduce(function(h, p) {
      return Object.assign(Object.assign({}, h), p);
    }, d);
  }) : Object.values(s);
}
function zo(e, t, n) {
  return n === "string" ? Co(e, t) : e > t ? 1 : -1;
}
function Mt([e, t], [n, r], i) {
  return t === r || t == null && r == null ? Oo(e, n) : r == null ? 1 : t == null ? -1 : zo(t, r, i);
}
function _n(e) {
  return e == null ? e : new Date(e).getTime();
}
function Ko(e, t, n, r) {
  var i;
  const [s, o, u, a] = e, d = n === "desc" ? 1 : -1;
  if (((i = t["forward-identity"]) === null || i === void 0 ? void 0 : i[2]) === "id")
    return Mt(r, [s, a], null) === d;
  const [c, f] = r, h = t["checked-data-type"], p = h === "date" ? _n(f) : f, _ = h === "date" ? _n(u) : u;
  return Mt([c, p], [s, _], h) === d;
}
function Bo(e, t) {
  const n = t[1];
  return e.attrs[n];
}
function Go(e, t, n) {
  const r = Object.keys(n)[0];
  return We(e, t, r);
}
function Qo(e, t, n, r) {
  if (n)
    return Bo(e, n);
  if (r)
    return Go(e, t, r);
}
function Yo(e, t, n) {
  var r, i;
  if (!Array.isArray(n.fields))
    return ro(e, t);
  const s = /* @__PURE__ */ new Map();
  for (const o of n.fields) {
    const u = We(e, t, o), a = (r = u == null ? void 0 : u["forward-identity"]) === null || r === void 0 ? void 0 : r[2];
    a && zn(u) && s.set(a, u);
  }
  if (!s.has("id")) {
    const o = We(e, t, "id"), u = (i = o == null ? void 0 : o["forward-identity"]) === null || i === void 0 ? void 0 : i[2];
    u && s.set(u, o);
  }
  return s;
}
function Ho(e, { etype: t, pageInfo: n, dq: r, form: i }) {
  var s, o;
  const u = (s = i == null ? void 0 : i.$) === null || s === void 0 ? void 0 : s.order, a = mi(i), d = Jo(i);
  let c = yo(e, r);
  const f = n == null ? void 0 : n["start-cursor"], h = Qo(e, t, f, u);
  if (h && ((o = h == null ? void 0 : h["forward-identity"]) === null || o === void 0 ? void 0 : o[2]) !== "id") {
    const b = h["checked-data-type"] === "date", g = h.id;
    c = c.map(([T]) => {
      var w, v, I, O, U;
      let W = (U = (O = (I = (v = (w = e.eav.get(T)) === null || w === void 0 ? void 0 : w.get(g)) === null || v === void 0 ? void 0 : v.values()) === null || I === void 0 ? void 0 : I.next()) === null || O === void 0 ? void 0 : O.value) === null || U === void 0 ? void 0 : U[2];
      return b && (W = _n(W)), [T, W];
    });
  }
  c.sort(d === "asc" ? function(g, T) {
    return Mt(g, T, h == null ? void 0 : h["checked-data-type"]);
  } : function(g, T) {
    return Mt(T, g, h == null ? void 0 : h["checked-data-type"]);
  });
  let p = {};
  const _ = Yo(e, t, r);
  for (const b of c) {
    const [g] = b;
    if (p[g] || !a && f && h && Ko(f, h, d, b))
      continue;
    const T = no(e, _, g);
    T && (p[g] = T);
  }
  return p;
}
function Jo(e) {
  var t;
  const n = (t = e.$) === null || t === void 0 ? void 0 : t.order;
  return n && n[Object.keys(n)[0]] || "asc";
}
function mi(e) {
  var t, n, r;
  const i = (t = e.$) === null || t === void 0 ? void 0 : t.offset, s = (n = e.$) === null || n === void 0 ? void 0 : n.before, o = (r = e.$) === null || r === void 0 ? void 0 : r.after;
  return !i && !s && !o;
}
function Zo(e, { etype: t, level: n, form: r, join: i, pageInfo: s }) {
  var o, u, a, d, c;
  if (!mi(r) && (!s || !s["start-cursor"]))
    return [];
  const f = xo(Fo(e, t, n, (o = r.$) === null || o === void 0 ? void 0 : o.where), i), h = Wo(Ut, t, n), p = (u = r.$) === null || u === void 0 ? void 0 : u.fields, _ = Ho(e, {
    etype: t,
    pageInfo: s,
    form: r,
    dq: { where: f, find: h, fields: p }
  }), b = ((a = r.$) === null || a === void 0 ? void 0 : a.limit) || ((d = r.$) === null || d === void 0 ? void 0 : d.first) || ((c = r.$) === null || c === void 0 ? void 0 : c.last);
  if (b != null) {
    const g = Object.entries(_);
    return g.length <= b ? _ : Object.fromEntries(g.slice(0, b));
  }
  return _;
}
function Xo(e, t) {
  try {
    return Zo(e, t);
  } catch (n) {
    if (n instanceof qe)
      return {};
    throw n;
  }
}
function vi(e, t) {
  const n = Xo(e, t);
  return qo(Ut, e, t, n);
}
function eu(e) {
  const t = {};
  for (const [n, r] of Object.entries(e))
    t[n] = {
      startCursor: r["start-cursor"],
      endCursor: r["end-cursor"],
      hasNextPage: r["has-next-page?"],
      hasPreviousPage: r["has-previous-page?"]
    };
  return t;
}
function tu({ store: e, pageInfo: t, aggregate: n }, r) {
  const s = { data: Object.keys(r).reduce(function(u, a) {
    return n != null && n[a] || a === "$$ruleParams" || (u[a] = vi(e, {
      etype: a,
      form: r[a],
      level: 0,
      pageInfo: t == null ? void 0 : t[a]
    })), u;
  }, {}) };
  return t && (s.pageInfo = eu(t)), n && (s.aggregate = n), s;
}
function nu() {
  const t = {
    __etype: 1,
    __ops: 1,
    create: 1,
    update: 1,
    link: 1,
    unlink: 1,
    delete: 1,
    merge: 1,
    ruleParams: 1
  };
  return new Set(Object.keys(t));
}
const ru = nu();
function gn(e, t, n) {
  const r = {
    __etype: e,
    __ops: n
  };
  return new Proxy(r, {
    get: (i, s) => {
      if (s === "__ops")
        return n;
      if (s === "__etype")
        return e;
      if (ru.has(s))
        return (o, u) => gn(e, t, [
          ...n,
          u ? [s, e, t, o, u] : [s, e, t, o]
        ]);
    }
  });
}
function fc(e, t) {
  return `lookup__${e}__${JSON.stringify(t)}`;
}
function Rt(e) {
  return e.startsWith("lookup__");
}
function Si(e) {
  const [t, n, ...r] = e.split("__");
  return [n, JSON.parse(r.join("__"))];
}
function iu(e) {
  return new Proxy({
    __etype: e
  }, {
    get(t, n) {
      if (n === "__etype")
        return e;
      const r = n;
      return Rt(r) ? gn(e, Si(r), []) : gn(e, r, []);
    }
  });
}
function Gn() {
  return new Proxy({}, {
    get(e, t) {
      return iu(t);
    }
  });
}
const lc = Gn();
function su(e) {
  return e.__ops;
}
function ou(e, t) {
  const { attrIdMap: n, refSwapAttrIds: r } = e, i = [];
  for (const o of t) {
    const u = n[o];
    if (u)
      i.push(u);
    else if (Array.isArray(o) && o.length == 2 && n[o[0]]) {
      const [a, d] = o;
      i.push([n[a], d]);
    } else
      i.push(o);
  }
  const [s] = t;
  if ((s === "add-triple" || s === "retract-triple") && r.has(t[2])) {
    const o = i[1];
    i[1] = i[3], i[3] = o;
  }
  return i;
}
function K(e, t, n) {
  return Object.values(e).find((r) => {
    const [i, s, o] = r["forward-identity"];
    return s === t && o === n;
  });
}
function je(e, t, n) {
  return Object.values(e).find((r) => {
    const i = r["reverse-identity"];
    if (!i)
      return !1;
    const [s, o, u] = i;
    return o === t && u === n;
  });
}
function uu(e) {
  if (Array.isArray(e))
    return e;
  const t = Object.entries(e);
  if (t.length !== 1)
    throw new Error("lookup must be an object with a single unique attr and value.");
  return t[0];
}
function Tn(e, t, n) {
  return n.indexOf(".") !== -1 && // attr names can have `.` in them, so use the attr we find with a `.`
  // before assuming it's a ref lookup.
  !K(e, t, n);
}
function wn(e) {
  const [t, n, ...r] = e.split(".");
  if (r.length > 0 || n !== "id")
    throw new Error(`${e} is not a valid lookup attribute.`);
  return t;
}
function au(e, t, n) {
  if (!Tn(e, t, n))
    return K(e, t, n);
  const r = wn(n), i = K(e, t, r) || je(e, t, r);
  if (i && i["value-type"] !== "ref")
    throw new Error(`${n} does not reference a valid link attribute.`);
  return i;
}
function mn(e) {
  return typeof e == "string" && !Rt(e) ? null : typeof e == "string" && Rt(e) ? Si(e) : uu(e);
}
function re(e, t, n) {
  const r = mn(n);
  if (r === null)
    return n;
  const [i, s] = r, o = au(e, t, i);
  if (!o || !o["unique?"])
    throw new Error(`${i} is not a unique attribute.`);
  return [o.id, s];
}
function Oi(e, t, n, r) {
  const i = re(e, t, n);
  return Array.isArray(i) ? [[
    "add-triple",
    i,
    K(e, t, "id").id,
    i
  ]].concat(r) : r;
}
function cu({ attrs: e }, [t, n, r]) {
  const i = Object.entries(r).flatMap(([s, o]) => {
    const u = Array.isArray(o) ? o : [o], a = K(e, t, s), d = je(e, t, s);
    return u.map((c) => a ? [
      "add-triple",
      re(e, t, n),
      a.id,
      re(e, a["reverse-identity"][1], c)
    ] : [
      "add-triple",
      re(e, d["forward-identity"][1], c),
      d.id,
      re(e, t, n)
    ]);
  });
  return Oi(e, t, n, i);
}
function du({ attrs: e }, [t, n, r]) {
  const i = Object.entries(r).flatMap(([s, o]) => {
    const u = Array.isArray(o) ? o : [o], a = K(e, t, s), d = je(e, t, s);
    return u.map((c) => a ? [
      "retract-triple",
      re(e, t, n),
      a.id,
      re(e, a["reverse-identity"][1], c)
    ] : [
      "retract-triple",
      re(e, d["forward-identity"][1], c),
      d.id,
      re(e, t, n)
    ]);
  });
  return Oi(e, t, n, i);
}
function fu(e, t, n) {
  if (Array.isArray(n)) {
    const [r, i] = n;
    for (const s of e || []) {
      const o = s == null ? void 0 : s.aev.get(r);
      if (o) {
        for (const [u, a, d] of ne(o, 2))
          if (d === i)
            return !0;
      }
    }
  } else
    for (const r of e || []) {
      const i = r == null ? void 0 : r.eav.get(n);
      if (i) {
        for (const s of i.keys())
          if (r.attrs[s]["forward-identity"][1] == t)
            return !0;
      }
    }
  return !1;
}
function Ei({ stores: e, attrs: t }, [n, r, i, s]) {
  return (s == null ? void 0 : s.upsert) === !1 ? { mode: "update" } : (s == null ? void 0 : s.upsert) === !0 ? null : fu(e, n, r) ? { mode: "update" } : null;
}
function lu(e, t) {
  const { stores: n, attrs: r } = e, [i, s, o, u] = t, a = Vn(o), d = re(r, i, s);
  return [["id", d]].concat(Object.entries(a)).map(([f, h]) => {
    const p = K(r, i, f);
    return p["checked-data-type"] === "date" && e.useDateObjects && (h = xt(h)), ["add-triple", d, p.id, h, { mode: "create" }];
  });
}
function hu(e, t) {
  const { stores: n, attrs: r } = e, [i, s, o, u] = t, a = Vn(o), d = re(r, i, s), c = Ei(e, [i, d, o, u]);
  return [["id", d]].concat(Object.entries(a)).map(([h, p]) => {
    const _ = K(r, i, h);
    return _["checked-data-type"] === "date" && e.useDateObjects && (p = xt(p)), [
      "add-triple",
      d,
      _.id,
      p,
      ...c ? [c] : []
    ];
  });
}
function pu({ attrs: e }, [t, n]) {
  return [["delete-entity", re(e, t, n), t]];
}
function yu(e, t) {
  const { stores: n, attrs: r } = e, [i, s, o, u] = t, a = Vn(o), d = re(r, i, s), c = Ei(e, [i, d, o, u]), f = Object.entries(a).map(([p, _]) => {
    const b = K(r, i, p);
    return [
      "deep-merge-triple",
      d,
      b.id,
      _,
      ...c ? [c] : []
    ];
  });
  return [[
    "add-triple",
    d,
    K(r, i, "id").id,
    d,
    ...c ? [c] : []
  ]].concat(f);
}
function bu({ attrs: e }, [t, n, r]) {
  return [["rule-params", re(e, t, n), t, r]];
}
function _u(e) {
  const [t, n, r, i, s] = e;
  if (!i)
    return e;
  const o = Object.assign({}, i);
  return delete o.id, [t, n, r, o, ...s ? [s] : []];
}
function gu(e, t) {
  const [n, ...r] = _u(t);
  switch (n) {
    case "merge":
      return yu(e, r);
    case "create":
      return lu(e, r);
    case "update":
      return hu(e, r);
    case "link":
      return cu(e, r);
    case "unlink":
      return du(e, r);
    case "delete":
      return pu(e, r);
    case "ruleParams":
      return bu(e, r);
    default:
      throw new Error(`unsupported action ${n}`);
  }
}
function Tu(e) {
  switch (e) {
    case "string":
    case "date":
    case "boolean":
    case "number":
      return e;
    default:
      return;
  }
}
function wu(e, t, n) {
  var r, i;
  const s = (i = (r = e.entities[t]) === null || r === void 0 ? void 0 : r.attrs) === null || i === void 0 ? void 0 : i[n];
  if (n === "id")
    return null;
  if (!s)
    throw new Error(`${t}.${n} does not exist in your schema`);
  const { unique: o, indexed: u } = s == null ? void 0 : s.config, a = Tu(s == null ? void 0 : s.valueType);
  return {
    "index?": u,
    "unique?": o,
    "checked-data-type": a
  };
}
function vt(e, t, n, r) {
  const i = e ? wu(e, t, n) : null, s = B(), u = [B(), t, n];
  return Object.assign(Object.assign({ id: s, "forward-identity": u, "value-type": "blob", cardinality: "one", "unique?": !1, "index?": !1, isUnsynced: !0 }, i || {}), r || {});
}
function mu(e, t, n) {
  return Object.values(e.links).find((i) => i.forward.on === t && i.forward.label === n || i.reverse.on === t && i.reverse.label === n);
}
function vu(e, t, n) {
  const r = mu(e, t, n);
  if (!r)
    throw new Error(`Couldn't find the link ${t}.${n} in your schema`);
  const { forward: i, reverse: s } = r;
  return {
    "forward-identity": [B(), i.on, i.label],
    "reverse-identity": [B(), s.on, s.label],
    cardinality: i.has === "one" ? "one" : "many",
    "unique?": s.has === "one",
    "on-delete": i.onDelete,
    "on-delete-reverse": s.onDelete
  };
}
function Cr(e, t, n, r) {
  const i = e ? vu(e, t, n) : null, s = B(), o = [B(), t, n], u = [B(), n, t];
  return Object.assign(Object.assign({ id: s, "forward-identity": o, "reverse-identity": u, "value-type": "ref", cardinality: "many", "unique?": !1, "index?": !1, isUnsynced: !0 }, i || {}), r || {});
}
const Su = /* @__PURE__ */ new Set(["create", "update", "merge", "link", "unlink"]), Ou = /* @__PURE__ */ new Set(["link", "unlink"]), Eu = /* @__PURE__ */ new Set(["create", "update", "merge"]), Au = /* @__PURE__ */ new Set([
  "link",
  "unlink",
  "create",
  "update",
  "merge",
  "delete",
  "ruleParams"
]), vn = { "unique?": !0, "index?": !0 }, Cu = Object.assign(Object.assign({}, vn), { cardinality: "one" });
function ju(e) {
  const t = [], [n, r, i, s] = e;
  if (!Au.has(n))
    return t;
  const o = mn(i);
  if (o && t.push({ etype: r, lookupPair: o }), n === "link")
    for (const [u, a] of Object.entries(s)) {
      const d = Array.isArray(a) ? a : [a];
      for (const c of d) {
        const f = mn(c);
        f && t.push({
          etype: r,
          lookupPair: f,
          linkLabel: u
        });
      }
    }
  return t;
}
function ku({ attrs: e, schema: t }, n) {
  var r, i;
  const [s, o, u] = [/* @__PURE__ */ new Set(), Object.assign({}, e), []];
  function a(f) {
    o[f.id] = f, u.push(["add-attr", f]), s.add(f.id);
  }
  function d(f) {
    f != null && f.isUnsynced && !s.has(f.id) && (u.push(["add-attr", f]), s.add(f.id));
  }
  function c(f, h) {
    const p = K(o, f, h), _ = je(o, f, h);
    d(p), d(_), !p && !_ && a(Cr(t, f, h, Cu));
  }
  for (const f of n)
    for (const { etype: h, lookupPair: p, linkLabel: _ } of ju(f)) {
      const b = p[0];
      if (_) {
        c(h, _);
        const g = K(o, h, _), T = je(o, h, _);
        d(g), d(T);
        const w = ((r = g == null ? void 0 : g["reverse-identity"]) === null || r === void 0 ? void 0 : r[1]) || ((i = T == null ? void 0 : T["forward-identity"]) === null || i === void 0 ? void 0 : i[1]) || _;
        if (Tn(o, w, b))
          c(w, wn(b));
        else {
          const v = K(o, w, b);
          v || a(vt(t, w, b, vn)), d(v);
        }
      } else if (Tn(o, h, b))
        c(h, wn(b));
      else {
        const g = K(o, h, b);
        g || a(vt(t, h, b, vn)), d(g);
      }
    }
  for (const f of n) {
    const [h, p, _, b] = f;
    if (Su.has(h)) {
      const g = K(o, p, "id");
      d(g), g || a(vt(t, p, "id", { "unique?": !0 }));
      for (const T of Object.keys(b)) {
        const w = K(o, p, T);
        if (d(w), Eu.has(h) && (w || a(vt(t, p, T, T === "id" ? { "unique?": !0 } : null))), Ou.has(h)) {
          const v = je(o, p, T);
          !w && !v && a(Cr(t, p, T)), d(v);
        }
      }
    }
  }
  return [o, u];
}
function Pu(e, t) {
  const r = (Array.isArray(t) ? t : [t]).flatMap((a) => su(a)), [i, s] = ku(e, r), o = Object.assign(Object.assign({}, e), { attrs: i }), u = r.flatMap((a) => gu(o, a));
  return [...s, ...u];
}
var jr = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
class Ai {
  constructor(t) {
    this.dbName = t, this._storeName = "kv", this._dbPromise = this._init();
  }
  _init() {
    return new Promise((t, n) => {
      const r = indexedDB.open(this.dbName, 1);
      r.onerror = (i) => {
        n(i);
      }, r.onsuccess = (i) => {
        const s = i.target;
        t(s.result);
      }, r.onupgradeneeded = (i) => {
        i.target.result.createObjectStore(this._storeName);
      };
    });
  }
  getItem(t) {
    return jr(this, void 0, void 0, function* () {
      const n = yield this._dbPromise;
      return new Promise((r, i) => {
        const u = n.transaction([this._storeName], "readonly").objectStore(this._storeName).get(t);
        u.onerror = (a) => {
          i(a);
        }, u.onsuccess = (a) => {
          u.result ? r(u.result) : r(null);
        };
      });
    });
  }
  setItem(t, n) {
    return jr(this, void 0, void 0, function* () {
      const r = yield this._dbPromise;
      return new Promise((i, s) => {
        const a = r.transaction([this._storeName], "readwrite").objectStore(this._storeName).put(n, t);
        a.onerror = (d) => {
          s(d);
        }, a.onsuccess = (d) => {
          i();
        };
      });
    });
  }
}
var Iu = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
class Ci {
  static getIsOnline() {
    return Iu(this, void 0, void 0, function* () {
      return navigator.onLine;
    });
  }
  static listen(t) {
    const n = () => {
      t(!0);
    }, r = () => {
      t(!1);
    };
    return addEventListener("online", n), addEventListener("offline", r), () => {
      removeEventListener("online", n), removeEventListener("offline", r);
    };
  }
}
class ze extends Error {
  constructor(t, n) {
    super(t), this.hint = n;
    const r = new.target.prototype;
    Object.setPrototypeOf && Object.setPrototypeOf(this, r), Error.captureStackTrace && Error.captureStackTrace(this, ze), this.name = "InstantError";
  }
  get [Symbol.toStringTag]() {
    return "InstantError";
  }
}
var Mu = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
class Lt extends ze {
  constructor(t) {
    var n;
    const r = ((n = t.body) === null || n === void 0 ? void 0 : n.message) || `API Error (${t.status})`;
    super(r, t.body.hint);
    const i = new.target.prototype;
    Object.setPrototypeOf && Object.setPrototypeOf(this, i), Error.captureStackTrace && Error.captureStackTrace(this, Lt), this.name = "InstantAPIError", this.status = t.status, this.body = t.body;
  }
  get [Symbol.toStringTag]() {
    return "InstantAPIError";
  }
}
function pe(e, t) {
  return Mu(this, void 0, void 0, function* () {
    const n = yield fetch(e, t), r = yield n.json();
    return n.status === 200 ? Promise.resolve(r) : Promise.reject(new Lt({ status: n.status, body: r }));
  });
}
var Be = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
function Ru({ apiURI: e, appId: t, email: n }) {
  return pe(`${e}/runtime/auth/send_magic_code`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({ "app-id": t, email: n })
  });
}
function $u(e) {
  return Be(this, arguments, void 0, function* ({ apiURI: t, appId: n, email: r, code: i, refreshToken: s }) {
    return yield pe(`${t}/runtime/auth/verify_magic_code`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify(Object.assign({
        "app-id": n,
        email: r,
        code: i
      }, s ? { "refresh-token": s } : {}))
    });
  });
}
function xu(e) {
  return Be(this, arguments, void 0, function* ({ apiURI: t, appId: n, refreshToken: r }) {
    return yield pe(`${t}/runtime/auth/verify_refresh_token`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        "app-id": n,
        "refresh-token": r
      })
    });
  });
}
function Du(e) {
  return Be(this, arguments, void 0, function* ({ apiURI: t, appId: n }) {
    return yield pe(`${t}/runtime/auth/sign_in_guest`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        "app-id": n
      })
    });
  });
}
function kr(e) {
  return Be(this, arguments, void 0, function* ({ apiURI: t, appId: n, code: r, codeVerifier: i, refreshToken: s }) {
    return yield pe(`${t}/runtime/oauth/token`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        app_id: n,
        code: r,
        code_verifier: i,
        refresh_token: s
      })
    });
  });
}
function Uu(e) {
  return Be(this, arguments, void 0, function* ({ apiURI: t, appId: n, nonce: r, idToken: i, clientName: s, refreshToken: o }) {
    return yield pe(`${t}/runtime/oauth/id_token`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        app_id: n,
        nonce: r,
        id_token: i,
        client_name: s,
        refresh_token: o
      })
    });
  });
}
function Lu(e) {
  return Be(this, arguments, void 0, function* ({ apiURI: t, appId: n, refreshToken: r }) {
    return yield pe(`${t}/runtime/signout`, {
      method: "POST",
      headers: {
        "content-type": "application/json"
      },
      body: JSON.stringify({
        app_id: n,
        refresh_token: r
      })
    });
  });
}
var ct = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
function Nu(e) {
  return ct(this, arguments, void 0, function* ({ apiURI: t, appId: n, path: r, file: i, refreshToken: s, contentType: o, contentDisposition: u }) {
    const a = {
      app_id: n,
      path: r,
      authorization: `Bearer ${s}`,
      "content-type": o || i.type
    };
    return u && (a["content-disposition"] = u), yield pe(`${t}/storage/upload`, {
      method: "PUT",
      headers: a,
      body: i
    });
  });
}
function Fu(e) {
  return ct(this, arguments, void 0, function* ({ apiURI: t, appId: n, path: r, refreshToken: i }) {
    const { data: s } = yield pe(`${t}/storage/files?app_id=${n}&filename=${encodeURIComponent(r)}`, {
      method: "DELETE",
      headers: {
        "content-type": "application/json",
        authorization: `Bearer ${i}`
      }
    });
    return s;
  });
}
function Wu(e) {
  return ct(this, arguments, void 0, function* ({ apiURI: t, appId: n, fileName: r, refreshToken: i, metadata: s = {} }) {
    const { data: o } = yield pe(`${t}/storage/signed-upload-url`, {
      method: "POST",
      headers: {
        "content-type": "application/json",
        authorization: `Bearer ${i}`
      },
      body: JSON.stringify({
        app_id: n,
        filename: r
      })
    });
    return o;
  });
}
function Vu(e, t) {
  return ct(this, void 0, void 0, function* () {
    return (yield fetch(e, {
      method: "PUT",
      body: t,
      headers: {
        "Content-Type": t.type
      }
    })).ok;
  });
}
function qu(e) {
  return ct(this, arguments, void 0, function* ({ apiURI: t, appId: n, path: r, refreshToken: i }) {
    const { data: s } = yield pe(`${t}/storage/signed-download-url?app_id=${n}&filename=${encodeURIComponent(r)}`, {
      method: "GET",
      headers: {
        "content-type": "application/json",
        authorization: `Bearer ${i}`
      }
    });
    return s;
  });
}
let Qn = !1, ji = !1, ki = !1;
typeof window < "u" && typeof window.localStorage < "u" && (Qn = !!window.localStorage.getItem("devBackend"), ji = !!window.localStorage.getItem("__instantLogging"), ki = !!window.localStorage.getItem("__devtoolLocalDash"));
function Pr(e, t) {
  if (!t)
    return e;
  const n = {};
  return t.forEach((r) => {
    n[r] = e[r];
  }), n;
}
function zu(e, t, n) {
  var r, i;
  const s = {
    peers: {}
  };
  if (t && "user" in t ? t.user : !0) {
    const u = Pr((r = e.user) !== null && r !== void 0 ? r : {}, t == null ? void 0 : t.keys);
    s.user = Object.assign(Object.assign({}, u), { peerId: n });
  }
  for (const u of Object.keys((i = e.peers) !== null && i !== void 0 ? i : {})) {
    const a = (t == null ? void 0 : t.peers) === void 0, d = Array.isArray(t == null ? void 0 : t.peers) && (t == null ? void 0 : t.peers.includes(u));
    if (a || d) {
      const c = Pr(e.peers[u], t == null ? void 0 : t.keys);
      s.peers[u] = Object.assign(Object.assign({}, c), { peerId: u });
    }
  }
  return s;
}
function Ku(e, t) {
  if (e.isLoading !== t.isLoading || e.error !== t.error || (e.user || t.user) && (!e.user || !t.user || !_r(e.user, t.user)) || !ai(e.peers, t.peers))
    return !0;
  for (const r of Object.keys(e.peers))
    if (!_r(e.peers[r], t.peers[r]))
      return !0;
  return !1;
}
class Ir {
  constructor() {
    this.promise = new Promise((t, n) => {
      this._resolve = t, this._reject = n;
    });
  }
  resolve(t) {
    this._resolve(t);
  }
  reject(t) {
    this._reject(t);
  }
}
var Ye = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
function Bu(e, t) {
  typeof requestIdleCallback > "u" ? e() : requestIdleCallback(e, { timeout: t });
}
class Mr {
  constructor(t, n, r, i, s = (d) => d, o = (d) => d, u = 100, a = 1e3) {
    this._subs = [], this._nextSave = null, this._persister = t, this._key = n, this._onMerge = i, this._loadedCbs = [], this._isLoading = !0, this.currentValue = r, this.serialize = s, this.parse = o, this._saveThrottleMs = u, this._pendingSaveCbs = [], this._version = 0, this._idleCallbackMaxWaitMs = a, this._load();
  }
  _getFromStorage() {
    return Ye(this, void 0, void 0, function* () {
      try {
        return this.parse(yield this._persister.getItem(this._key));
      } catch (t) {
        return console.error(`Unable to read from storage for key=${this._key}`, t), null;
      }
    });
  }
  _load() {
    return Ye(this, void 0, void 0, function* () {
      const t = yield this._getFromStorage();
      this._isLoading = !1, this._onMerge(t, this.currentValue);
      for (const n of this._loadedCbs)
        n();
    });
  }
  waitForLoaded() {
    return Ye(this, void 0, void 0, function* () {
      if (!this._isLoading)
        return;
      yield new Promise((n) => {
        this._loadedCbs.push(n);
      });
    });
  }
  isLoading() {
    return this._isLoading;
  }
  version() {
    return this._version;
  }
  waitForSync() {
    return Ye(this, void 0, void 0, function* () {
      if (!this._nextSave)
        return;
      yield new Promise((n) => {
        this._pendingSaveCbs.push(n);
      });
    });
  }
  _writeToStorage() {
    this._persister.setItem(this._key, this.serialize(this.currentValue));
    for (const t of this._pendingSaveCbs)
      t();
    this._pendingSaveCbs.length = 0;
  }
  flush() {
    return Ye(this, void 0, void 0, function* () {
      this._nextSave && (clearTimeout(this._nextSave), this._writeToStorage());
    });
  }
  _enqueuePersist(t) {
    if (this._nextSave) {
      t && this._pendingSaveCbs.push(t);
      return;
    }
    this._nextSave = setTimeout(() => {
      Bu(() => {
        this._nextSave = null, this._writeToStorage();
      }, this._idleCallbackMaxWaitMs);
    }, this._saveThrottleMs);
  }
  set(t, n) {
    this._version++, this.currentValue = t(this.currentValue), this._isLoading ? this._loadedCbs.push(() => this._enqueuePersist(n)) : this._enqueuePersist(n);
    for (const r of this._subs)
      r(this.currentValue);
  }
  subscribe(t) {
    return this._subs.push(t), t(this.currentValue), () => {
      this._subs = this._subs.filter((n) => n !== t);
    };
  }
}
function Pi(e, t = []) {
  e.forEach((n) => {
    const { data: r } = n, { "datalog-result": i } = r, { "join-rows": s } = i;
    for (const o of s)
      for (const u of o)
        t.push(u);
    Pi(n["child-nodes"], t);
  });
}
function Rr(e) {
  const t = [];
  return Pi(e, t), t;
}
function $r(e) {
  return Object.values(e.links).reduce((t, n) => {
    var r, i, s, o;
    return (r = t[s = n.forward.on]) !== null && r !== void 0 || (t[s] = {}), t[n.forward.on][n.forward.label] = {
      isForward: !0,
      isSingular: n.forward.has === "one",
      link: n
    }, (i = t[o = n.reverse.on]) !== null && i !== void 0 || (t[o] = {}), t[n.reverse.on][n.reverse.label] = {
      isForward: !1,
      isSingular: n.reverse.has === "one",
      link: n
    }, t;
  }, {});
}
const Yn = "v0.22.34";
function Gu(e) {
  return {
    info: e ? console.info.bind(console) : () => {
    },
    debug: e ? console.debug.bind(console) : () => {
    },
    error: e ? console.error.bind(console) : () => {
    }
  };
}
class Y {
  constructor(t, n, r, i = { indexed: !1, unique: !1 }) {
    this.valueType = t, this.required = n, this.isIndexed = r, this.config = i, this.metadata = {};
  }
  /**
   * @deprecated Only use this temporarily for attributes that you want
   * to treat as required in frontend code but can’t yet mark as required
   * and enforced for backend
   */
  clientRequired() {
    return new Y(this.valueType, !1, this.isIndexed, this.config);
  }
  optional() {
    return new Y(this.valueType, !1, this.isIndexed, this.config);
  }
  unique() {
    return new Y(this.valueType, this.required, this.isIndexed, Object.assign(Object.assign({}, this.config), { unique: !0 }));
  }
  indexed() {
    return new Y(this.valueType, this.required, !0, Object.assign(Object.assign({}, this.config), { indexed: !0 }));
  }
}
class Nt {
  constructor(t, n) {
    this.attrs = t, this.links = n;
  }
  asType() {
    return new Nt(this.attrs, this.links);
  }
}
class Ft {
  constructor(t, n, r) {
    this.entities = t, this.links = n, this.rooms = r;
  }
  /**
   * @deprecated
   * `withRoomSchema` is deprecated. Define your schema in `rooms` directly:
   *
   * @example
   * // Before:
   * const schema = i.schema({
   *   // ...
   * }).withRoomSchema<RoomSchema>()
   *
   * // After
   * const schema = i.schema({
   *  rooms: {
   *    // ...
   *  }
   * })
   *
   * @see https://instantdb.com/docs/presence-and-topics#typesafety
   */
  withRoomSchema() {
    return new Ft(this.entities, this.links, {});
  }
}
class R extends Error {
  constructor(t, n) {
    const r = n ? `At path '${n}': ${t}` : t;
    super(r), this.name = "QueryValidationError";
  }
}
const xr = [
  "where",
  "order",
  "limit",
  "last",
  "first",
  "offset",
  "after",
  "before",
  "fields",
  "aggregate"
], Qu = (e) => e.valueType || "unknown", Ii = (e, t, n = !1) => {
  if (n || e == null)
    return !0;
  switch (t) {
    case "string":
      return typeof e == "string";
    case "number":
      return typeof e == "number" && !isNaN(e);
    case "boolean":
      return typeof e == "boolean";
    case "date":
      return e instanceof Date || typeof e == "string" || typeof e == "number";
    default:
      return !0;
  }
}, Yu = (e, t, n, r, i, s, o) => {
  const u = s.valueType === "json", a = (d, c, f) => {
    if (!Ii(f, c, u))
      throw new R(`Invalid value for operator '${d}' on attribute '${r}' in entity '${i}'. Expected ${c}, but received: ${typeof f}`, o);
  };
  switch (e) {
    case "in":
    case "$in":
      if (!Array.isArray(t))
        throw new R(`Operator '${e}' for attribute '${r}' in entity '${i}' must be an array, but received: ${typeof t}`, o);
      for (const d of t)
        a(e, n, d);
      break;
    case "$not":
    case "$ne":
    case "$gt":
    case "$lt":
    case "$gte":
    case "$lte":
      a(e, n, t);
      break;
    case "$like":
    case "$ilike":
      if (a(e, "string", t), e === "$ilike" && !s.isIndexed)
        throw new R(`Operator '${e}' can only be used with indexed attributes, but '${r}' in entity '${i}' is not indexed`, o);
      break;
    case "$isNull":
      a(e, "boolean", t);
      break;
    default:
      throw new R(`Unknown operator '${e}' for attribute '${r}' in entity '${i}'`, o);
  }
}, Ne = (e, t, n, r, i) => {
  const s = Qu(n), o = n.valueType === "json";
  if (typeof e == "object" && e !== null && !Array.isArray(e)) {
    if (o)
      return;
    const a = e;
    for (const [d, c] of Object.entries(a))
      Yu(d, c, s, t, r, n, `${i}.${d}`);
  } else if (!Ii(e, s, o))
    throw new R(`Invalid value for attribute '${t}' in entity '${r}'. Expected ${s}, but received: ${typeof e}`, i);
}, Hu = (e, t, n, r, i) => {
  const s = e.split(".");
  if (s.length < 2)
    throw new R(`Invalid dot notation path '${e}'. Must contain at least one dot.`, i);
  let o = n;
  for (let c = 0; c < s.length - 1; c++) {
    const f = s[c], h = r.entities[o];
    if (!h)
      throw new R(`Entity '${o}' does not exist in schema while traversing dot notation path '${e}'.`, i);
    const p = h.links[f];
    if (!p) {
      const _ = Object.keys(h.links);
      throw new R(`Link '${f}' does not exist on entity '${o}' in dot notation path '${e}'. Available links: ${_.length > 0 ? _.join(", ") : "none"}`, i);
    }
    o = p.entityName;
  }
  const u = s[s.length - 1], a = r.entities[o];
  if (!a)
    throw new R(`Target entity '${o}' does not exist in schema for dot notation path '${e}'.`, i);
  if (u === "id") {
    if (typeof t == "string" && !Ve(t))
      throw new R(`Invalid value for id field in entity '${o}'. Expected a UUID, but received: ${t}`, i);
    Ne(t, e, new Y("string", !1, !0), n, i);
    return;
  }
  const d = a.attrs[u];
  if (Object.keys(a.links).includes(u)) {
    if (typeof t == "string" && !Ve(t))
      throw new R(`Invalid value for link '${u}' in entity '${o}'. Expected a UUID, but received: ${t}`, i);
    Ne(t, e, new Y("string", !1, !0), n, i);
    return;
  }
  if (!d) {
    const c = Object.keys(a.attrs);
    throw new R(`Attribute '${u}' does not exist on entity '${o}' in dot notation path '${e}'. Available attributes: ${c.length > 0 ? c.join(", ") + ", id" : "id"}`, i);
  }
  Ne(t, e, d, n, i);
}, Mi = (e, t, n, r) => {
  for (const [i, s] of Object.entries(e)) {
    if (i === "or" || i === "and") {
      if (Array.isArray(s))
        for (const d of s)
          typeof d == "object" && d !== null && Mi(d, t, n, `${r}.${i}[${d}]`);
      continue;
    }
    if (i === "id") {
      Ne(s, "id", new Y("string", !1, !0), t, `${r}.id`);
      continue;
    }
    if (i.includes(".")) {
      Hu(i, s, t, n, `${r}.${i}`);
      continue;
    }
    const o = n.entities[t];
    if (!o)
      continue;
    const u = o.attrs[i], a = o.links[i];
    if (!u && !a) {
      const d = Object.keys(o.attrs), c = Object.keys(o.links);
      throw new R(`Attribute or link '${i}' does not exist on entity '${t}'. Available attributes: ${d.length > 0 ? d.join(", ") : "none"}. Available links: ${c.length > 0 ? c.join(", ") : "none"}`, `${r}.${i}`);
    }
    if (u)
      Ne(s, i, u, t, `${r}.${i}`);
    else if (a) {
      if (typeof s == "string" && !Ve(s))
        throw new R(`Invalid value for link '${i}' in entity '${t}'. Expected a UUID, but received: ${s}`, `${r}.${i}`);
      const d = new Y("string", !1, !0);
      Ne(s, i, d, t, `${r}.${i}`);
    }
  }
}, Ju = (e, t, n, r, i = 0) => {
  for (const o of Object.keys(e))
    if (!xr.includes(o))
      throw new R(`Invalid query parameter '${o}' in $ object. Valid parameters are: ${xr.join(", ")}. Found: ${o}`, r);
  const s = [
    "limit",
    "offset",
    "before",
    "after",
    "first",
    "last"
  ];
  for (const o of s)
    if (e[o] !== void 0 && i > 0)
      throw new R(`'${o}' can only be used on top-level namespaces. It cannot be used in nested queries.`, r);
  if (e.where && n) {
    if (typeof e.where != "object" || e.where === null)
      throw new R(`'where' clause must be an object in entity '${t}', but received: ${typeof e.where}`, r ? `${r}.where` : void 0);
    Mi(e.where, t, n, r ? `${r}.where` : "where");
  }
}, Ri = (e, t, n, r, i = 0) => {
  var s;
  if (!e || typeof e != "object")
    throw new R(`Query part for entity '${t}' must be an object, but received: ${typeof e}`, r);
  for (const o of Object.keys(e))
    if (o !== "$") {
      if (n && !(o in n.entities[t].links)) {
        const a = Object.keys(n.entities[t].links);
        throw new R(`Link '${o}' does not exist on entity '${t}'. Available links: ${a.length > 0 ? a.join(", ") : "none"}`, `${r}.${o}`);
      }
      const u = e[o];
      if (typeof u == "object" && u !== null) {
        const a = (s = n == null ? void 0 : n.entities[t].links[o]) === null || s === void 0 ? void 0 : s.entityName;
        a && Ri(u, a, n, `${r}.${o}`, i + 1);
      }
    } else {
      const u = e[o];
      if (typeof u != "object" || u === null)
        throw new R(`Query parameter '$' must be an object in entity '${t}', but received: ${typeof u}`, `${r}.$`);
      Ju(u, t, n, `${r}.$`, i);
    }
}, Dr = (e, t) => {
  if (typeof e != "object" || e === null)
    throw new R(`Query must be an object, but received: ${typeof e}${e === null ? " (null)" : ""}`);
  if (Array.isArray(e))
    throw new R(`Query must be an object, but received: ${typeof e}`);
  const n = e;
  for (const r of Object.keys(n)) {
    if (Array.isArray(e[r]))
      throw new R(`Query keys must be strings, but found key of type: ${typeof r}`, r);
    if (typeof r != "string")
      throw new R(`Query keys must be strings, but found key of type: ${typeof r}`, r);
    if (r !== "$$ruleParams") {
      if (t && !t.entities[r]) {
        const i = Object.keys(t.entities);
        throw new R(`Entity '${r}' does not exist in schema. Available entities: ${i.length > 0 ? i.join(", ") : "none"}`, r);
      }
      Ri(n[r], r, t, r, 0);
    }
  }
}, Ur = (e) => typeof e != "string" ? !1 : Rt(e) ? !0 : Ve(e);
class ae extends Error {
  constructor(t) {
    super(t), this.name = "TransactionValidationError";
  }
}
const $i = (e) => e.length > 0 ? e.join(", ") : "none", Zu = (e, t) => new ae(`Entity '${e}' does not exist in schema. Available entities: ${$i(t)}`), Lr = {
  string: (e) => typeof e == "string",
  number: (e) => typeof e == "number" && !isNaN(e),
  boolean: (e) => typeof e == "boolean",
  date: (e) => e instanceof Date || typeof e == "string" || typeof e == "number",
  json: () => !0
}, Xu = (e, t) => {
  var n, r;
  return e == null ? !0 : (r = (n = Lr[t.valueType]) === null || n === void 0 ? void 0 : n.call(Lr, e)) !== null && r !== void 0 ? r : !1;
}, xi = (e, t) => {
  const n = t.entities[e];
  if (!n)
    throw Zu(e, Object.keys(t.entities));
  return n;
}, Zt = (e, t, n) => {
  const r = xi(e, n);
  if (typeof t != "object" || t === null)
    throw new ae(`Arguments for data operation on entity '${e}' must be an object, but received: ${typeof t}`);
  for (const [i, s] of Object.entries(t)) {
    if (i === "id")
      continue;
    const o = r.attrs[i];
    if (o && !Xu(s, o))
      throw new ae(`Invalid value for attribute '${i}' in entity '${e}'. Expected ${o.valueType}, but received: ${typeof s}`);
  }
}, Nr = (e, t, n) => {
  const r = xi(e, n);
  if (typeof t != "object" || t === null)
    throw new ae(`Arguments for link operation on entity '${e}' must be an object, but received: ${typeof t}`);
  for (const [i, s] of Object.entries(t)) {
    if (!r.links[i]) {
      const u = Object.keys(r.links);
      throw new ae(`Link '${i}' does not exist on entity '${e}'. Available links: ${$i(u)}`);
    }
    if (s != null) {
      if (Array.isArray(s)) {
        for (const u of s)
          if (!Ur(u))
            throw new ae(`Invalid entity ID in link '${i}' for entity '${e}'. Expected a UUID or a lookup, but received: ${u}`);
      } else if (!Ur(s))
        throw new ae(`Invalid UUID in link '${i}' for entity '${e}'. Expected a UUID, but received: ${s}`);
    }
  }
}, ea = {
  create: Zt,
  update: Zt,
  merge: Zt,
  link: Nr,
  unlink: Nr,
  delete: () => {
  }
}, ta = (e, t) => {
  if (!t)
    return;
  const [n, r, i, s] = e;
  if (!Array.isArray(i) && !Ve(i))
    throw new ae(`Invalid id for entity '${r}'. Expected a UUID, but received: ${i}`);
  if (typeof r != "string")
    throw new ae(`Entity name must be a string, but received: ${typeof r}`);
  const o = ea[n];
  o && s !== void 0 && o(r, s, t);
}, na = (e, t) => {
  const n = Array.isArray(e) ? e : [e];
  for (const r of n) {
    if (!r || typeof r != "object")
      throw new ae(`Transaction chunk must be an object, but received: ${typeof r}`);
    if (!Array.isArray(r.__ops))
      throw new ae(`Transaction chunk must have __ops array, but received: ${typeof r.__ops}`);
    for (const i of r.__ops) {
      if (!Array.isArray(i))
        throw new ae(`Transaction operation must be an array, but received: ${typeof i}`);
      ta(i, t);
    }
  }
};
var Fr = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
let Di = 0;
class Wr {
  constructor(t) {
    this.type = "ws", this.id = `${this.type}_${Di++}`, this.conn = new WebSocket(t), this.conn.onopen = (n) => {
      this.onopen && this.onopen({ target: this });
    }, this.conn.onmessage = (n) => {
      this.onmessage && this.onmessage({
        target: this,
        message: JSON.parse(n.data.toString())
      });
    }, this.conn.onclose = (n) => {
      this.onclose && this.onclose({ target: this });
    }, this.conn.onerror = (n) => {
      this.onerror && this.onerror({ target: this });
    };
  }
  close() {
    this.conn.close();
  }
  isOpen() {
    return this.conn.readyState === WebSocket.OPEN;
  }
  isConnecting() {
    return this.conn.readyState === WebSocket.CONNECTING;
  }
  send(t) {
    return this.conn.send(JSON.stringify(t));
  }
}
class ra {
  constructor(t, n) {
    this.type = "sse", this.initParams = null, this.sendQueue = [], this.closeFired = !1, this.sseInitTimeout = null, this.id = `${this.type}_${Di++}`, this.url = n, this.ES = t, this.conn = new t(n), this.sseInitTimeout = setTimeout(() => {
      this.initParams || this.handleError();
    }, 1e4), this.conn.onmessage = (r) => {
      const i = JSON.parse(r.data);
      if (i.op === "sse-init") {
        this.initParams = {
          machineId: i["machine-id"],
          sessionId: i["session-id"],
          sseToken: i["sse-token"]
        }, this.onopen && this.onopen({ target: this }), clearTimeout(this.sseInitTimeout);
        return;
      }
      this.onmessage && this.onmessage({
        target: this,
        message: i
      });
    }, this.conn.onerror = (r) => {
      this.handleError();
    };
  }
  // Runs the onerror and closes the connection
  handleError() {
    try {
      this.onerror && this.onerror({ target: this });
    } finally {
      this.handleClose();
    }
  }
  handleClose() {
    this.conn.close(), this.onclose && !this.closeFired && (this.closeFired = !0, this.onclose({ target: this }));
  }
  postMessages(t) {
    return Fr(this, void 0, void 0, function* () {
      try {
        (yield fetch(this.url, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            machine_id: this.initParams.machineId,
            session_id: this.initParams.sessionId,
            sse_token: this.initParams.sseToken,
            messages: t
          })
        })).ok || this.handleError();
      } catch {
        this.handleError();
      }
    });
  }
  flushQueue() {
    return Fr(this, void 0, void 0, function* () {
      if (this.sendPromise || !this.sendQueue.length)
        return;
      const t = this.sendQueue;
      this.sendQueue = [];
      const n = this.postMessages(t);
      this.sendPromise = n, n.then(() => {
        this.sendPromise = null, this.flushQueue();
      });
    });
  }
  send(t) {
    if (!this.isOpen() || !this.initParams)
      throw this.isConnecting() ? new Error("Failed to execute 'send' on 'EventSource': Still in CONNECTING state.") : this.conn.readyState === this.ES.CLOSED ? new Error("EventSource is already in CLOSING or CLOSED state.") : new Error("EventSource is in invalid state.");
    this.sendQueue.push(t), this.flushQueue();
  }
  isOpen() {
    return this.conn.readyState === this.ES.OPEN && this.initParams !== null;
  }
  isConnecting() {
    return this.conn.readyState === this.ES.CONNECTING || this.conn.readyState === this.ES.OPEN && this.initParams === null;
  }
  close() {
    this.handleClose();
  }
}
var N = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
}, ia = function(e, t) {
  var n = {};
  for (var r in e) Object.prototype.hasOwnProperty.call(e, r) && t.indexOf(r) < 0 && (n[r] = e[r]);
  if (e != null && typeof Object.getOwnPropertySymbols == "function")
    for (var i = 0, r = Object.getOwnPropertySymbols(e); i < r.length; i++)
      t.indexOf(r[i]) < 0 && Object.prototype.propertyIsEnumerable.call(e, r[i]) && (n[r[i]] = e[r[i]]);
  return n;
};
const ge = {
  CONNECTING: "connecting",
  OPENED: "opened",
  AUTHENTICATED: "authenticated",
  CLOSED: "closed",
  ERRORED: "errored"
}, sa = 3e4, oa = 3e4, ua = 200, aa = {
  apiURI: "https://api.instantdb.com",
  websocketURI: "wss://api.instantdb.com/runtime/session"
}, Xt = "_instant_oauth_redirect", en = "currentUser";
function ca({ transportType: e, appId: t, apiURI: n, wsURI: r, EventSourceImpl: i }) {
  if (!i)
    return new Wr(`${r}?app_id=${t}`);
  switch (e) {
    case "ws":
      return new Wr(`${r}?app_id=${t}`);
    case "sse":
      return new ra(i, `${n}/runtime/sse?app_id=${t}`);
    default:
      throw new Error("Unknown transport type " + e);
  }
}
function da() {
  return typeof window < "u" || typeof chrome < "u";
}
const Vr = {
  "set-presence": !0,
  "set-presence-ok": !0,
  "refresh-presence": !0,
  "patch-presence": !0
};
function fa(e, t) {
  var n;
  const r = typeof e == "string" ? JSON.parse(e) : e;
  for (const i in r) {
    const s = r[i];
    if (!((n = s == null ? void 0 : s.result) === null || n === void 0) && n.store) {
      const o = s.result.store;
      s.result.store = Ks(Object.assign(Object.assign({}, o), { useDateObjects: t }));
    }
  }
  return r;
}
function la(e) {
  var t;
  const n = {};
  for (const r in e) {
    const i = e[r], s = Object.assign({}, i);
    !((t = i.result) === null || t === void 0) && t.store && (s.result = Object.assign(Object.assign({}, i.result), { store: zs(i.result.store) })), n[r] = s;
  }
  return n;
}
function qr(e) {
  return [...e].sort((t, n) => {
    const [r, i] = t, [s, o] = n, u = i.order || 0, a = o.order || 0;
    return u == a ? r < s ? -1 : r > s ? 1 : 0 : u - a;
  });
}
class ha {
  constructor(t, n = Ai, r = Ci, i, s) {
    var o, u, a;
    if (this._isOnline = !0, this._isShutdown = !1, this.status = ge.CONNECTING, this.queryCbs = {}, this.queryOnceDfds = {}, this.authCbs = [], this.attrsCbs = [], this.mutationErrorCbs = [], this.connectionStatusCbs = [], this.mutationDeferredStore = /* @__PURE__ */ new Map(), this._reconnectTimeoutId = null, this._reconnectTimeoutMs = 0, this._transportType = "ws", this._wsOk = null, this._localIdPromises = {}, this._errorMessage = null, this._oauthCallbackResponse = null, this._linkIndex = null, this._rooms = {}, this._roomsPendingLeave = {}, this._presence = {}, this._broadcastQueue = [], this._broadcastSubs = {}, this._currentUserCached = { isLoading: !0, error: void 0, user: void 0 }, this._beforeUnloadCbs = [], this._dataForQueryCache = {}, this._onMergeQuerySubs = (d, c) => {
      const f = d || {}, h = Object.assign({}, c);
      Object.entries(c).forEach(([_, b]) => {
        var g;
        const T = (g = f == null ? void 0 : f[_]) === null || g === void 0 ? void 0 : g.result, w = b.result;
        T && !w && (h[_].result = T);
      }), Object.keys(f).filter((_) => !c[_]).sort((_, b) => {
        var g, T;
        const w = ((g = f[_]) === null || g === void 0 ? void 0 : g.lastAccessed) || 0;
        return (((T = f[b]) === null || T === void 0 ? void 0 : T.lastAccessed) || 0) - w;
      }).slice(0, this.queryCacheLimit).forEach((_) => {
        h[_] = f[_];
      }), this.querySubs.set((_) => h), this.loadedNotifyAll();
    }, this._onMergePendingMutations = (d, c) => {
      const f = new Map([...d.entries(), ...c.entries()]);
      this.pendingMutations.set((p) => f), this.loadedNotifyAll(), this._rewriteMutationsSorted(this.attrs, d).forEach(([p, _]) => {
        !c.has(p) && !_["tx-id"] && this._sendMutation(p, _);
      });
    }, this.getPreviousResult = (d) => {
      const c = X(d);
      return this.dataForQuery(c);
    }, this.notifyOne = (d) => {
      var c, f;
      const h = (c = this.queryCbs[d]) !== null && c !== void 0 ? c : [], p = (f = this._dataForQueryCache[d]) === null || f === void 0 ? void 0 : f.data, _ = this.dataForQuery(d);
      _ && (At(_, p) || h.forEach((b) => b.cb(_)));
    }, this.notifyOneQueryOnce = (d) => {
      var c;
      const f = (c = this.queryOnceDfds[d]) !== null && c !== void 0 ? c : [], h = this.dataForQuery(d);
      f.forEach((p) => {
        this._completeQueryOnce(p.q, d, p.dfd), p.dfd.resolve(h);
      });
    }, this.notifyQueryError = (d, c) => {
      (this.queryCbs[d] || []).forEach((h) => h.cb({ error: c }));
    }, this.pushTx = (d) => {
      this.config.disableValidation || na(d, this.config.schema);
      try {
        const c = Pu({
          attrs: this.optimisticAttrs(),
          schema: this.config.schema,
          stores: Object.values(this.querySubs.currentValue).map((f) => {
            var h;
            return (h = f == null ? void 0 : f.result) === null || h === void 0 ? void 0 : h.store;
          }),
          useDateObjects: this.config.useDateObjects
        }, d);
        return this.pushOps(c);
      } catch (c) {
        return this.pushOps([], c);
      }
    }, this.pushOps = (d, c) => {
      const f = B(), h = [...this.pendingMutations.currentValue.values()], p = Math.max(0, ...h.map((g) => g.order || 0)) + 1, _ = {
        op: "transact",
        "tx-steps": d,
        created: Date.now(),
        error: c,
        order: p
      };
      this.pendingMutations.set((g) => (g.set(f, _), g));
      const b = new Ir();
      return this.mutationDeferredStore.set(f, b), this._sendMutation(f, _), this.notifyAll(), b.promise;
    }, this._transportOnOpen = (d) => {
      const c = d.target;
      if (this._transport !== c) {
        this._log.info("[socket][open]", c.id, "skip; this is no longer the current transport");
        return;
      }
      this._log.info("[socket][open]", this._transport.id), this._setStatus(ge.OPENED), this.getCurrentUser().then((f) => {
        var h;
        this._trySend(B(), {
          op: "init",
          "app-id": this.config.appId,
          "refresh-token": (h = f.user) === null || h === void 0 ? void 0 : h.refresh_token,
          versions: this.versions,
          // If an admin token is provided for an app, we will
          // skip all permission checks. This is an advanced feature,
          // to let users write internal tools
          // This option is not exposed in `Config`, as it's
          // not ready for prime time
          "__admin-token": this.config.__adminToken
        });
      }).catch((f) => {
        this._log.error("[socket][error]", c.id, f);
      });
    }, this._transportOnMessage = (d) => {
      const c = d.target, f = d.message;
      if (this._transport !== c) {
        this._log.info("[socket][message]", c.id, f, "skip; this is no longer the current transport");
        return;
      }
      !this._wsOk && c.type === "ws" && (this._wsOk = !0), this._transportType = "ws", this._handleReceive(c.id, d.message);
    }, this._transportOnError = (d) => {
      const c = d.target;
      if (this._transport !== c) {
        this._log.info("[socket][error]", c.id, "skip; this is no longer the current transport");
        return;
      }
      this._log.error("[socket][error]", c.id, d);
    }, this._scheduleReconnect = () => {
      !this._wsOk && this._transportType !== "sse" && (this._transportType = "sse", this._reconnectTimeoutMs = 0), setTimeout(() => {
        if (this._reconnectTimeoutMs = Math.min(this._reconnectTimeoutMs + 1e3, 1e4), !this._isOnline) {
          this._log.info("[socket][close]", this._transport.id, "we are offline, no need to start socket");
          return;
        }
        this._startSocket();
      }, this._reconnectTimeoutMs);
    }, this._transportOnClose = (d) => {
      const c = d.target;
      if (this._transport !== c) {
        this._log.info("[socket][close]", c.id, "skip; this is no longer the current transport");
        return;
      }
      this._setStatus(ge.CLOSED);
      for (const f of Object.values(this._rooms))
        f.isConnected = !1;
      if (this._isShutdown) {
        this._log.info("[socket][close]", c.id, "Reactor has been shut down and will not reconnect");
        return;
      }
      this._log.info("[socket][close]", c.id, "schedule reconnect, ms =", this._reconnectTimeoutMs), this._scheduleReconnect();
    }, this._EventSource = s, this.config = Object.assign(Object.assign({}, aa), t), this.queryCacheLimit = (o = this.config.queryCacheLimit) !== null && o !== void 0 ? o : 10, this._pendingTxCleanupTimeout = (u = this.config.pendingTxCleanupTimeout) !== null && u !== void 0 ? u : oa, this._pendingMutationCleanupThreshold = (a = this.config.pendingMutationCleanupThreshold) !== null && a !== void 0 ? a : ua, this._log = Gu(t.verbose || Qn || ji), this.versions = Object.assign(Object.assign({}, i || {}), { "@instantdb/core": Yn }), this.config.schema && (this._linkIndex = $r(this.config.schema)), !!da()) {
      if (!t.appId)
        throw new Error("Instant must be initialized with an appId.");
      if (!Ve(t.appId))
        throw new Error(`Instant must be initialized with a valid appId. \`${t.appId}\` is not a valid uuid.`);
      typeof BroadcastChannel == "function" && (this._broadcastChannel = new BroadcastChannel("@instantdb"), this._broadcastChannel.addEventListener("message", (d) => N(this, void 0, void 0, function* () {
        var c;
        try {
          if (((c = d.data) === null || c === void 0 ? void 0 : c.type) === "auth") {
            const f = yield this.getCurrentUser();
            this.updateUser(f.user);
          }
        } catch (f) {
          this._log.error("[error] handle broadcast channel", f);
        }
      }))), this._initStorage(n), this._oauthCallbackResponse = this._oauthLoginInit(), this.getCurrentUser(), r.getIsOnline().then((d) => {
        this._isOnline = d, this._startSocket(), r.listen((c) => {
          c !== this._isOnline && (this._log.info("[network] online =", c), this._isOnline = c, this._isOnline ? this._startSocket() : (this._log.info("Changing status from", this.status, "to", ge.CLOSED), this._setStatus(ge.CLOSED)));
        });
      }), typeof addEventListener < "u" && (this._beforeUnload = this._beforeUnload.bind(this), addEventListener("beforeunload", this._beforeUnload));
    }
  }
  updateSchema(t) {
    this.config = Object.assign(Object.assign({}, this.config), { schema: t, cardinalityInference: !!t }), this._linkIndex = t ? $r(this.config.schema) : null;
  }
  _initStorage(t) {
    this._persister = new t(`instant_${this.config.appId}_5`), this.querySubs = new Mr(this._persister, "querySubs", {}, this._onMergeQuerySubs, la, (n) => fa(n, this.config.useDateObjects)), this.pendingMutations = new Mr(
      this._persister,
      "pendingMutations",
      /* @__PURE__ */ new Map(),
      this._onMergePendingMutations,
      (n) => [...n.entries()],
      (n) => new Map(typeof n == "string" ? JSON.parse(n) : n),
      100,
      // throttle
      100
    ), this._beforeUnloadCbs.push(() => {
      this.pendingMutations.flush(), this.querySubs.flush();
    });
  }
  _beforeUnload() {
    for (const t of this._beforeUnloadCbs)
      t();
  }
  /**
   * @param {'enqueued' | 'pending' | 'synced' | 'timeout' |  'error' } status
   * @param {string} eventId
   * @param {{message?: string, type?: string, status?: number, hint?: unknown}} [errorMsg]
   */
  _finishTransaction(t, n, r) {
    const i = this.mutationDeferredStore.get(n);
    this.mutationDeferredStore.delete(n);
    const s = t !== "error" && t !== "timeout";
    if (!i && !s && console.error("Mutation failed", Object.assign({ status: t, eventId: n }, r)), !!i)
      if (s)
        i.resolve({ status: t, eventId: n });
      else if (r.type) {
        const { status: o } = r, u = ia(r, ["status"]);
        i.reject(new Lt({
          // @ts-expect-error body.type is not constant typed
          body: u,
          status: o
        }));
      } else
        i.reject(new ze(r.message, r.hint));
  }
  _setStatus(t, n) {
    this.status = t, this._errorMessage = n, this.notifyConnectionStatusSubs(t);
  }
  _flushEnqueuedRoomData(t) {
    var n, r;
    const i = (r = (n = this._presence[t]) === null || n === void 0 ? void 0 : n.result) === null || r === void 0 ? void 0 : r.user, s = this._broadcastQueue[t];
    if (this._broadcastQueue[t] = [], i && this._trySetPresence(t, i), s)
      for (const o of s) {
        const { topic: u, roomType: a, data: d } = o;
        this._tryBroadcast(t, a, u, d);
      }
  }
  _handleReceive(t, n) {
    var r, i, s, o, u, a;
    const d = !!this.config.schema && ("cardinalityInference" in this.config ? !!this.config.cardinalityInference : !0);
    switch (Vr[n.op] || this._log.info("[receive]", t, n.op, n), n.op) {
      case "init-ok":
        this._setStatus(ge.AUTHENTICATED), this._reconnectTimeoutMs = 0, this._setAttrs(n.attrs), this._flushPendingMessages(), this._sessionId = n["session-id"];
        for (const M of Object.keys(this._rooms)) {
          const V = (i = (r = this._presence[M]) === null || r === void 0 ? void 0 : r.result) === null || i === void 0 ? void 0 : i.user;
          this._tryJoinRoom(M, V);
        }
        break;
      case "add-query-exists":
        this.notifyOneQueryOnce(X(n.q));
        break;
      case "add-query-ok":
        const { q: c, result: f } = n, h = X(c), p = (o = (s = f == null ? void 0 : f[0]) === null || s === void 0 ? void 0 : s.data) === null || o === void 0 ? void 0 : o["page-info"], _ = (a = (u = f == null ? void 0 : f[0]) === null || u === void 0 ? void 0 : u.data) === null || a === void 0 ? void 0 : a.aggregate, b = Rr(f), g = hn(this.attrs, b, d, this._linkIndex, this.config.useDateObjects);
        this.querySubs.set((M) => (M[h].result = {
          store: g,
          pageInfo: p,
          aggregate: _,
          processedTxId: n["processed-tx-id"]
        }, M)), this._cleanupPendingMutationsQueries(), this.notifyOne(h), this.notifyOneQueryOnce(h), this._cleanupPendingMutationsTimeout();
        break;
      case "refresh-ok":
        const { computations: T, attrs: w } = n, v = n["processed-tx-id"];
        w && this._setAttrs(w), this._cleanupPendingMutationsTimeout();
        const I = this._rewriteMutations(this.attrs, this.pendingMutations.currentValue, v);
        I !== this.pendingMutations.currentValue && this.pendingMutations.set(() => I);
        const O = qr(I.entries()), U = T.map((M) => {
          var V, H, J, ce;
          const de = M["instaql-query"], ie = M["instaql-result"], lt = X(de), ht = Rr(ie), pt = hn(this.attrs, ht, d, this._linkIndex, this.config.useDateObjects), yt = this._applyOptimisticUpdates(pt, O, v), bt = (H = (V = ie == null ? void 0 : ie[0]) === null || V === void 0 ? void 0 : V.data) === null || H === void 0 ? void 0 : H["page-info"], Wt = (ce = (J = ie == null ? void 0 : ie[0]) === null || J === void 0 ? void 0 : J.data) === null || ce === void 0 ? void 0 : ce.aggregate;
          return { hash: lt, store: yt, pageInfo: bt, aggregate: Wt };
        });
        U.forEach(({ hash: M, store: V, pageInfo: H, aggregate: J }) => {
          this.querySubs.set((ce) => (ce[M].result = { store: V, pageInfo: H, aggregate: J, processedTxId: v }, ce));
        }), this._cleanupPendingMutationsQueries(), U.forEach(({ hash: M }) => {
          this.notifyOne(M);
        });
        break;
      case "transact-ok": {
        const { "client-event-id": M, "tx-id": V } = n, J = this._rewriteMutations(this.attrs, this.pendingMutations.currentValue).get(M);
        if (!J)
          break;
        this.pendingMutations.set((de) => (de.set(M, Object.assign(Object.assign({}, de.get(M)), { "tx-id": V, confirmed: Date.now() })), de));
        const ce = J["tx-steps"].filter(([de, ...ie]) => de === "add-attr").map(([de, ie]) => ie).concat(Object.values(this.attrs));
        this._setAttrs(ce), this._finishTransaction("synced", M), this._cleanupPendingMutationsTimeout();
        break;
      }
      case "patch-presence": {
        const M = n["room-id"];
        this._patchPresencePeers(M, n.edits), this._notifyPresenceSubs(M);
        break;
      }
      case "refresh-presence": {
        const M = n["room-id"];
        this._setPresencePeers(M, n.data), this._notifyPresenceSubs(M);
        break;
      }
      case "server-broadcast":
        const W = n["room-id"], _e = n.topic;
        this._notifyBroadcastSubs(W, _e, n);
        break;
      case "join-room-ok":
        const ye = n["room-id"], Ge = this._rooms[ye];
        if (!Ge) {
          this._roomsPendingLeave[ye] && (this._tryLeaveRoom(ye), delete this._roomsPendingLeave[ye]);
          break;
        }
        Ge.isConnected = !0, this._notifyPresenceSubs(ye), this._flushEnqueuedRoomData(ye);
        break;
      case "join-room-error":
        const dt = n["room-id"], ft = this._rooms[dt];
        ft && (ft.error = n.error), this._notifyPresenceSubs(dt);
        break;
      case "error":
        this._handleReceiveError(n);
        break;
    }
  }
  /**
   * @param {'timeout' | 'error'} status
   * @param {string} eventId
   * @param {{message: string, type?: string, status?: number, hint?: unknown}} errorMsg
   */
  _handleMutationError(t, n, r) {
    const i = this.pendingMutations.currentValue.get(n);
    if (i && (t !== "timeout" || !i["tx-id"])) {
      this.pendingMutations.set((o) => (o.delete(n), o));
      const s = {
        message: r.message,
        hint: r.hint
      };
      this.notifyAll(), this.notifyAttrsSubs(), this.notifyMutationErrorSubs(s), this._finishTransaction(t, n, r);
    }
  }
  _handleReceiveError(t) {
    var n, r, i, s, o;
    console.log("error", t);
    const u = t["client-event-id"], a = this.pendingMutations.currentValue.get(u), d = {
      message: t.message || "Uh-oh, something went wrong. Ping Joe & Stopa."
    };
    if (t.hint && (d.hint = t.hint), a) {
      this._handleMutationError("error", u, t);
      return;
    }
    if (!((n = t["original-event"]) === null || n === void 0) && n.hasOwnProperty("q") && ((r = t["original-event"]) === null || r === void 0 ? void 0 : r.op) === "add-query") {
      const h = (i = t["original-event"]) === null || i === void 0 ? void 0 : i.q, p = X(h);
      this.notifyQueryError(X(h), d), this.notifyQueryOnceError(h, p, u, d);
      return;
    }
    if (((s = t["original-event"]) === null || s === void 0 ? void 0 : s.op) === "init") {
      if (t.type === "record-not-found" && ((o = t.hint) === null || o === void 0 ? void 0 : o["record-type"]) === "app-user") {
        this.changeCurrentUser(null);
        return;
      }
      this._setStatus(ge.ERRORED, d), this.notifyAll();
      return;
    }
    const f = Object.assign({}, t);
    delete f.message, delete f.hint, console.error(t.message, f), t.hint && console.error(`This error comes with some debugging information. Here it is: 
`, t.hint);
  }
  notifyQueryOnceError(t, n, r, i) {
    var s;
    const o = (s = this.queryOnceDfds[n]) === null || s === void 0 ? void 0 : s.find((u) => u.eventId === r);
    o && (o.dfd.reject(i), this._completeQueryOnce(t, n, o.dfd));
  }
  _setAttrs(t) {
    this.attrs = t.reduce((n, r) => (n[r.id] = r, n), {}), this.notifyAttrsSubs();
  }
  _startQuerySub(t, n) {
    const r = B();
    return this.querySubs.set((i) => (i[n] = i[n] || { q: t, result: null, eventId: r }, i[n].lastAccessed = Date.now(), i)), this._trySendAuthed(r, { op: "add-query", q: t }), r;
  }
  /**
   *  When a user subscribes to a query the following side effects occur:
   *
   *  - We update querySubs to include the new query
   *  - We update queryCbs to include the new cb
   *  - If we already have a result for the query we call cb immediately
   *  - We send the server an `add-query` message
   *
   *  Returns an unsubscribe function
   */
  subscribeQuery(t, n, r) {
    var i;
    this.config.disableValidation || Dr(t, this.config.schema), r && "ruleParams" in r && (t = Object.assign({ $$ruleParams: r.ruleParams }, t));
    const s = X(t), o = this.getPreviousResult(t);
    return o && n(o), this.queryCbs[s] = (i = this.queryCbs[s]) !== null && i !== void 0 ? i : [], this.queryCbs[s].push({ q: t, cb: n }), this._startQuerySub(t, s), () => {
      this._unsubQuery(t, s, n);
    };
  }
  queryOnce(t, n) {
    var r;
    this.config.disableValidation || Dr(t, this.config.schema), n && "ruleParams" in n && (t = Object.assign({ $$ruleParams: n.ruleParams }, t));
    const i = new Ir();
    if (!this._isOnline)
      return i.reject(new Error("We can't run `queryOnce`, because the device is offline.")), i.promise;
    if (!this.querySubs)
      return i.reject(new Error("We can't run `queryOnce` on the backend. Use adminAPI.query instead: https://www.instantdb.com/docs/backend#query")), i.promise;
    const s = X(t), o = this._startQuerySub(t, s);
    return this.queryOnceDfds[s] = (r = this.queryOnceDfds[s]) !== null && r !== void 0 ? r : [], this.queryOnceDfds[s].push({ q: t, dfd: i, eventId: o }), setTimeout(() => i.reject(new Error("Query timed out")), sa), i.promise;
  }
  _completeQueryOnce(t, n, r) {
    this.queryOnceDfds[n] && (this.queryOnceDfds[n] = this.queryOnceDfds[n].filter((i) => i.dfd !== r), this._cleanupQuery(t, n));
  }
  _unsubQuery(t, n, r) {
    this.queryCbs[n] && (this.queryCbs[n] = this.queryCbs[n].filter((i) => i.cb !== r), this._cleanupQuery(t, n));
  }
  _cleanupQuery(t, n) {
    var r, i;
    !((r = this.queryCbs[n]) === null || r === void 0) && r.length || !((i = this.queryOnceDfds[n]) === null || i === void 0) && i.length || (delete this.queryCbs[n], delete this.queryOnceDfds[n], this._trySendAuthed(B(), { op: "remove-query", q: t }));
  }
  // When we `pushTx`, it's possible that we don't yet have `this.attrs`
  // This means that `tx-steps` in `pendingMutations` will include `add-attr`
  // commands for attrs that already exist.
  //
  // This will also affect `add-triple` and `retract-triple` which
  // reference attr-ids that do not match the server.
  //
  // We fix this by rewriting `tx-steps` in each `pendingMutation`.
  // We remove `add-attr` commands for attrs that already exist.
  // We update `add-triple` and `retract-triple` commands to use the
  // server attr-ids.
  _rewriteMutations(t, n, r) {
    if (!t)
      return n;
    const i = (c) => {
      const [f, h, p] = c["forward-identity"];
      return K(t, h, p);
    }, s = (c) => {
      const [f, h, p] = c["forward-identity"];
      return je(t, h, p);
    }, o = { attrIdMap: {}, refSwapAttrIds: /* @__PURE__ */ new Set() };
    let u = !1;
    const a = (c, f) => {
      const h = [];
      for (const p of c) {
        const [_] = p;
        if (_ === "add-attr") {
          const [g, T] = p, w = i(T);
          if (w && T.id !== w.id) {
            o.attrIdMap[T.id] = w.id, u = !0;
            continue;
          }
          if (T["value-type"] === "ref") {
            const v = s(T);
            if (v) {
              o.attrIdMap[T.id] = v.id, o.refSwapAttrIds.add(T.id), u = !0;
              continue;
            }
          }
        }
        if (r && f && r >= f && _ === "add-attr" || _ === "update-attr" || _ === "delete-attr") {
          u = !0;
          continue;
        }
        const b = u ? ou(o, p) : p;
        h.push(b);
      }
      return u ? h : c;
    }, d = /* @__PURE__ */ new Map();
    for (const [c, f] of n.entries())
      d.set(c, Object.assign(Object.assign({}, f), { "tx-steps": a(f["tx-steps"], f["tx-id"]) }));
    return u ? d : n;
  }
  _rewriteMutationsSorted(t, n) {
    return qr(this._rewriteMutations(t, n).entries());
  }
  // ---------------------------
  // Transact
  optimisticAttrs() {
    var t;
    const n = [
      ...this.pendingMutations.currentValue.values()
    ].flatMap((u) => u["tx-steps"]), r = new Set(n.filter(([u, a]) => u === "delete-attr").map(([u, a]) => a)), i = [];
    for (const [u, a] of n)
      if (u === "add-attr")
        i.push(a);
      else if (u === "update-attr" && a.id && (!((t = this.attrs) === null || t === void 0) && t[a.id])) {
        const d = Object.assign(Object.assign({}, this.attrs[a.id]), a);
        i.push(d);
      }
    const s = [
      ...Object.values(this.attrs || {}),
      ...i
    ].filter((u) => !r.has(u.id));
    return Object.fromEntries(s.map((u) => [u.id, u]));
  }
  /** Runs instaql on a query and a store */
  dataForQuery(t) {
    const n = this._errorMessage;
    if (n)
      return { error: n };
    if (!this.querySubs || !this.pendingMutations)
      return;
    const r = this.querySubs.version(), i = this.querySubs.currentValue, s = this.pendingMutations.version(), o = this.pendingMutations.currentValue, { q: u, result: a } = i[t] || {};
    if (!a)
      return;
    const d = this._dataForQueryCache[t];
    if (d && r === d.querySubVersion && s === d.pendingMutationsVersion)
      return d.data;
    const { store: c, pageInfo: f, aggregate: h, processedTxId: p } = a, _ = this._rewriteMutationsSorted(c.attrs, o), b = this._applyOptimisticUpdates(c, _, p), g = tu({ store: b, pageInfo: f, aggregate: h }, u);
    return this._dataForQueryCache[t] = {
      querySubVersion: r,
      pendingMutationsVersion: s,
      data: g
    }, g;
  }
  _applyOptimisticUpdates(t, n, r) {
    for (const [i, s] of n)
      (!s["tx-id"] || r && s["tx-id"] > r) && (t = so(t, s["tx-steps"]));
    return t;
  }
  /** Re-compute all subscriptions */
  notifyAll() {
    Object.keys(this.queryCbs).forEach((t) => {
      this.notifyOne(t);
    });
  }
  loadedNotifyAll() {
    this.pendingMutations.isLoading() || this.querySubs.isLoading() || this.notifyAll();
  }
  shutdown() {
    var t;
    this._log.info("[shutdown]", this.config.appId), this._isShutdown = !0, (t = this._transport) === null || t === void 0 || t.close();
  }
  /**
   * Sends mutation to server and schedules a timeout to cancel it if
   * we don't hear back in time.
   * Note: If we're offline we don't schedule a timeout, we'll schedule it
   * later once we're back online and send the mutation again
   *
   */
  _sendMutation(t, n) {
    if (n.error) {
      this._handleMutationError("error", t, {
        message: n.error.message
      });
      return;
    }
    if (this.status !== ge.AUTHENTICATED) {
      this._finishTransaction("enqueued", t);
      return;
    }
    const r = Math.max(5e3, this.pendingMutations.currentValue.size * 5e3);
    this._isOnline ? (this._trySend(t, n), setTimeout(() => {
      this._isOnline && this._handleMutationError("timeout", t, {
        message: "transaction timed out"
      });
    }, r)) : this._finishTransaction("enqueued", t);
  }
  // ---------------------------
  // Websocket
  /** Send messages we accumulated while we were connecting */
  _flushPendingMessages() {
    Object.keys(this.queryCbs).map((i) => this.querySubs.currentValue[i]).filter((i) => i).forEach(({ eventId: i, q: s }) => {
      this._trySendAuthed(i, { op: "add-query", q: s });
    }), Object.values(this.queryOnceDfds).flat().forEach(({ eventId: i, q: s }) => {
      this._trySendAuthed(i, { op: "add-query", q: s });
    }), this._rewriteMutationsSorted(this.attrs, this.pendingMutations.currentValue).forEach(([i, s]) => {
      s["tx-id"] || this._sendMutation(i, s);
    });
  }
  /**
   * Clean up pendingMutations that all queries have seen
   */
  _cleanupPendingMutationsQueries() {
    let t = Number.MAX_SAFE_INTEGER;
    for (const { result: n } of Object.values(this.querySubs.currentValue))
      n != null && n.processedTxId && (t = Math.min(t, n == null ? void 0 : n.processedTxId));
    this.pendingMutations.set((n) => {
      for (const [r, i] of Array.from(n.entries()))
        i["tx-id"] && i["tx-id"] <= t && n.delete(r);
      return n;
    });
  }
  /**
   * After mutations is confirmed by server, we give each query 30 sec
   * to update its results. If that doesn't happen, we assume query is
   * unaffected by this mutation and it’s safe to delete it from local queue
   */
  _cleanupPendingMutationsTimeout() {
    if (this.pendingMutations.currentValue.size < this._pendingMutationCleanupThreshold)
      return;
    const t = Date.now();
    this.pendingMutations.set((n) => {
      for (const [r, i] of Array.from(n.entries()))
        i.confirmed && i.confirmed + this._pendingTxCleanupTimeout < t && n.delete(r);
      return n;
    });
  }
  _trySendAuthed(...t) {
    this.status === ge.AUTHENTICATED && this._trySend(...t);
  }
  _trySend(t, n, r) {
    this._transport.isOpen() && (Vr[n.op] || this._log.info("[send]", this._transport.id, n.op, n), this._transport.send(Object.assign({ "client-event-id": t }, n)));
  }
  _startSocket() {
    if (this._wsOk = null, this._isShutdown) {
      this._log.info("[socket][start]", this.config.appId, "Reactor has been shut down and will not start a new socket");
      return;
    }
    if (this._transport && this._transport.isConnecting()) {
      this._log.info("[socket][start]", this._transport.id, "maintained as current transport, we were still in a connecting state");
      return;
    }
    const t = this._transport;
    this._transport = ca({
      transportType: this._transportType,
      appId: this.config.appId,
      apiURI: this.config.apiURI,
      wsURI: this.config.websocketURI,
      EventSourceImpl: this._EventSource
    }), this._transport.onopen = this._transportOnOpen, this._transport.onmessage = this._transportOnMessage, this._transport.onclose = this._transportOnClose, this._transport.onerror = this._transportOnError, this._log.info("[socket][start]", this._transport.id), t != null && t.isOpen() && (this._log.info("[socket][start]", this._transport.id, "close previous transport id = ", t.id), t.close());
  }
  /**
   * Given a key, returns a stable local id, unique to this device and app.
   *
   * This can be useful if you want to create guest ids for example.
   *
   * Note: If the user deletes their local storage, this id will change.
   *
   * We use this._localIdPromises to ensure that we only generate a local
   * id once, even if multiple callers call this function concurrently.
   */
  getLocalId(t) {
    return N(this, void 0, void 0, function* () {
      const n = `localToken_${t}`, r = yield this._persister.getItem(n);
      if (r)
        return r;
      if (this._localIdPromises[n])
        return this._localIdPromises[n];
      const i = B();
      return this._localIdPromises[n] = this._persister.setItem(n, i).then(() => i), this._localIdPromises[n];
    });
  }
  // ----
  // Auth
  _replaceUrlAfterOAuth() {
    if (typeof URL > "u")
      return;
    const t = new URL(window.location.href);
    if (t.searchParams.get(Xt)) {
      const n = t.toString();
      t.searchParams.delete(Xt), t.searchParams.delete("code"), t.searchParams.delete("error");
      const r = t.pathname + (t.searchParams.size ? "?" + t.searchParams : "") + t.hash;
      if (history.replaceState(history.state, "", r), // @ts-ignore (waiting for ts support)
      typeof navigation == "object" && // @ts-ignore (waiting for ts support)
      typeof navigation.addEventListener == "function" && // @ts-ignore (waiting for ts support)
      typeof navigation.removeEventListener == "function") {
        let i = !1;
        const s = (o) => {
          var u;
          i || (i = !0, navigation.removeEventListener("navigate", s), !o.userInitiated && o.navigationType === "replace" && ((u = o.destination) === null || u === void 0 ? void 0 : u.url) === n && history.replaceState(history.state, "", r));
        };
        navigation.addEventListener("navigate", s);
      }
    }
  }
  /**
   *
   * @returns Promise<null | {error: {message: string}}>
   */
  _oauthLoginInit() {
    return N(this, void 0, void 0, function* () {
      var t, n, r, i;
      if (typeof window > "u" || typeof window.location > "u" || typeof URLSearchParams > "u")
        return null;
      const s = new URLSearchParams(window.location.search);
      if (!s.get(Xt))
        return null;
      const o = s.get("error");
      if (o)
        return this._replaceUrlAfterOAuth(), { error: { message: o } };
      const u = s.get("code");
      if (!u)
        return null;
      this._replaceUrlAfterOAuth();
      try {
        const a = yield this._getCurrentUser(), d = (a == null ? void 0 : a.type) === "guest", { user: c } = yield kr({
          apiURI: this.config.apiURI,
          appId: this.config.appId,
          code: u,
          refreshToken: d ? a.refresh_token : void 0
        });
        return this.setCurrentUser(c), null;
      } catch (a) {
        return ((t = a == null ? void 0 : a.body) === null || t === void 0 ? void 0 : t.type) === "record-not-found" && ((r = (n = a == null ? void 0 : a.body) === null || n === void 0 ? void 0 : n.hint) === null || r === void 0 ? void 0 : r["record-type"]) === "app-oauth-code" && (yield this._hasCurrentUser()) ? null : { error: { message: ((i = a == null ? void 0 : a.body) === null || i === void 0 ? void 0 : i.message) || "Error logging in." } };
      }
    });
  }
  _waitForOAuthCallbackResponse() {
    return N(this, void 0, void 0, function* () {
      return yield this._oauthCallbackResponse;
    });
  }
  __subscribeMutationErrors(t) {
    return this.mutationErrorCbs.push(t), () => {
      this.mutationErrorCbs = this.mutationErrorCbs.filter((n) => n !== t);
    };
  }
  subscribeAuth(t) {
    this.authCbs.push(t);
    const n = this._currentUserCached;
    n.isLoading || t(this._currentUserCached);
    let r = !1;
    return this.getCurrentUser().then((i) => {
      r || At(i, n) || t(i);
    }), () => {
      r = !0, this.authCbs = this.authCbs.filter((i) => i !== t);
    };
  }
  getAuth() {
    return N(this, void 0, void 0, function* () {
      const { user: t, error: n } = yield this.getCurrentUser();
      if (n)
        throw new ze("Could not get current user: " + n.message);
      return t;
    });
  }
  subscribeConnectionStatus(t) {
    return this.connectionStatusCbs.push(t), () => {
      this.connectionStatusCbs = this.connectionStatusCbs.filter((n) => n !== t);
    };
  }
  subscribeAttrs(t) {
    return this.attrsCbs.push(t), this.attrs && t(this.attrs), () => {
      this.attrsCbs = this.attrsCbs.filter((n) => n !== t);
    };
  }
  notifyAuthSubs(t) {
    this.authCbs.forEach((n) => n(t));
  }
  notifyMutationErrorSubs(t) {
    this.mutationErrorCbs.forEach((n) => n(t));
  }
  notifyAttrsSubs() {
    if (!this.attrs)
      return;
    const t = this.optimisticAttrs();
    this.attrsCbs.forEach((n) => n(t));
  }
  notifyConnectionStatusSubs(t) {
    this.connectionStatusCbs.forEach((n) => n(t));
  }
  setCurrentUser(t) {
    return N(this, void 0, void 0, function* () {
      yield this._persister.setItem(en, t);
    });
  }
  getCurrentUserCached() {
    return this._currentUserCached;
  }
  _getCurrentUser() {
    return N(this, void 0, void 0, function* () {
      const t = yield this._persister.getItem(en);
      return typeof t == "string" ? JSON.parse(t) : t;
    });
  }
  getCurrentUser() {
    return N(this, void 0, void 0, function* () {
      const t = yield this._waitForOAuthCallbackResponse();
      if (t != null && t.error) {
        const i = { error: t.error, user: void 0 };
        return this._currentUserCached = Object.assign({ isLoading: !1 }, i), i;
      }
      const r = { user: yield this._getCurrentUser(), error: void 0 };
      return this._currentUserCached = Object.assign({ isLoading: !1 }, r), r;
    });
  }
  _hasCurrentUser() {
    return N(this, void 0, void 0, function* () {
      const t = yield this._persister.getItem(en);
      return JSON.parse(t) != null;
    });
  }
  changeCurrentUser(t) {
    return N(this, void 0, void 0, function* () {
      var n;
      const { user: r } = yield this.getCurrentUser();
      if (!At(r, t)) {
        yield this.setCurrentUser(t), this.updateUser(t);
        try {
          (n = this._broadcastChannel) === null || n === void 0 || n.postMessage({ type: "auth" });
        } catch (i) {
          console.error("Error posting message to broadcast channel", i);
        }
      }
    });
  }
  updateUser(t) {
    const n = { error: void 0, user: t };
    this._currentUserCached = Object.assign({ isLoading: !1 }, n), this._dataForQueryCache = {}, this.querySubs.set((r) => (Object.keys(r).forEach((i) => {
      delete r[i].result;
    }), r)), this._reconnectTimeoutMs = 0, this._transport.close(), this._oauthCallbackResponse = null, this.notifyAuthSubs(n);
  }
  sendMagicCode({ email: t }) {
    return Ru({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      email: t
    });
  }
  signInWithMagicCode(t) {
    return N(this, arguments, void 0, function* ({ email: n, code: r }) {
      var i;
      const s = yield this.getCurrentUser(), o = ((i = s == null ? void 0 : s.user) === null || i === void 0 ? void 0 : i.type) === "guest", u = yield $u({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        email: n,
        code: r,
        refreshToken: o ? s.user.refresh_token : void 0
      });
      return yield this.changeCurrentUser(u.user), u;
    });
  }
  signInWithCustomToken(t) {
    return N(this, void 0, void 0, function* () {
      const n = yield xu({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        refreshToken: t
      });
      return yield this.changeCurrentUser(n.user), n;
    });
  }
  signInAsGuest() {
    return N(this, void 0, void 0, function* () {
      const t = yield Du({
        apiURI: this.config.apiURI,
        appId: this.config.appId
      });
      return yield this.changeCurrentUser(t.user), t;
    });
  }
  potentiallyInvalidateToken(t, n) {
    var r;
    const i = (r = t == null ? void 0 : t.user) === null || r === void 0 ? void 0 : r.refresh_token;
    if (!i)
      return;
    if (n.invalidateToken === !1) {
      this._log.info("[auth-invalidate] skipped invalidateToken");
      return;
    }
    Lu({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      refreshToken: i
    }).then(() => {
      this._log.info("[auth-invalidate] completed invalidateToken");
    }).catch((o) => {
    });
  }
  signOut(t) {
    return N(this, void 0, void 0, function* () {
      const n = yield this.getCurrentUser();
      this.potentiallyInvalidateToken(n, t), yield this.changeCurrentUser(null);
    });
  }
  /**
   * Creates an OAuth authorization URL.
   *
   * @param {Object} params - The parameters to create the authorization URL.
   * @param {string} params.clientName - The name of the client requesting authorization.
   * @param {string} params.redirectURL - The URL to redirect users to after authorization.
   * @returns {string} The created authorization URL.
   */
  createAuthorizationURL({ clientName: t, redirectURL: n }) {
    const { apiURI: r, appId: i } = this.config;
    return `${r}/runtime/oauth/start?app_id=${i}&client_name=${t}&redirect_uri=${n}`;
  }
  /**
   * @param {Object} params
   * @param {string} params.code - The code received from the OAuth service.
   * @param {string} [params.codeVerifier] - The code verifier used to generate the code challenge.
   */
  exchangeCodeForToken(t) {
    return N(this, arguments, void 0, function* ({ code: n, codeVerifier: r }) {
      var i;
      const s = yield this.getCurrentUser(), o = ((i = s == null ? void 0 : s.user) === null || i === void 0 ? void 0 : i.type) === "guest", u = yield kr({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        code: n,
        codeVerifier: r,
        refreshToken: o ? s.user.refresh_token : void 0
      });
      return yield this.changeCurrentUser(u.user), u;
    });
  }
  issuerURI() {
    const { apiURI: t, appId: n } = this.config;
    return `${t}/runtime/${n}`;
  }
  /**
   * @param {Object} params
   * @param {string} params.clientName - The name of the client requesting authorization.
   * @param {string} params.idToken - The id_token from the external service
   * @param {string | null | undefined} [params.nonce] - The nonce used when requesting the id_token from the external service
   */
  signInWithIdToken(t) {
    return N(this, arguments, void 0, function* ({ idToken: n, clientName: r, nonce: i }) {
      var s;
      const o = yield this.getCurrentUser(), u = (s = o == null ? void 0 : o.user) === null || s === void 0 ? void 0 : s.refresh_token, a = yield Uu({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        idToken: n,
        clientName: r,
        nonce: i,
        refreshToken: u
      });
      return yield this.changeCurrentUser(a.user), a;
    });
  }
  // --------
  // Rooms
  /**
   * @param {string} roomId
   * @param {any | null | undefined} [initialData] -- initial presence data to send when joining the room
   * @returns () => void
   */
  joinRoom(t, n) {
    return this._rooms[t] || (this._rooms[t] = {
      isConnected: !1,
      error: void 0
    }), this._presence[t] = this._presence[t] || {}, n && (this._presence[t].result = this._presence[t].result || {}, this._presence[t].result.user = n, this._notifyPresenceSubs(t)), this._tryJoinRoom(t, n), () => {
      this._cleanupRoom(t);
    };
  }
  _cleanupRoom(t) {
    var n, r, i, s;
    if (!(!((r = (n = this._presence[t]) === null || n === void 0 ? void 0 : n.handlers) === null || r === void 0) && r.length) && !Object.keys((i = this._broadcastSubs[t]) !== null && i !== void 0 ? i : {}).length) {
      const o = (s = this._rooms[t]) === null || s === void 0 ? void 0 : s.isConnected;
      delete this._rooms[t], delete this._presence[t], delete this._broadcastSubs[t], o ? this._tryLeaveRoom(t) : this._roomsPendingLeave[t] = !0;
    }
  }
  // --------
  // Presence
  // TODO: look into typing again
  getPresence(t, n, r = {}) {
    const i = this._rooms[n], s = this._presence[n];
    return !i || !s || !s.result ? null : Object.assign(Object.assign({}, zu(s.result, r, this._sessionId)), { isLoading: !i.isConnected, error: i.error });
  }
  // TODO: look into typing again
  publishPresence(t, n, r) {
    const i = this._rooms[n], s = this._presence[n];
    if (!i || !s)
      return;
    s.result = s.result || {};
    const o = Object.assign(Object.assign({}, s.result.user), r);
    s.result.user = o, i.isConnected && (this._trySetPresence(n, o), this._notifyPresenceSubs(n));
  }
  _trySetPresence(t, n) {
    this._trySendAuthed(B(), {
      op: "set-presence",
      "room-id": t,
      data: n
    });
  }
  _tryJoinRoom(t, n) {
    this._trySendAuthed(B(), { op: "join-room", "room-id": t, data: n }), delete this._roomsPendingLeave[t];
  }
  _tryLeaveRoom(t) {
    this._trySendAuthed(B(), { op: "leave-room", "room-id": t });
  }
  // TODO: look into typing again
  subscribePresence(t, n, r, i) {
    const s = this.joinRoom(n, r.initialData), o = Object.assign(Object.assign({}, r), { roomId: n, cb: i, prev: null });
    return this._presence[n] = this._presence[n] || {}, this._presence[n].handlers = this._presence[n].handlers || [], this._presence[n].handlers.push(o), this._notifyPresenceSub(n, o), () => {
      var u, a, d;
      this._presence[n].handlers = (d = (a = (u = this._presence[n]) === null || u === void 0 ? void 0 : u.handlers) === null || a === void 0 ? void 0 : a.filter((c) => c !== o)) !== null && d !== void 0 ? d : [], s();
    };
  }
  _notifyPresenceSubs(t) {
    var n, r;
    (r = (n = this._presence[t]) === null || n === void 0 ? void 0 : n.handlers) === null || r === void 0 || r.forEach((i) => {
      this._notifyPresenceSub(t, i);
    });
  }
  _notifyPresenceSub(t, n) {
    const r = this.getPresence("", t, n);
    r && (n.prev && !Ku(r, n.prev) || (n.prev = r, n.cb(r)));
  }
  _patchPresencePeers(t, n) {
    var r, i, s;
    const o = ((i = (r = this._presence[t]) === null || r === void 0 ? void 0 : r.result) === null || i === void 0 ? void 0 : i.peers) || {};
    let u = Object.fromEntries(Object.entries(o).map(([d, c]) => [d, { data: c }]));
    (s = this._presence[t]) === null || s === void 0 || s.result;
    const a = fn(u, (d) => {
      for (let [c, f, h] of n)
        switch (f) {
          case "+":
            Es(d, c, h);
            break;
          case "r":
            gr(d, c, h);
            break;
          case "-":
            di(d, c);
            break;
        }
      delete d[this._sessionId];
    });
    this._setPresencePeers(t, a);
  }
  _setPresencePeers(t, n) {
    const r = Object.assign({}, n);
    delete r[this._sessionId];
    const i = Object.fromEntries(Object.entries(r).map(([s, o]) => [s, o.data]));
    this._presence = fn(this._presence, (s) => {
      gr(s, [t, "result", "peers"], i);
    });
  }
  // --------
  // Broadcast
  publishTopic({ roomType: t, roomId: n, topic: r, data: i }) {
    var s;
    const o = this._rooms[n];
    if (o) {
      if (!o.isConnected) {
        this._broadcastQueue[n] = (s = this._broadcastQueue[n]) !== null && s !== void 0 ? s : [], this._broadcastQueue[n].push({ topic: r, roomType: t, data: i });
        return;
      }
      this._tryBroadcast(n, t, r, i);
    }
  }
  _tryBroadcast(t, n, r, i) {
    this._trySendAuthed(B(), {
      op: "client-broadcast",
      "room-id": t,
      roomType: n,
      topic: r,
      data: i
    });
  }
  subscribeTopic(t, n, r) {
    const i = this.joinRoom(t);
    return this._broadcastSubs[t] = this._broadcastSubs[t] || {}, this._broadcastSubs[t][n] = this._broadcastSubs[t][n] || [], this._broadcastSubs[t][n].push(r), this._presence[t] = this._presence[t] || {}, () => {
      this._broadcastSubs[t][n] = this._broadcastSubs[t][n].filter((s) => s !== r), this._broadcastSubs[t][n].length || delete this._broadcastSubs[t][n], i();
    };
  }
  _notifyBroadcastSubs(t, n, r) {
    var i, s, o;
    (o = (s = (i = this._broadcastSubs) === null || i === void 0 ? void 0 : i[t]) === null || s === void 0 ? void 0 : s[n]) === null || o === void 0 || o.forEach((u) => {
      var a, d, c, f, h, p;
      const _ = (a = r.data) === null || a === void 0 ? void 0 : a.data, b = r.data["peer-id"] === this._sessionId ? (c = (d = this._presence[t]) === null || d === void 0 ? void 0 : d.result) === null || c === void 0 ? void 0 : c.user : (p = (h = (f = this._presence[t]) === null || f === void 0 ? void 0 : f.result) === null || h === void 0 ? void 0 : h.peers) === null || p === void 0 ? void 0 : p[r.data["peer-id"]];
      return u(_, b);
    });
  }
  // --------
  // Storage
  uploadFile(t, n, r) {
    return N(this, void 0, void 0, function* () {
      var i;
      const s = yield this.getCurrentUser(), o = (i = s == null ? void 0 : s.user) === null || i === void 0 ? void 0 : i.refresh_token;
      return Nu(Object.assign(Object.assign({}, r), { apiURI: this.config.apiURI, appId: this.config.appId, path: t, file: n, refreshToken: o }));
    });
  }
  deleteFile(t) {
    return N(this, void 0, void 0, function* () {
      var n;
      const r = yield this.getCurrentUser(), i = (n = r == null ? void 0 : r.user) === null || n === void 0 ? void 0 : n.refresh_token;
      return yield Fu({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        path: t,
        refreshToken: i
      });
    });
  }
  // Deprecated Storage API (Jan 2025)
  // ---------------------------------
  upload(t, n) {
    return N(this, void 0, void 0, function* () {
      var r;
      const i = yield this.getCurrentUser(), s = (r = i == null ? void 0 : i.user) === null || r === void 0 ? void 0 : r.refresh_token, o = t || n.name, u = yield Wu({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        fileName: o,
        refreshToken: s
      });
      return yield Vu(u, n);
    });
  }
  getDownloadUrl(t) {
    return N(this, void 0, void 0, function* () {
      var n;
      const r = yield this.getCurrentUser(), i = (n = r == null ? void 0 : r.user) === null || n === void 0 ? void 0 : n.refresh_token;
      return yield qu({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        path: t,
        refreshToken: i
      });
    });
  }
}
function pa(e, t) {
  return new Ft(
    Ui(e, t),
    // (XXX): LinksDef<any> stems from TypeScript’s inability to reconcile the
    // type EntitiesWithLinks<EntitiesWithoutLinks, Links> with
    // EntitiesWithoutLinks. TypeScript is strict about ensuring that types are
    // correctly aligned and does not allow for substituting a type that might
    // be broader or have additional properties.
    t,
    void 0
  );
}
function ya(e) {
  return new Nt(e, {});
}
function ba() {
  return new Y("string", !0, !1);
}
function _a() {
  return new Y("number", !0, !1);
}
function ga() {
  return new Y("boolean", !0, !1);
}
function Ta() {
  return new Y("date", !0, !1);
}
function wa() {
  return new Y("json", !0, !1);
}
function ma() {
  return new Y("json", !0, !1);
}
function Ui(e, t) {
  var n, r, i, s;
  const o = { fwd: {}, rev: {} };
  for (const a of Object.values(t))
    (n = o.fwd)[r = a.forward.on] || (n[r] = {}), (i = o.rev)[s = a.reverse.on] || (i[s] = {}), o.fwd[a.forward.on][a.forward.label] = {
      entityName: a.reverse.on,
      cardinality: a.forward.has
    }, o.rev[a.reverse.on][a.reverse.label] = {
      entityName: a.forward.on,
      cardinality: a.reverse.has
    };
  return Object.fromEntries(Object.entries(e).map(([a, d]) => [
    a,
    new Nt(d.attrs, Object.assign(Object.assign({}, o.fwd[a]), o.rev[a]))
  ]));
}
function va({ entities: e, links: t, rooms: n }) {
  const r = t ?? {}, i = n ?? {};
  return new Ft(
    Ui(e, r),
    // (XXX): LinksDef<any> stems from TypeScript's inability to reconcile the
    // type EntitiesWithLinks<EntitiesWithoutLinks, Links> with
    // EntitiesWithoutLinks. TypeScript is strict about ensuring that types are
    // correctly aligned and does not allow for substituting a type that might
    // be broader or have additional properties.
    r,
    i
  );
}
const hc = {
  // constructs
  graph: pa,
  schema: va,
  entity: ya,
  // value types
  string: ba,
  number: _a,
  boolean: ga,
  date: Ta,
  json: wa,
  any: ma
};
let St;
function Sa(e, t) {
  St == null || St.dispose();
  const n = ka(t), r = Aa(t, u), i = Ea(Oa(e));
  function s(c) {
    var f;
    c.source === i.element.contentWindow && ((f = c.data) === null || f === void 0 ? void 0 : f.type) === "close" && n.isVisible() && u();
  }
  function o(c) {
    const f = c.shiftKey && c.ctrlKey && c.key === "0", h = c.key === "Escape" || c.key === "Esc";
    (f || h && n.isVisible()) && u();
  }
  function u() {
    n.isVisible() ? n.element.style.display = "none" : (n.element.style.display = "block", n.element.contains(i.element) || n.element.appendChild(i.element));
  }
  function a() {
    n.element.remove(), r.element.remove(), removeEventListener("keydown", o), removeEventListener("message", s);
  }
  function d() {
    document.body.appendChild(n.element), document.body.appendChild(r.element), addEventListener("keydown", o), addEventListener("message", s), St = {
      dispose: a
    };
  }
  return d();
}
function Oa(e) {
  return `${Qn || ki ? "http://localhost:3000" : "https://instantdb.com"}/_devtool?appId=${e}`;
}
function Ea(e) {
  const t = document.createElement("iframe");
  return t.src = e, t.className = "instant-devtool-iframe", Object.assign(t.style, {
    width: "100%",
    height: "100%",
    backgroundColor: "white",
    border: "none"
  }), { element: t };
}
function Aa(e, t) {
  const n = `
    <svg width="32" height="32" viewBox="0 0 512 512" fill="none" xmlns="http://www.w3.org/2000/svg">
      <rect width="512" height="512" fill="black"/>
      <rect x="97.0973" y="91.3297" width="140" height="330" fill="white"/>
    </svg>
  `, r = document.createElement("button");
  return r.innerHTML = n, r.className = "instant-devtool-toggler", Object.assign(r.style, Object.assign(Object.assign({
    // pos
    position: "fixed"
  }, Ca(e.position)), {
    height: "32px",
    width: "32px",
    // layout
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    zIndex: "9010",
    // look
    padding: "0",
    margin: "0",
    border: "none",
    cursor: "pointer"
  })), r.addEventListener("click", t), { element: r };
}
function Ca(e) {
  switch (e) {
    case "bottom-left":
      return { bottom: "24px", left: "24px" };
    case "bottom-right":
      return { bottom: "24px", right: "24px" };
    case "top-right":
      return { top: "24px", right: "24px" };
    case "top-left":
      return { top: "24px", left: "24px" };
  }
}
function ja(e) {
  switch (e) {
    case "bottom-left":
      return { bottom: "24px", right: "24px", left: "60px", top: "72px" };
    case "bottom-right":
      return { bottom: "24px", left: "24px", right: "60px", top: "72px" };
    case "top-right":
      return { top: "24px", left: "24px", right: "60px", bottom: "72px" };
    case "top-left":
      return { top: "24px", right: "24px", left: "60px", bottom: "72px" };
  }
}
function ka(e) {
  const t = document.createElement("div");
  Object.assign(t.style, Object.assign(Object.assign({ position: "fixed" }, ja(e.position)), { display: "block", borderRadius: "4px", border: "1px #ccc solid", boxShadow: "0px 0px 8px #00000044", backgroundColor: "#eee", zIndex: "999990" })), t.style.display = "none", t.className = "instant-devtool-container";
  function n() {
    return t.style.display !== "none";
  }
  return { element: t, isVisible: n };
}
const Pa = {
  apiURI: "https://api.instantdb.com",
  websocketURI: "wss://api.instantdb.com/runtime/session"
};
function Ia() {
  var e;
  return globalThis.__instantDbSchemaHashStore = (e = globalThis.__instantDbSchemaHashStore) !== null && e !== void 0 ? e : /* @__PURE__ */ new WeakMap(), globalThis.__instantDbSchemaHashStore;
}
function Ma() {
  var e;
  return globalThis.__instantDbStore = (e = globalThis.__instantDbStore) !== null && e !== void 0 ? e : {}, globalThis.__instantDbStore;
}
function Sn(e) {
  const t = e.__adminToken;
  return e.appId + "_" + (e.websocketURI || "default_ws_uri") + "_" + (e.apiURI || "default_api_uri") + "_" + (t || "client_only") + "_" + e.useDateObjects;
}
const On = Ma(), tn = Ia();
class Ra {
  constructor(t) {
    this.db = t, this.sendMagicCode = (n) => this.db.sendMagicCode(n), this.signInWithMagicCode = (n) => this.db.signInWithMagicCode(n), this.signInWithToken = (n) => this.db.signInWithCustomToken(n), this.signInAsGuest = () => this.db.signInAsGuest(), this.createAuthorizationURL = (n) => this.db.createAuthorizationURL(n), this.signInWithIdToken = (n) => this.db.signInWithIdToken(n), this.exchangeOAuthCode = (n) => this.db.exchangeCodeForToken(n), this.issuerURI = () => this.db.issuerURI(), this.signOut = (n = { invalidateToken: !0 }) => this.db.signOut(n);
  }
}
class $a {
  constructor(t) {
    this.db = t, this.uploadFile = (n, r, i = {}) => this.db.uploadFile(n, r, i), this.delete = (n) => this.db.deleteFile(n), this.upload = (n, r) => this.db.upload(n, r), this.put = this.upload, this.getDownloadUrl = (n) => this.db.getDownloadUrl(n);
  }
}
function xa(e) {
  return JSON.parse(JSON.stringify(e));
}
class Da {
  constructor(t) {
    this.tx = Gn(), this._reactor = t, this.auth = new Ra(this._reactor), this.storage = new $a(this._reactor);
  }
  /**
   * Use this to write data! You can create, update, delete, and link objects
   *
   * @see https://instantdb.com/docs/instaml
   *
   * @example
   *   // Create a new object in the `goals` namespace
   *   const goalId = id();
   *   db.transact(db.tx.goals[goalId].update({title: "Get fit"}))
   *
   *   // Update the title
   *   db.transact(db.tx.goals[goalId].update({title: "Get super fit"}))
   *
   *   // Delete it
   *   db.transact(db.tx.goals[goalId].delete())
   *
   *   // Or create an association:
   *   todoId = id();
   *   db.transact([
   *    db.tx.todos[todoId].update({ title: 'Go on a run' }),
   *    db.tx.goals[goalId].link({todos: todoId}),
   *  ])
   */
  transact(t) {
    return this._reactor.pushTx(t);
  }
  getLocalId(t) {
    return this._reactor.getLocalId(t);
  }
  /**
   * Use this to query your data!
   *
   * @see https://instantdb.com/docs/instaql
   *
   * @example
   *  // listen to all goals
   *  db.subscribeQuery({ goals: {} }, (resp) => {
   *    console.log(resp.data.goals)
   *  })
   *
   *  // goals where the title is "Get Fit"
   *  db.subscribeQuery(
   *    { goals: { $: { where: { title: "Get Fit" } } } },
   *    (resp) => {
   *      console.log(resp.data.goals)
   *    }
   *  )
   *
   *  // all goals, _alongside_ their todos
   *  db.subscribeQuery({ goals: { todos: {} } }, (resp) => {
   *    console.log(resp.data.goals)
   *  });
   */
  subscribeQuery(t, n, r) {
    return this._reactor.subscribeQuery(t, n, r);
  }
  /**
   * Listen for the logged in state. This is useful
   * for deciding when to show a login screen.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const unsub = db.subscribeAuth((auth) => {
   *     if (auth.user) {
   *     console.log('logged in as', auth.user.email)
   *    } else {
   *      console.log('logged out')
   *    }
   *  })
   */
  subscribeAuth(t) {
    return this._reactor.subscribeAuth(t);
  }
  /**
   * One time query for the logged in state. This is useful
   * for scenarios where you want to know the current auth
   * state without subscribing to changes.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const user = await db.getAuth();
   *   console.log('logged in as', user.email)
   */
  getAuth() {
    return this._reactor.getAuth();
  }
  /**
   * Listen for connection status changes to Instant. This is useful
   * for building things like connectivity indicators
   *
   * @see https://www.instantdb.com/docs/patterns#connection-status
   * @example
   *   const unsub = db.subscribeConnectionStatus((status) => {
   *     const connectionState =
   *       status === 'connecting' || status === 'opened'
   *         ? 'authenticating'
   *       : status === 'authenticated'
   *         ? 'connected'
   *       : status === 'closed'
   *         ? 'closed'
   *       : status === 'errored'
   *         ? 'errored'
   *       : 'unexpected state';
   *
   *     console.log('Connection status:', connectionState);
   *   });
   */
  subscribeConnectionStatus(t) {
    return this._reactor.subscribeConnectionStatus(t);
  }
  /**
   * Join a room to publish and subscribe to topics and presence.
   *
   * @see https://instantdb.com/docs/presence-and-topics
   * @example
   * // init
   * const db = init();
   * const room = db.joinRoom(roomType, roomId);
   * // usage
   * const unsubscribeTopic = room.subscribeTopic("foo", console.log);
   * const unsubscribePresence = room.subscribePresence({}, console.log);
   * room.publishTopic("hello", { message: "hello world!" });
   * room.publishPresence({ name: "joe" });
   * // later
   * unsubscribePresence();
   * unsubscribeTopic();
   * room.leaveRoom();
   */
  joinRoom(t = "_defaultRoomType", n = "_defaultRoomId", r) {
    return {
      leaveRoom: this._reactor.joinRoom(n, r == null ? void 0 : r.initialPresence),
      subscribeTopic: (s, o) => this._reactor.subscribeTopic(n, s, o),
      subscribePresence: (s, o) => this._reactor.subscribePresence(t, n, s, o),
      publishTopic: (s, o) => this._reactor.publishTopic({ roomType: t, roomId: n, topic: s, data: o }),
      publishPresence: (s) => this._reactor.publishPresence(t, n, s),
      getPresence: (s) => this._reactor.getPresence(t, n, s)
    };
  }
  shutdown() {
    delete On[Sn(this._reactor.config)], this._reactor.shutdown();
  }
  /**
   * Use this for one-off queries.
   * Returns local data if available, otherwise fetches from the server.
   * Because we want to avoid stale data, this method will throw an error
   * if the user is offline or there is no active connection to the server.
   *
   * @see https://instantdb.com/docs/instaql
   *
   * @example
   *
   *  const resp = await db.queryOnce({ goals: {} });
   *  console.log(resp.data.goals)
   */
  queryOnce(t, n) {
    return this._reactor.queryOnce(t, n);
  }
}
function zr(e) {
  if (!e)
    return "0";
  if (tn.get(e))
    return tn.get(e);
  const t = X(e);
  return tn.set(e, t), t;
}
function Ua(e, t) {
  return zr(e._reactor.config.schema) !== zr(t);
}
function La(e, t, n, r, i) {
  const s = On[Sn(e)];
  if (s)
    return Ua(s, e.schema) && s._reactor.updateSchema(e.schema), s;
  const o = new ha(Object.assign(Object.assign(Object.assign({}, Pa), e), { cardinalityInference: !!e.schema }), t || Ai, n || Ci, Object.assign(Object.assign({}, r || {}), { "@instantdb/core": Yn }), i), u = new Da(o);
  return On[Sn(e)] = u, Na(e.appId, e.devtool), u;
}
function Na(e, t) {
  if (typeof window > "u" || typeof window.location > "u" || typeof document > "u" || typeof t == "boolean" && !t)
    return;
  const n = Object.assign({ position: "bottom-right", allowedHosts: ["localhost"] }, typeof t == "object" ? t : {});
  n.allowedHosts.includes(window.location.hostname) && Sa(e, n);
}
var En = { exports: {} }, He = {};
/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var Kr;
function Fa() {
  if (Kr) return He;
  Kr = 1;
  var e = Xr, t = Symbol.for("react.element"), n = Symbol.for("react.fragment"), r = Object.prototype.hasOwnProperty, i = e.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner, s = { key: !0, ref: !0, __self: !0, __source: !0 };
  function o(u, a, d) {
    var c, f = {}, h = null, p = null;
    d !== void 0 && (h = "" + d), a.key !== void 0 && (h = "" + a.key), a.ref !== void 0 && (p = a.ref);
    for (c in a) r.call(a, c) && !s.hasOwnProperty(c) && (f[c] = a[c]);
    if (u && u.defaultProps) for (c in a = u.defaultProps, a) f[c] === void 0 && (f[c] = a[c]);
    return { $$typeof: t, type: u, key: h, ref: p, props: f, _owner: i.current };
  }
  return He.Fragment = n, He.jsx = o, He.jsxs = o, He;
}
var Je = {}, Br;
function Wa() {
  if (Br) return Je;
  Br = 1;
  var e = {};
  /**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   */
  return e.NODE_ENV !== "production" && function() {
    var t = Xr, n = Symbol.for("react.element"), r = Symbol.for("react.portal"), i = Symbol.for("react.fragment"), s = Symbol.for("react.strict_mode"), o = Symbol.for("react.profiler"), u = Symbol.for("react.provider"), a = Symbol.for("react.context"), d = Symbol.for("react.forward_ref"), c = Symbol.for("react.suspense"), f = Symbol.for("react.suspense_list"), h = Symbol.for("react.memo"), p = Symbol.for("react.lazy"), _ = Symbol.for("react.offscreen"), b = Symbol.iterator, g = "@@iterator";
    function T(l) {
      if (l === null || typeof l != "object")
        return null;
      var y = b && l[b] || l[g];
      return typeof y == "function" ? y : null;
    }
    var w = t.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    function v(l) {
      {
        for (var y = arguments.length, m = new Array(y > 1 ? y - 1 : 0), S = 1; S < y; S++)
          m[S - 1] = arguments[S];
        I("error", l, m);
      }
    }
    function I(l, y, m) {
      {
        var S = w.ReactDebugCurrentFrame, k = S.getStackAddendum();
        k !== "" && (y += "%s", m = m.concat([k]));
        var P = m.map(function(j) {
          return String(j);
        });
        P.unshift("Warning: " + y), Function.prototype.apply.call(console[l], console, P);
      }
    }
    var O = !1, U = !1, W = !1, _e = !1, ye = !1, Ge;
    Ge = Symbol.for("react.module.reference");
    function dt(l) {
      return !!(typeof l == "string" || typeof l == "function" || l === i || l === o || ye || l === s || l === c || l === f || _e || l === _ || O || U || W || typeof l == "object" && l !== null && (l.$$typeof === p || l.$$typeof === h || l.$$typeof === u || l.$$typeof === a || l.$$typeof === d || // This needs to include all possible module reference object
      // types supported by any Flight configuration anywhere since
      // we don't know which Flight build this will end up being used
      // with.
      l.$$typeof === Ge || l.getModuleId !== void 0));
    }
    function ft(l, y, m) {
      var S = l.displayName;
      if (S)
        return S;
      var k = y.displayName || y.name || "";
      return k !== "" ? m + "(" + k + ")" : m;
    }
    function M(l) {
      return l.displayName || "Context";
    }
    function V(l) {
      if (l == null)
        return null;
      if (typeof l.tag == "number" && v("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), typeof l == "function")
        return l.displayName || l.name || null;
      if (typeof l == "string")
        return l;
      switch (l) {
        case i:
          return "Fragment";
        case r:
          return "Portal";
        case o:
          return "Profiler";
        case s:
          return "StrictMode";
        case c:
          return "Suspense";
        case f:
          return "SuspenseList";
      }
      if (typeof l == "object")
        switch (l.$$typeof) {
          case a:
            var y = l;
            return M(y) + ".Consumer";
          case u:
            var m = l;
            return M(m._context) + ".Provider";
          case d:
            return ft(l, l.render, "ForwardRef");
          case h:
            var S = l.displayName || null;
            return S !== null ? S : V(l.type) || "Memo";
          case p: {
            var k = l, P = k._payload, j = k._init;
            try {
              return V(j(P));
            } catch {
              return null;
            }
          }
        }
      return null;
    }
    var H = Object.assign, J = 0, ce, de, ie, lt, ht, pt, yt;
    function bt() {
    }
    bt.__reactDisabledLog = !0;
    function Wt() {
      {
        if (J === 0) {
          ce = console.log, de = console.info, ie = console.warn, lt = console.error, ht = console.group, pt = console.groupCollapsed, yt = console.groupEnd;
          var l = {
            configurable: !0,
            enumerable: !0,
            value: bt,
            writable: !0
          };
          Object.defineProperties(console, {
            info: l,
            log: l,
            warn: l,
            error: l,
            group: l,
            groupCollapsed: l,
            groupEnd: l
          });
        }
        J++;
      }
    }
    function Ni() {
      {
        if (J--, J === 0) {
          var l = {
            configurable: !0,
            enumerable: !0,
            writable: !0
          };
          Object.defineProperties(console, {
            log: H({}, l, {
              value: ce
            }),
            info: H({}, l, {
              value: de
            }),
            warn: H({}, l, {
              value: ie
            }),
            error: H({}, l, {
              value: lt
            }),
            group: H({}, l, {
              value: ht
            }),
            groupCollapsed: H({}, l, {
              value: pt
            }),
            groupEnd: H({}, l, {
              value: yt
            })
          });
        }
        J < 0 && v("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
      }
    }
    var Vt = w.ReactCurrentDispatcher, qt;
    function _t(l, y, m) {
      {
        if (qt === void 0)
          try {
            throw Error();
          } catch (k) {
            var S = k.stack.trim().match(/\n( *(at )?)/);
            qt = S && S[1] || "";
          }
        return `
` + qt + l;
      }
    }
    var zt = !1, gt;
    {
      var Fi = typeof WeakMap == "function" ? WeakMap : Map;
      gt = new Fi();
    }
    function Zn(l, y) {
      if (!l || zt)
        return "";
      {
        var m = gt.get(l);
        if (m !== void 0)
          return m;
      }
      var S;
      zt = !0;
      var k = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      var P;
      P = Vt.current, Vt.current = null, Wt();
      try {
        if (y) {
          var j = function() {
            throw Error();
          };
          if (Object.defineProperty(j.prototype, "props", {
            set: function() {
              throw Error();
            }
          }), typeof Reflect == "object" && Reflect.construct) {
            try {
              Reflect.construct(j, []);
            } catch (Z) {
              S = Z;
            }
            Reflect.construct(l, [], j);
          } else {
            try {
              j.call();
            } catch (Z) {
              S = Z;
            }
            l.call(j.prototype);
          }
        } else {
          try {
            throw Error();
          } catch (Z) {
            S = Z;
          }
          l();
        }
      } catch (Z) {
        if (Z && S && typeof Z.stack == "string") {
          for (var A = Z.stack.split(`
`), G = S.stack.split(`
`), x = A.length - 1, L = G.length - 1; x >= 1 && L >= 0 && A[x] !== G[L]; )
            L--;
          for (; x >= 1 && L >= 0; x--, L--)
            if (A[x] !== G[L]) {
              if (x !== 1 || L !== 1)
                do
                  if (x--, L--, L < 0 || A[x] !== G[L]) {
                    var ue = `
` + A[x].replace(" at new ", " at ");
                    return l.displayName && ue.includes("<anonymous>") && (ue = ue.replace("<anonymous>", l.displayName)), typeof l == "function" && gt.set(l, ue), ue;
                  }
                while (x >= 1 && L >= 0);
              break;
            }
        }
      } finally {
        zt = !1, Vt.current = P, Ni(), Error.prepareStackTrace = k;
      }
      var Re = l ? l.displayName || l.name : "", Oe = Re ? _t(Re) : "";
      return typeof l == "function" && gt.set(l, Oe), Oe;
    }
    function Wi(l, y, m) {
      return Zn(l, !1);
    }
    function Vi(l) {
      var y = l.prototype;
      return !!(y && y.isReactComponent);
    }
    function Tt(l, y, m) {
      if (l == null)
        return "";
      if (typeof l == "function")
        return Zn(l, Vi(l));
      if (typeof l == "string")
        return _t(l);
      switch (l) {
        case c:
          return _t("Suspense");
        case f:
          return _t("SuspenseList");
      }
      if (typeof l == "object")
        switch (l.$$typeof) {
          case d:
            return Wi(l.render);
          case h:
            return Tt(l.type, y, m);
          case p: {
            var S = l, k = S._payload, P = S._init;
            try {
              return Tt(P(k), y, m);
            } catch {
            }
          }
        }
      return "";
    }
    var Qe = Object.prototype.hasOwnProperty, Xn = {}, er = w.ReactDebugCurrentFrame;
    function wt(l) {
      if (l) {
        var y = l._owner, m = Tt(l.type, l._source, y ? y.type : null);
        er.setExtraStackFrame(m);
      } else
        er.setExtraStackFrame(null);
    }
    function qi(l, y, m, S, k) {
      {
        var P = Function.call.bind(Qe);
        for (var j in l)
          if (P(l, j)) {
            var A = void 0;
            try {
              if (typeof l[j] != "function") {
                var G = Error((S || "React class") + ": " + m + " type `" + j + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof l[j] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                throw G.name = "Invariant Violation", G;
              }
              A = l[j](y, j, S, m, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
            } catch (x) {
              A = x;
            }
            A && !(A instanceof Error) && (wt(k), v("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", S || "React class", m, j, typeof A), wt(null)), A instanceof Error && !(A.message in Xn) && (Xn[A.message] = !0, wt(k), v("Failed %s type: %s", m, A.message), wt(null));
          }
      }
    }
    var zi = Array.isArray;
    function Kt(l) {
      return zi(l);
    }
    function Ki(l) {
      {
        var y = typeof Symbol == "function" && Symbol.toStringTag, m = y && l[Symbol.toStringTag] || l.constructor.name || "Object";
        return m;
      }
    }
    function Bi(l) {
      try {
        return tr(l), !1;
      } catch {
        return !0;
      }
    }
    function tr(l) {
      return "" + l;
    }
    function nr(l) {
      if (Bi(l))
        return v("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", Ki(l)), tr(l);
    }
    var rr = w.ReactCurrentOwner, Gi = {
      key: !0,
      ref: !0,
      __self: !0,
      __source: !0
    }, ir, sr;
    function Qi(l) {
      if (Qe.call(l, "ref")) {
        var y = Object.getOwnPropertyDescriptor(l, "ref").get;
        if (y && y.isReactWarning)
          return !1;
      }
      return l.ref !== void 0;
    }
    function Yi(l) {
      if (Qe.call(l, "key")) {
        var y = Object.getOwnPropertyDescriptor(l, "key").get;
        if (y && y.isReactWarning)
          return !1;
      }
      return l.key !== void 0;
    }
    function Hi(l, y) {
      typeof l.ref == "string" && rr.current;
    }
    function Ji(l, y) {
      {
        var m = function() {
          ir || (ir = !0, v("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", y));
        };
        m.isReactWarning = !0, Object.defineProperty(l, "key", {
          get: m,
          configurable: !0
        });
      }
    }
    function Zi(l, y) {
      {
        var m = function() {
          sr || (sr = !0, v("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", y));
        };
        m.isReactWarning = !0, Object.defineProperty(l, "ref", {
          get: m,
          configurable: !0
        });
      }
    }
    var Xi = function(l, y, m, S, k, P, j) {
      var A = {
        // This tag allows us to uniquely identify this as a React Element
        $$typeof: n,
        // Built-in properties that belong on the element
        type: l,
        key: y,
        ref: m,
        props: j,
        // Record the component responsible for creating this element.
        _owner: P
      };
      return A._store = {}, Object.defineProperty(A._store, "validated", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: !1
      }), Object.defineProperty(A, "_self", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: S
      }), Object.defineProperty(A, "_source", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: k
      }), Object.freeze && (Object.freeze(A.props), Object.freeze(A)), A;
    };
    function es(l, y, m, S, k) {
      {
        var P, j = {}, A = null, G = null;
        m !== void 0 && (nr(m), A = "" + m), Yi(y) && (nr(y.key), A = "" + y.key), Qi(y) && (G = y.ref, Hi(y, k));
        for (P in y)
          Qe.call(y, P) && !Gi.hasOwnProperty(P) && (j[P] = y[P]);
        if (l && l.defaultProps) {
          var x = l.defaultProps;
          for (P in x)
            j[P] === void 0 && (j[P] = x[P]);
        }
        if (A || G) {
          var L = typeof l == "function" ? l.displayName || l.name || "Unknown" : l;
          A && Ji(j, L), G && Zi(j, L);
        }
        return Xi(l, A, G, k, S, rr.current, j);
      }
    }
    var Bt = w.ReactCurrentOwner, or = w.ReactDebugCurrentFrame;
    function Me(l) {
      if (l) {
        var y = l._owner, m = Tt(l.type, l._source, y ? y.type : null);
        or.setExtraStackFrame(m);
      } else
        or.setExtraStackFrame(null);
    }
    var Gt;
    Gt = !1;
    function Qt(l) {
      return typeof l == "object" && l !== null && l.$$typeof === n;
    }
    function ur() {
      {
        if (Bt.current) {
          var l = V(Bt.current.type);
          if (l)
            return `

Check the render method of \`` + l + "`.";
        }
        return "";
      }
    }
    function ts(l) {
      return "";
    }
    var ar = {};
    function ns(l) {
      {
        var y = ur();
        if (!y) {
          var m = typeof l == "string" ? l : l.displayName || l.name;
          m && (y = `

Check the top-level render call using <` + m + ">.");
        }
        return y;
      }
    }
    function cr(l, y) {
      {
        if (!l._store || l._store.validated || l.key != null)
          return;
        l._store.validated = !0;
        var m = ns(y);
        if (ar[m])
          return;
        ar[m] = !0;
        var S = "";
        l && l._owner && l._owner !== Bt.current && (S = " It was passed a child from " + V(l._owner.type) + "."), Me(l), v('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', m, S), Me(null);
      }
    }
    function dr(l, y) {
      {
        if (typeof l != "object")
          return;
        if (Kt(l))
          for (var m = 0; m < l.length; m++) {
            var S = l[m];
            Qt(S) && cr(S, y);
          }
        else if (Qt(l))
          l._store && (l._store.validated = !0);
        else if (l) {
          var k = T(l);
          if (typeof k == "function" && k !== l.entries)
            for (var P = k.call(l), j; !(j = P.next()).done; )
              Qt(j.value) && cr(j.value, y);
        }
      }
    }
    function rs(l) {
      {
        var y = l.type;
        if (y == null || typeof y == "string")
          return;
        var m;
        if (typeof y == "function")
          m = y.propTypes;
        else if (typeof y == "object" && (y.$$typeof === d || // Note: Memo only checks outer props here.
        // Inner props are checked in the reconciler.
        y.$$typeof === h))
          m = y.propTypes;
        else
          return;
        if (m) {
          var S = V(y);
          qi(m, l.props, "prop", S, l);
        } else if (y.PropTypes !== void 0 && !Gt) {
          Gt = !0;
          var k = V(y);
          v("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", k || "Unknown");
        }
        typeof y.getDefaultProps == "function" && !y.getDefaultProps.isReactClassApproved && v("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
      }
    }
    function is(l) {
      {
        for (var y = Object.keys(l.props), m = 0; m < y.length; m++) {
          var S = y[m];
          if (S !== "children" && S !== "key") {
            Me(l), v("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", S), Me(null);
            break;
          }
        }
        l.ref !== null && (Me(l), v("Invalid attribute `ref` supplied to `React.Fragment`."), Me(null));
      }
    }
    var fr = {};
    function lr(l, y, m, S, k, P) {
      {
        var j = dt(l);
        if (!j) {
          var A = "";
          (l === void 0 || typeof l == "object" && l !== null && Object.keys(l).length === 0) && (A += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.");
          var G = ts();
          G ? A += G : A += ur();
          var x;
          l === null ? x = "null" : Kt(l) ? x = "array" : l !== void 0 && l.$$typeof === n ? (x = "<" + (V(l.type) || "Unknown") + " />", A = " Did you accidentally export a JSX literal instead of a component?") : x = typeof l, v("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", x, A);
        }
        var L = es(l, y, m, k, P);
        if (L == null)
          return L;
        if (j) {
          var ue = y.children;
          if (ue !== void 0)
            if (S)
              if (Kt(ue)) {
                for (var Re = 0; Re < ue.length; Re++)
                  dr(ue[Re], l);
                Object.freeze && Object.freeze(ue);
              } else
                v("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
            else
              dr(ue, l);
        }
        if (Qe.call(y, "key")) {
          var Oe = V(l), Z = Object.keys(y).filter(function(ds) {
            return ds !== "key";
          }), Yt = Z.length > 0 ? "{key: someKey, " + Z.join(": ..., ") + ": ...}" : "{key: someKey}";
          if (!fr[Oe + Yt]) {
            var cs = Z.length > 0 ? "{" + Z.join(": ..., ") + ": ...}" : "{}";
            v(`A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`, Yt, Oe, cs, Oe), fr[Oe + Yt] = !0;
          }
        }
        return l === i ? is(L) : rs(L), L;
      }
    }
    function ss(l, y, m) {
      return lr(l, y, m, !0);
    }
    function os(l, y, m) {
      return lr(l, y, m, !1);
    }
    var us = os, as = ss;
    Je.Fragment = i, Je.jsx = us, Je.jsxs = as;
  }(), Je;
}
var Va = {};
Va.NODE_ENV === "production" ? En.exports = Fa() : En.exports = Wa();
var z = En.exports;
const qa = {
  isLoading: !0,
  data: void 0,
  pageInfo: void 0,
  error: void 0
};
function Gr(e) {
  return Object.assign({ isLoading: !e, data: void 0, pageInfo: void 0, error: void 0 }, e || {});
}
function za(e, t, n) {
  t && n && "ruleParams" in n && (t = Object.assign({ $$ruleParams: n.ruleParams }, t));
  const r = t ? xa(t) : null, i = X(r), s = kt(Gr(e._reactor.getPreviousResult(r))), o = Se(
    (a) => (s.current = Gr(e._reactor.getPreviousResult(r)), r ? e.subscribeQuery(r, (c) => {
      s.current = Object.assign({ isLoading: !c, data: void 0, pageInfo: void 0, error: void 0 }, c), a();
    }) : () => {
    }),
    // Build a new subscribe function if the query changes
    [i]
  );
  return { state: rn(o, () => s.current, () => qa), query: r };
}
function Ka() {
  const e = kt(null);
  ke(() => {
    n();
  }, []);
  function t(r, i) {
    clearTimeout(e.current), e.current = setTimeout(i, r);
  }
  function n() {
    clearTimeout(e.current);
  }
  return { set: t, clear: n };
}
const Ba = 1e3;
function Ga(e, t, n) {
  ke(() => e.core._reactor.subscribeTopic(e.id, t, (i, s) => {
    n(i, s);
  }), [e.id, t]);
}
function Qa(e, t) {
  return ke(() => e.core._reactor.joinRoom(e.id), [e.id]), Se((r) => {
    e.core._reactor.publishTopic({
      roomType: e.type,
      roomId: e.id,
      topic: t,
      data: r
    });
  }, [e.id, t]);
}
function Ya(e, t = {}) {
  var n, r;
  const [i, s] = ei(() => {
    var a;
    return (a = e.core._reactor.getPresence(e.type, e.id, t)) !== null && a !== void 0 ? a : {
      peers: {},
      isLoading: !0
    };
  });
  ke(() => e.core._reactor.subscribePresence(e.type, e.id, t, (d) => {
    s(d);
  }), [e.id, t.user, (n = t.peers) === null || n === void 0 ? void 0 : n.join(), (r = t.keys) === null || r === void 0 ? void 0 : r.join()]);
  const o = Se((a) => {
    e.core._reactor.publishPresence(e.type, e.id, a);
  }, [e.type, e.id]);
  return sn(() => Object.assign(Object.assign({}, i), { publishPresence: o }), [i, o]);
}
function Ha(e, t, n) {
  ke(() => e.core._reactor.joinRoom(e.id, t), [e.id]), ke(() => e.core._reactor.publishPresence(e.type, e.id, t), [e.type, e.id, n ?? JSON.stringify(t)]);
}
function Ja(e, t, n = {}) {
  const r = Ka(), i = Ee.usePresence(e, {
    keys: [t]
  }), s = sn(() => {
    var c;
    const f = e._core._reactor.getPresence(e.type, e.id);
    return n != null && n.writeOnly ? [] : Object.values((c = f == null ? void 0 : f.peers) !== null && c !== void 0 ? c : {}).filter((h) => h[t] === !0);
  }, [n == null ? void 0 : n.writeOnly, i]), o = Se((c) => {
    var f;
    e.core._reactor.publishPresence(e.type, e.id, {
      [t]: c
    }), c && ((n == null ? void 0 : n.timeout) === null || (n == null ? void 0 : n.timeout) === 0 || r.set((f = n == null ? void 0 : n.timeout) !== null && f !== void 0 ? f : Ba, () => {
      e.core._reactor.publishPresence(e.type, e.id, {
        [t]: null
      });
    }));
  }, [e.type, e.id, t, n == null ? void 0 : n.timeout, r]), u = Se((c) => {
    const h = !((n == null ? void 0 : n.stopOnEnter) && c.key === "Enter");
    o(h);
  }, [n.stopOnEnter, o]), a = Se(() => {
    o(!1);
  }, [o]), d = sn(() => ({ onKeyDown: u, onBlur: a }), [u, a]);
  return {
    active: s,
    setActive: o,
    inputProps: d
  };
}
const Ee = {
  useTopicEffect: Ga,
  usePublishTopic: Qa,
  usePresence: Ya,
  useSyncPresence: Ha,
  useTypingIndicator: Ja
};
class Za {
  constructor(t, n, r) {
    this.useTopicEffect = (i, s) => {
      Ee.useTopicEffect(this, i, s);
    }, this.usePublishTopic = (i) => Ee.usePublishTopic(this, i), this.usePresence = (i = {}) => Ee.usePresence(this, i), this.useSyncPresence = (i, s) => Ee.useSyncPresence(this, i, s), this.useTypingIndicator = (i, s = {}) => Ee.useTypingIndicator(this, i, s), this.core = t, this._core = this.core, this.type = n, this.id = r;
  }
}
var Xa = function(e, t, n, r) {
  function i(s) {
    return s instanceof n ? s : new n(function(o) {
      o(s);
    });
  }
  return new (n || (n = Promise))(function(s, o) {
    function u(c) {
      try {
        d(r.next(c));
      } catch (f) {
        o(f);
      }
    }
    function a(c) {
      try {
        d(r.throw(c));
      } catch (f) {
        o(f);
      }
    }
    function d(c) {
      c.done ? s(c.value) : i(c.value).then(u, a);
    }
    d((r = r.apply(e, t || [])).next());
  });
};
const ec = {
  isLoading: !0,
  user: void 0,
  error: void 0
};
class tc {
  constructor(t, n) {
    this.tx = Gn(), this.getLocalId = (r) => this.core.getLocalId(r), this.useLocalId = (r) => {
      const [i, s] = ei(null);
      return ke(() => {
        Xa(this, void 0, void 0, function* () {
          const u = yield this.getLocalId(r);
          s(u);
        });
      }, [r]), i;
    }, this.rooms = Ee, this.transact = (r) => this.core.transact(r), this.useQuery = (r, i) => za(this.core, r, i).state, this.useAuth = () => {
      const r = kt(this.core._reactor._currentUserCached), i = Se((o) => this.core.subscribeAuth((a) => {
        r.current = Object.assign({ isLoading: !1 }, a), o();
      }), []);
      return rn(i, () => r.current, () => ec);
    }, this.useUser = () => {
      const { user: r } = this.useAuth();
      if (!r)
        throw new ze("useUser must be used within an auth-protected route");
      return r;
    }, this.useConnectionStatus = () => {
      const r = kt(this.core._reactor.status), i = Se((o) => this.core.subscribeConnectionStatus((a) => {
        a !== r.current && (r.current = a, o());
      }), []);
      return rn(
        i,
        () => r.current,
        // For SSR, always return 'connecting' as the initial state
        () => "connecting"
      );
    }, this.queryOnce = (r, i) => this.core.queryOnce(r, i), this.SignedIn = ({ children: r }) => {
      const i = this.useAuth();
      return i.isLoading || i.error || !i.user ? null : z.jsx(z.Fragment, { children: r });
    }, this.SignedOut = ({ children: r }) => {
      const i = this.useAuth();
      return i.isLoading || i.error || i.user ? null : z.jsx(z.Fragment, { children: r });
    }, this.core = La(
      t,
      // @ts-expect-error because TS can't resolve subclass statics
      this.constructor.Storage,
      // @ts-expect-error because TS can't resolve subclass statics
      this.constructor.NetworkListener,
      n,
      // @ts-expect-error because TS can't resolve subclass statics
      this.constructor.EventSourceImpl
    ), this._core = this.core, this.auth = this.core.auth, this.storage = this.core.storage;
  }
  /**
   * Obtain a handle to a room, which allows you to listen to topics and presence data
   *
   * If you don't provide a `type` or `id`, Instant will default to `_defaultRoomType` and `_defaultRoomId`
   * as the room type and id, respectively.
   *
   * @see https://instantdb.com/docs/presence-and-topics
   *
   * @example
   *  const room = db.room('chat', roomId);
   *  const { peers } = db.rooms.usePresence(room);
   */
  room(t = "_defaultRoomType", n = "_defaultRoomId") {
    return new Za(this.core, t, n);
  }
  /**
   * One time query for the logged in state. This is useful
   * for scenarios where you want to know the current auth
   * state without subscribing to changes.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const user = await db.getAuth();
   *   console.log('logged in as', user.email)
   */
  getAuth() {
    return this.core.getAuth();
  }
}
class Qr extends Error {
  constructor(t, n) {
    super(t), this.name = "ParseError", this.type = n.type, this.field = n.field, this.value = n.value, this.line = n.line;
  }
}
function nn(e) {
}
function nc(e) {
  if (typeof e == "function")
    throw new TypeError(
      "`callbacks` must be an object, got a function instead. Did you mean `{onEvent: fn}`?"
    );
  const { onEvent: t = nn, onError: n = nn, onRetry: r = nn, onComment: i } = e;
  let s = "", o = !0, u, a = "", d = "";
  function c(b) {
    const g = o ? b.replace(/^\xEF\xBB\xBF/, "") : b, [T, w] = rc(`${s}${g}`);
    for (const v of T)
      f(v);
    s = w, o = !1;
  }
  function f(b) {
    if (b === "") {
      p();
      return;
    }
    if (b.startsWith(":")) {
      i && i(b.slice(b.startsWith(": ") ? 2 : 1));
      return;
    }
    const g = b.indexOf(":");
    if (g !== -1) {
      const T = b.slice(0, g), w = b[g + 1] === " " ? 2 : 1, v = b.slice(g + w);
      h(T, v, b);
      return;
    }
    h(b, "", b);
  }
  function h(b, g, T) {
    switch (b) {
      case "event":
        d = g;
        break;
      case "data":
        a = `${a}${g}
`;
        break;
      case "id":
        u = g.includes("\0") ? void 0 : g;
        break;
      case "retry":
        /^\d+$/.test(g) ? r(parseInt(g, 10)) : n(
          new Qr(`Invalid \`retry\` value: "${g}"`, {
            type: "invalid-retry",
            value: g,
            line: T
          })
        );
        break;
      default:
        n(
          new Qr(
            `Unknown field "${b.length > 20 ? `${b.slice(0, 20)}…` : b}"`,
            { type: "unknown-field", field: b, value: g, line: T }
          )
        );
        break;
    }
  }
  function p() {
    a.length > 0 && t({
      id: u,
      event: d || void 0,
      // If the data buffer's last character is a U+000A LINE FEED (LF) character,
      // then remove the last character from the data buffer.
      data: a.endsWith(`
`) ? a.slice(0, -1) : a
    }), u = void 0, a = "", d = "";
  }
  function _(b = {}) {
    s && b.consume && f(s), o = !0, u = void 0, a = "", d = "", s = "";
  }
  return { feed: c, reset: _ };
}
function rc(e) {
  const t = [];
  let n = "", r = 0;
  for (; r < e.length; ) {
    const i = e.indexOf("\r", r), s = e.indexOf(`
`, r);
    let o = -1;
    if (i !== -1 && s !== -1 ? o = Math.min(i, s) : i !== -1 ? o = i : s !== -1 && (o = s), o === -1) {
      n = e.slice(r);
      break;
    } else {
      const u = e.slice(r, o);
      t.push(u), r = o + 1, e[r - 1] === "\r" && e[r] === `
` && r++;
    }
  }
  return [t, n];
}
class Yr extends Event {
  /**
   * Constructs a new `ErrorEvent` instance. This is typically not called directly,
   * but rather emitted by the `EventSource` object when an error occurs.
   *
   * @param type - The type of the event (should be "error")
   * @param errorEventInitDict - Optional properties to include in the error event
   */
  constructor(t, n) {
    var r, i;
    super(t), this.code = (r = n == null ? void 0 : n.code) != null ? r : void 0, this.message = (i = n == null ? void 0 : n.message) != null ? i : void 0;
  }
  /**
   * Node.js "hides" the `message` and `code` properties of the `ErrorEvent` instance,
   * when it is `console.log`'ed. This makes it harder to debug errors. To ease debugging,
   * we explicitly include the properties in the `inspect` method.
   *
   * This is automatically called by Node.js when you `console.log` an instance of this class.
   *
   * @param _depth - The current depth
   * @param options - The options passed to `util.inspect`
   * @param inspect - The inspect function to use (prevents having to import it from `util`)
   * @returns A string representation of the error
   */
  [Symbol.for("nodejs.util.inspect.custom")](t, n, r) {
    return r(Hr(this), n);
  }
  /**
   * Deno "hides" the `message` and `code` properties of the `ErrorEvent` instance,
   * when it is `console.log`'ed. This makes it harder to debug errors. To ease debugging,
   * we explicitly include the properties in the `inspect` method.
   *
   * This is automatically called by Deno when you `console.log` an instance of this class.
   *
   * @param inspect - The inspect function to use (prevents having to import it from `util`)
   * @param options - The options passed to `Deno.inspect`
   * @returns A string representation of the error
   */
  [Symbol.for("Deno.customInspect")](t, n) {
    return t(Hr(this), n);
  }
}
function ic(e) {
  const t = globalThis.DOMException;
  return typeof t == "function" ? new t(e, "SyntaxError") : new SyntaxError(e);
}
function An(e) {
  return e instanceof Error ? "errors" in e && Array.isArray(e.errors) ? e.errors.map(An).join(", ") : "cause" in e && e.cause instanceof Error ? `${e}: ${An(e.cause)}` : e.message : `${e}`;
}
function Hr(e) {
  return {
    type: e.type,
    message: e.message,
    code: e.code,
    defaultPrevented: e.defaultPrevented,
    cancelable: e.cancelable,
    timeStamp: e.timeStamp
  };
}
var Li = (e) => {
  throw TypeError(e);
}, Hn = (e, t, n) => t.has(e) || Li("Cannot " + n), E = (e, t, n) => (Hn(e, t, "read from private field"), n ? n.call(e) : t.get(e)), F = (e, t, n) => t.has(e) ? Li("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(e) : t.set(e, n), $ = (e, t, n, r) => (Hn(e, t, "write to private field"), t.set(e, n), n), Te = (e, t, n) => (Hn(e, t, "access private method"), n), ee, Ae, xe, Ct, $t, it, Le, st, ve, De, Fe, Ue, et, fe, Cn, jn, kn, Jr, Pn, In, tt, Mn, Rn;
class jt extends EventTarget {
  constructor(t, n) {
    var r, i;
    super(), F(this, fe), this.CONNECTING = 0, this.OPEN = 1, this.CLOSED = 2, F(this, ee), F(this, Ae), F(this, xe), F(this, Ct), F(this, $t), F(this, it), F(this, Le), F(this, st, null), F(this, ve), F(this, De), F(this, Fe, null), F(this, Ue, null), F(this, et, null), F(this, jn, async (s) => {
      var o;
      E(this, De).reset();
      const { body: u, redirected: a, status: d, headers: c } = s;
      if (d === 204) {
        Te(this, fe, tt).call(this, "Server sent HTTP 204, not reconnecting", 204), this.close();
        return;
      }
      if (a ? $(this, xe, new URL(s.url)) : $(this, xe, void 0), d !== 200) {
        Te(this, fe, tt).call(this, `Non-200 status code (${d})`, d);
        return;
      }
      if (!(c.get("content-type") || "").startsWith("text/event-stream")) {
        Te(this, fe, tt).call(this, 'Invalid content type, expected "text/event-stream"', d);
        return;
      }
      if (E(this, ee) === this.CLOSED)
        return;
      $(this, ee, this.OPEN);
      const f = new Event("open");
      if ((o = E(this, et)) == null || o.call(this, f), this.dispatchEvent(f), typeof u != "object" || !u || !("getReader" in u)) {
        Te(this, fe, tt).call(this, "Invalid response body, expected a web ReadableStream", d), this.close();
        return;
      }
      const h = new TextDecoder(), p = u.getReader();
      let _ = !0;
      do {
        const { done: b, value: g } = await p.read();
        g && E(this, De).feed(h.decode(g, { stream: !b })), b && (_ = !1, E(this, De).reset(), Te(this, fe, Mn).call(this));
      } while (_);
    }), F(this, kn, (s) => {
      $(this, ve, void 0), !(s.name === "AbortError" || s.type === "aborted") && Te(this, fe, Mn).call(this, An(s));
    }), F(this, Pn, (s) => {
      typeof s.id == "string" && $(this, st, s.id);
      const o = new MessageEvent(s.event || "message", {
        data: s.data,
        origin: E(this, xe) ? E(this, xe).origin : E(this, Ae).origin,
        lastEventId: s.id || ""
      });
      E(this, Ue) && (!s.event || s.event === "message") && E(this, Ue).call(this, o), this.dispatchEvent(o);
    }), F(this, In, (s) => {
      $(this, it, s);
    }), F(this, Rn, () => {
      $(this, Le, void 0), E(this, ee) === this.CONNECTING && Te(this, fe, Cn).call(this);
    });
    try {
      if (t instanceof URL)
        $(this, Ae, t);
      else if (typeof t == "string")
        $(this, Ae, new URL(t, sc()));
      else
        throw new Error("Invalid URL");
    } catch {
      throw ic("An invalid or illegal string was specified");
    }
    $(this, De, nc({
      onEvent: E(this, Pn),
      onRetry: E(this, In)
    })), $(this, ee, this.CONNECTING), $(this, it, 3e3), $(this, $t, (r = n == null ? void 0 : n.fetch) != null ? r : globalThis.fetch), $(this, Ct, (i = n == null ? void 0 : n.withCredentials) != null ? i : !1), Te(this, fe, Cn).call(this);
  }
  /**
   * Returns the state of this EventSource object's connection. It can have the values described below.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/readyState)
   *
   * Note: typed as `number` instead of `0 | 1 | 2` for compatibility with the `EventSource` interface,
   * defined in the TypeScript `dom` library.
   *
   * @public
   */
  get readyState() {
    return E(this, ee);
  }
  /**
   * Returns the URL providing the event stream.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/url)
   *
   * @public
   */
  get url() {
    return E(this, Ae).href;
  }
  /**
   * Returns true if the credentials mode for connection requests to the URL providing the event stream is set to "include", and false otherwise.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/withCredentials)
   */
  get withCredentials() {
    return E(this, Ct);
  }
  /** [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/error_event) */
  get onerror() {
    return E(this, Fe);
  }
  set onerror(t) {
    $(this, Fe, t);
  }
  /** [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/message_event) */
  get onmessage() {
    return E(this, Ue);
  }
  set onmessage(t) {
    $(this, Ue, t);
  }
  /** [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/open_event) */
  get onopen() {
    return E(this, et);
  }
  set onopen(t) {
    $(this, et, t);
  }
  addEventListener(t, n, r) {
    const i = n;
    super.addEventListener(t, i, r);
  }
  removeEventListener(t, n, r) {
    const i = n;
    super.removeEventListener(t, i, r);
  }
  /**
   * Aborts any instances of the fetch algorithm started for this EventSource object, and sets the readyState attribute to CLOSED.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/close)
   *
   * @public
   */
  close() {
    E(this, Le) && clearTimeout(E(this, Le)), E(this, ee) !== this.CLOSED && (E(this, ve) && E(this, ve).abort(), $(this, ee, this.CLOSED), $(this, ve, void 0));
  }
}
ee = /* @__PURE__ */ new WeakMap(), Ae = /* @__PURE__ */ new WeakMap(), xe = /* @__PURE__ */ new WeakMap(), Ct = /* @__PURE__ */ new WeakMap(), $t = /* @__PURE__ */ new WeakMap(), it = /* @__PURE__ */ new WeakMap(), Le = /* @__PURE__ */ new WeakMap(), st = /* @__PURE__ */ new WeakMap(), ve = /* @__PURE__ */ new WeakMap(), De = /* @__PURE__ */ new WeakMap(), Fe = /* @__PURE__ */ new WeakMap(), Ue = /* @__PURE__ */ new WeakMap(), et = /* @__PURE__ */ new WeakMap(), fe = /* @__PURE__ */ new WeakSet(), /**
* Connect to the given URL and start receiving events
*
* @internal
*/
Cn = function() {
  $(this, ee, this.CONNECTING), $(this, ve, new AbortController()), E(this, $t)(E(this, Ae), Te(this, fe, Jr).call(this)).then(E(this, jn)).catch(E(this, kn));
}, jn = /* @__PURE__ */ new WeakMap(), kn = /* @__PURE__ */ new WeakMap(), /**
* Get request options for the `fetch()` request
*
* @returns The request options
* @internal
*/
Jr = function() {
  var e;
  const t = {
    // [spec] Let `corsAttributeState` be `Anonymous`…
    // [spec] …will have their mode set to "cors"…
    mode: "cors",
    redirect: "follow",
    headers: { Accept: "text/event-stream", ...E(this, st) ? { "Last-Event-ID": E(this, st) } : void 0 },
    cache: "no-store",
    signal: (e = E(this, ve)) == null ? void 0 : e.signal
  };
  return "window" in globalThis && (t.credentials = this.withCredentials ? "include" : "same-origin"), t;
}, Pn = /* @__PURE__ */ new WeakMap(), In = /* @__PURE__ */ new WeakMap(), /**
* Handles the process referred to in the EventSource specification as "failing a connection".
*
* @param error - The error causing the connection to fail
* @param code - The HTTP status code, if available
* @internal
*/
tt = function(e, t) {
  var n;
  E(this, ee) !== this.CLOSED && $(this, ee, this.CLOSED);
  const r = new Yr("error", { code: t, message: e });
  (n = E(this, Fe)) == null || n.call(this, r), this.dispatchEvent(r);
}, /**
* Schedules a reconnection attempt against the EventSource endpoint.
*
* @param message - The error causing the connection to fail
* @param code - The HTTP status code, if available
* @internal
*/
Mn = function(e, t) {
  var n;
  if (E(this, ee) === this.CLOSED)
    return;
  $(this, ee, this.CONNECTING);
  const r = new Yr("error", { code: t, message: e });
  (n = E(this, Fe)) == null || n.call(this, r), this.dispatchEvent(r), $(this, Le, setTimeout(E(this, Rn), E(this, it)));
}, Rn = /* @__PURE__ */ new WeakMap(), /**
* ReadyState representing an EventSource currently trying to connect
*
* @public
*/
jt.CONNECTING = 0, /**
* ReadyState representing an EventSource connection that is open (eg connected)
*
* @public
*/
jt.OPEN = 1, /**
* ReadyState representing an EventSource connection that is closed (eg disconnected)
*
* @public
*/
jt.CLOSED = 2;
function sc() {
  const e = "document" in globalThis ? globalThis.document : void 0;
  return e && typeof e == "object" && "baseURI" in e && typeof e.baseURI == "string" ? e.baseURI : void 0;
}
const Jn = class Jn extends tc {
};
Jn.EventSourceImpl = jt;
let $n = Jn;
function oc(e) {
  return new $n(
    e,
    {
      "@instantdb/react": Yn
    }
  );
}
const pc = oc;
function yc({
  as: e = "div",
  spaceId: t,
  room: n,
  className: r,
  style: i,
  userCursorColor: s,
  children: o,
  renderCursor: u,
  propagate: a,
  zIndex: d
}) {
  const c = t || `cursors-space-default--${String(n.type)}-${n.id}`, f = n.usePresence({
    keys: [c]
  }), h = n._core._reactor.getPresence(n.type, n.id);
  function p(w, v) {
    const I = v.clientX, O = v.clientY, U = (I - w.left) / w.width * 100, W = (O - w.top) / w.height * 100;
    f.publishPresence({
      [c]: {
        x: I,
        y: O,
        xPercent: U,
        yPercent: W,
        color: s
      }
    });
  }
  function _(w) {
    a || w.stopPropagation();
    const v = w.currentTarget.getBoundingClientRect();
    p(v, w);
  }
  function b(w) {
    f.publishPresence({
      [c]: void 0
    });
  }
  function g(w) {
    if (w.touches.length !== 1)
      return;
    const v = w.touches[0];
    if (v.target instanceof Element) {
      a || w.stopPropagation();
      const I = v.target.getBoundingClientRect();
      p(I, v);
    }
  }
  function T(w) {
    f.publishPresence({
      [c]: void 0
    });
  }
  return fs(
    e,
    {
      onMouseMove: _,
      onMouseOut: b,
      onTouchMove: g,
      onTouchEnd: T,
      className: r,
      style: {
        position: "relative",
        ...i
      }
    },
    [
      o,
      /* @__PURE__ */ z.jsx(
        "div",
        {
          style: {
            ...Zr,
            ...ac,
            zIndex: d !== void 0 ? d : cc
          },
          children: Object.entries(f.peers).map(([w, v]) => {
            const I = v[c];
            return I ? /* @__PURE__ */ z.jsx(
              "div",
              {
                style: {
                  ...Zr,
                  transform: `translate(${I.xPercent}%, ${I.yPercent}%)`,
                  transformOrigin: "0 0",
                  transition: "transform 100ms"
                },
                children: u ? u({
                  color: I.color,
                  presence: h.peers[w]
                }) : /* @__PURE__ */ z.jsx(uc, { ...I })
              },
              w
            ) : null;
          })
        },
        c
      )
    ]
  );
}
function uc({ color: e }) {
  const n = e || "black";
  return /* @__PURE__ */ z.jsxs(
    "svg",
    {
      style: { height: 35, width: 35 },
      viewBox: "0 0 35 35",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        /* @__PURE__ */ z.jsxs(
          "g",
          {
            fill: "rgba(0,0,0,.2)",
            transform: "matrix(1, 0, 0, 1, -11.999999046325684, -8.406899452209473)",
            children: [
              /* @__PURE__ */ z.jsx("path", { d: "m12 24.4219v-16.015l11.591 11.619h-6.781l-.411.124z" }),
              /* @__PURE__ */ z.jsx("path", { d: "m21.0845 25.0962-3.605 1.535-4.682-11.089 3.686-1.553z" })
            ]
          }
        ),
        /* @__PURE__ */ z.jsxs(
          "g",
          {
            fill: "white",
            transform: "matrix(1, 0, 0, 1, -11.999999046325684, -8.406899452209473)",
            children: [
              /* @__PURE__ */ z.jsx("path", { d: "m12 24.4219v-16.015l11.591 11.619h-6.781l-.411.124z" }),
              /* @__PURE__ */ z.jsx("path", { d: "m21.0845 25.0962-3.605 1.535-4.682-11.089 3.686-1.553z" })
            ]
          }
        ),
        /* @__PURE__ */ z.jsxs(
          "g",
          {
            fill: n,
            transform: "matrix(1, 0, 0, 1, -11.999999046325684, -8.406899452209473)",
            children: [
              /* @__PURE__ */ z.jsx("path", { d: "m19.751 24.4155-1.844.774-3.1-7.374 1.841-.775z" }),
              /* @__PURE__ */ z.jsx("path", { d: "m13 10.814v11.188l2.969-2.866.428-.139h4.768z" })
            ]
          }
        )
      ]
    }
  );
}
const Zr = {
  position: "absolute",
  top: 0,
  left: 0,
  bottom: 0,
  right: 0
}, ac = {
  overflow: "hidden",
  pointerEvents: "none",
  userSelect: "none"
}, cc = 99999;
export {
  yc as Cursors,
  Lt as InstantAPIError,
  tc as InstantReactAbstractDatabase,
  $n as InstantReactWebDatabase,
  hc as i,
  B as id,
  oc as init,
  pc as init_experimental,
  fc as lookup,
  lc as tx
};
